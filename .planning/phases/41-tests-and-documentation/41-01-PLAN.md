---
phase: 41-tests-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/file/array/array-hof-iter.flt
  - tests/flt/file/array/array-hof-map.flt
  - tests/flt/file/array/array-hof-fold.flt
  - tests/flt/file/array/array-hof-init.flt
autonomous: true

must_haves:
  truths:
    - "Array.iter prints each element in order"
    - "Array.map produces a new array with transformed elements"
    - "Array.fold accumulates a result across all elements"
    - "Array.init creates an array from an index function"
    - "All existing array and hashtable flt tests still pass"
  artifacts:
    - path: "tests/flt/file/array/array-hof-iter.flt"
      provides: "flt test for Array.iter"
    - path: "tests/flt/file/array/array-hof-map.flt"
      provides: "flt test for Array.map"
    - path: "tests/flt/file/array/array-hof-fold.flt"
      provides: "flt test for Array.fold"
    - path: "tests/flt/file/array/array-hof-init.flt"
      provides: "flt test for Array.init"
  key_links:
    - from: "flt test files"
      to: "Prelude/Array.fun wrappers"
      via: "Array.iter / Array.map / Array.fold / Array.init module-qualified calls"
      pattern: "Array\\.(iter|map|fold|init)"
---

<objective>
Write four flt tests covering Array higher-order functions: iter, map, fold, init (TST-20). Run all existing array and hashtable flt tests to confirm nothing regressed.

Purpose: TST-18, TST-19, TST-21, TST-22 are already covered by flt tests from Phases 38 and 39. TST-20 (HOF builtins added in Phase 40) has zero flt coverage. This plan closes that gap and validates the complete test suite.
Output: Four new .flt files in tests/flt/file/array/; all flt tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-array-higher-order/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write flt tests for Array.iter, Array.map, Array.fold, Array.init</name>
  <files>
    tests/flt/file/array/array-hof-iter.flt
    tests/flt/file/array/array-hof-map.flt
    tests/flt/file/array/array-hof-fold.flt
    tests/flt/file/array/array-hof-init.flt
  </files>
  <action>
Create four flt test files following the same format as the existing array tests. Each file has:
- A comment line describing the test
- `// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`
- `// --- Input:` section with LangThree source
- `// --- Output:` section with expected stdout

CRITICAL SYNTAX RULES (from Phase 40 and STATE.md decisions):
- Multi-arg lambdas are a parse error: use `fun a -> fun b -> ...` not `fun a b ->`
- fold callback must be `fun acc -> fun x -> ...`
- Use `let _ =` not `let () =` for unit-returning expressions at module level
- Array.iter/map/fold/init are accessed via module-qualified name (no `open Array`)

FILE 1 — array-hof-iter.flt:
```
// Test: Array.iter applies function to each element in order (TST-20)
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let arr = Array.ofList [1; 2; 3]
let _ = Array.iter (fun x -> println (to_string x)) arr
// --- Output:
1
2
3
```

FILE 2 — array-hof-map.flt:
```
// Test: Array.map transforms each element, producing a new array (TST-20)
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let arr = Array.ofList [1; 2; 3]
let doubled = Array.map (fun x -> x * 2) arr
let _ = println (to_string (Array.get doubled 0))
let _ = println (to_string (Array.get doubled 1))
let _ = println (to_string (Array.get doubled 2))
// --- Output:
2
4
6
```

FILE 3 — array-hof-fold.flt:
```
// Test: Array.fold accumulates result across all elements (TST-20)
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let arr = Array.ofList [1; 2; 3; 4; 5]
let sum = Array.fold (fun acc -> fun x -> acc + x) 0 arr
let _ = println (to_string sum)
// --- Output:
15
```

FILE 4 — array-hof-init.flt:
```
// Test: Array.init creates array from index function (TST-20)
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let arr = Array.init 4 (fun i -> i * i)
let _ = println (to_string (Array.get arr 0))
let _ = println (to_string (Array.get arr 1))
let _ = println (to_string (Array.get arr 2))
let _ = println (to_string (Array.get arr 3))
// --- Output:
0
1
4
9
```

Write the files exactly as shown, without the surrounding backtick fence — just the raw content.
  </action>
  <verify>
Run: `/Users/ohama/.local/bin/fslit tests/flt/file/array/array-hof-iter.flt tests/flt/file/array/array-hof-map.flt tests/flt/file/array/array-hof-fold.flt tests/flt/file/array/array-hof-init.flt`

All four tests must pass (exit 0, no FAIL lines).
  </verify>
  <done>Four .flt files exist under tests/flt/file/array/; all four tests pass when run with the flt runner.</done>
</task>

<task type="auto">
  <name>Task 2: Run full flt suite and confirm no regressions</name>
  <files></files>
  <action>
Run the complete flt test suite to verify all existing tests (Phases 38 and 39) still pass alongside the new HOF tests.

Command: `/Users/ohama/.local/bin/fslit tests/flt/`

If any tests FAIL, read the failing test file and the error output, diagnose the issue, and fix it. Common issues:
- Wrong expected output (fix the .flt file to match actual interpreter output)
- Builtin not available (means the feature was not implemented — escalate, do not mark as done)

The suite must exit 0 with no FAIL lines before this task is complete.
  </action>
  <verify>
`/Users/ohama/.local/bin/fslit tests/flt/` exits 0 with no FAIL lines in the output.
  </verify>
  <done>All flt tests pass. TST-18, TST-19, TST-20, TST-21, TST-22 are fully covered and verified green.</done>
</task>

</tasks>

<verification>
- `ls tests/flt/file/array/` shows array-hof-iter.flt, array-hof-map.flt, array-hof-fold.flt, array-hof-init.flt alongside the four Phase 38 tests
- `/Users/ohama/.local/bin/fslit tests/flt/` exits 0 with all tests passing
- No FAIL lines in flt runner output
</verification>

<success_criteria>
- All four Array HOF flt tests created and passing (TST-20 complete)
- All Phase 38 array tests still passing (TST-18, TST-19 confirmed)
- All Phase 39 hashtable tests still passing (TST-21, TST-22 confirmed)
- flt suite exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/41-tests-and-documentation/41-01-SUMMARY.md`
</output>
