---
phase: 41-tests-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tutorial/19-mutable-data.md
  - tutorial/SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "A tutorial chapter exists covering Array and Hashtable with runnable examples"
    - "All code examples in the chapter produce the correct output when run with the interpreter"
    - "The chapter is linked in SUMMARY.md under an appropriate section"
  artifacts:
    - path: "tutorial/19-mutable-data.md"
      provides: "Tutorial chapter for mutable data structures (Array + Hashtable)"
    - path: "tutorial/SUMMARY.md"
      provides: "Updated table of contents linking to the new chapter"
      contains: "19-mutable-data.md"
  key_links:
    - from: "tutorial/SUMMARY.md"
      to: "tutorial/19-mutable-data.md"
      via: "mdBook link entry"
      pattern: "19-mutable-data"
---

<objective>
Write the tutorial chapter for mutable data structures (Array and Hashtable) in Korean, following the style and structure of existing chapters. Add it to SUMMARY.md so it appears in the tutorial book. Verify all code examples produce correct output. (TST-23)

Purpose: Users need documentation to understand when and how to use Arrays and Hashtables — the two mutable data structures added in v3.0. The chapter completes the v3.0 milestone documentation.
Output: tutorial/19-mutable-data.md with runnable examples; updated SUMMARY.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tutorial/SUMMARY.md
@tutorial/15-algorithms.md
@tutorial/18-file-io.md
@.planning/phases/40-array-higher-order/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tutorial/19-mutable-data.md (Array + Hashtable chapter)</name>
  <files>tutorial/19-mutable-data.md</files>
  <action>
Create tutorial/19-mutable-data.md. Write it in Korean following the style of existing chapters (e.g., 15-algorithms.md, 18-file-io.md). No "N장:" prefix in the H1 — match the convention of 18-file-io.md which uses a plain Korean title followed by English in parentheses.

CHAPTER STRUCTURE:

```
# 가변 데이터 구조 (Mutable Data Structures)

[Introductory paragraph: LangThree is primarily functional/immutable, but Array and Hashtable provide mutable state when needed — e.g., accumulating results efficiently, building lookup tables.]

## 배열 (Array)

[Intro: fixed-length, O(1) index access, elements mutable in place.]

### 생성과 기본 연산

[Show Array.create, Array.get, Array.length, Array.set with code block and expected output.]

### 리스트 변환

[Show Array.ofList and Array.toList round-trip.]

### 고차 함수

[Show Array.iter, Array.map, Array.fold, Array.init with examples. For fold, emphasize curried callback: `fun acc -> fun x -> ...`]

### 주의사항

[Array.get raises a catchable exception on out-of-bounds. Arrays use reference equality (two distinct arrays are never equal by value). No `open Array` needed — use module-qualified names.]

## 해시테이블 (Hashtable)

[Intro: dynamic key-value store, any Value as key/value, mutable.]

### 생성과 기본 연산

[Show Hashtable.create, Hashtable.set, Hashtable.get, Hashtable.containsKey with example.]

### 키 목록과 삭제

[Show Hashtable.keys (returns a list), Hashtable.remove.]

### 주의사항

[Key order from Hashtable.keys is not guaranteed. Hashtable.set on an existing key overwrites the value. No `open Hashtable` needed.]

## 언제 사용할까?

[Brief guidance: prefer immutable lists for most functional code; use Array when O(1) random access or in-place mutation matters; use Hashtable for dynamic dictionaries / frequency counts / caches.]
```

CODE BLOCK FORMAT — use plain fenced blocks (no language tag) as in other tutorial chapters:
```
let arr = Array.create 3 0
let _ = Array.set arr 1 42
let result = Array.get arr 1
let _ = println (to_string result)
```
Expected output shown as a comment or inline text after the block, matching the style of 18-file-io.md (showing `$ langthree file.l3` style) OR as a separate paragraph.

CRITICAL SYNTAX (from accumulated decisions — include these points where relevant):
- Multi-arg lambdas are a parse error: always use `fun a -> fun b -> ...`
- fold callback: `fun acc -> fun x -> acc + x`
- Use `let _ =` not `let () =` for unit expressions at top level
- Array prints as [|1; 2; 3|] when passed to to_string
- Hashtable.keys order is non-deterministic — do not show output that relies on key order

All code examples must be runnable against the Release binary:
`/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree`

Verify each example by running it with the binary before finalizing the file. Adjust expected outputs to match actual interpreter output.
  </action>
  <verify>
1. File tutorial/19-mutable-data.md exists and is non-empty.
2. Run each code example against the Release binary and confirm output matches what the chapter states.
3. Command for each example: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /tmp/example.l3`
  </verify>
  <done>tutorial/19-mutable-data.md exists; all code examples produce the output shown in the chapter when run with the interpreter.</done>
</task>

<task type="auto">
  <name>Task 2: Add chapter to tutorial/SUMMARY.md</name>
  <files>tutorial/SUMMARY.md</files>
  <action>
Read tutorial/SUMMARY.md. Add the new chapter under the "실용 프로그래밍" section, after the "파일 I/O와 시스템 함수" entry (18-file-io.md). Insert:

```
- [가변 데이터 구조](19-mutable-data.md)
```

The updated "실용 프로그래밍" section should look like:

```
# 실용 프로그래밍

- [문자열과 출력](07-strings-and-output.md)
- [문자 타입](17-char-type.md)
- [파이프와 합성](08-pipes-and-composition.md)
- [Prelude 표준 라이브러리](09-prelude.md)
- [모듈과 네임스페이스](10-modules.md)
- [파일 I/O와 시스템 함수](18-file-io.md)
- [가변 데이터 구조](19-mutable-data.md)
```

Write the updated SUMMARY.md.
  </action>
  <verify>
`grep "19-mutable-data" tutorial/SUMMARY.md` returns a match.
  </verify>
  <done>tutorial/SUMMARY.md contains an entry for 19-mutable-data.md in the 실용 프로그래밍 section.</done>
</task>

</tasks>

<verification>
- `ls tutorial/19-mutable-data.md` exists
- `grep "19-mutable-data" tutorial/SUMMARY.md` matches
- Each code example in the chapter produces correct output when run with the Release binary
- Chapter covers: Array.create/get/set/length, Array.ofList/toList, Array.iter/map/fold/init, Hashtable.create/set/get/containsKey/keys/remove
</verification>

<success_criteria>
- tutorial/19-mutable-data.md written in Korean, covering Array and Hashtable with verified runnable examples (TST-23 complete)
- tutorial/SUMMARY.md updated with link to new chapter
- No code example in the chapter is incorrect or produces wrong output
</success_criteria>

<output>
After completion, create `.planning/phases/41-tests-and-documentation/41-02-SUMMARY.md`
</output>
