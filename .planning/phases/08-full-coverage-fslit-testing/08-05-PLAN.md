---
phase: 08-full-coverage-fslit-testing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/emit/ast-pat-variable.flt
  - tests/flt/emit/ast-pat-wildcard.flt
  - tests/flt/emit/ast-pat-const-int.flt
  - tests/flt/emit/ast-pat-const-bool.flt
  - tests/flt/emit/ast-pat-constructor.flt
  - tests/flt/emit/ast-pat-constructor-arg.flt
  - tests/flt/emit/ast-pat-tuple.flt
  - tests/flt/emit/ast-pat-emptylist.flt
  - tests/flt/emit/ast-pat-cons.flt
  - tests/flt/emit/ast-pat-record.flt
  - tests/flt/emit/ast-pat-nested.flt
  - tests/flt/emit/ast-pat-when-guard.flt
  - tests/flt/emit/ast-pat-string-const.flt
autonomous: true

must_haves:
  truths:
    - "Every pattern AST node type has at least one --emit-ast test"
    - "Pattern nodes are tested within match expressions"
    - "Nested patterns and when guards are tested"
    - "All tests pass via fslit"
  artifacts:
    - path: "tests/flt/emit/ast-pat-variable.flt"
      provides: "VarPat AST test"
    - path: "tests/flt/emit/ast-pat-constructor-arg.flt"
      provides: "ConstructorPat with argument AST test"
    - path: "tests/flt/emit/ast-pat-record.flt"
      provides: "RecordPat AST test"
    - path: "tests/flt/emit/ast-pat-nested.flt"
      provides: "Nested pattern combinations AST test"
    - path: "tests/flt/emit/ast-pat-when-guard.flt"
      provides: "When guard in match clause AST test"
  key_links: []
---

<objective>
Create --emit-ast tests for all pattern AST node types, verified within match expressions.

Purpose: Verify the parser produces correct AST for every pattern construct used in match/let destructuring.
Output: 13 .flt test files in tests/flt/emit/ covering all pattern AST nodes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Key info:
- LangThree binary: src/LangThree/bin/Release/net10.0/LangThree
- fslit binary: /Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit
- Patterns appear inside Match expressions in the AST output
- Match output format: `Match (scrutinee, [(pattern1, expr1); (pattern2, expr2)])`
- When guards: The formatAst for Match shows `(pat, expr)` pairs -- check if when guards are visible in AST output (the MatchClause is `Pattern * Expr option * Expr` but formatAst may skip the guard)

Known pattern format nodes:
- `VarPat "x"` -- variable pattern
- `WildcardPat` -- underscore pattern
- `ConstPat (IntConst 0)` -- integer constant pattern
- `ConstPat (BoolConst true)` -- boolean constant pattern
- `ConstructorPat "Red"` -- constructor without args
- `ConstructorPat ("Some", VarPat "x")` -- constructor with arg
- `TuplePat [VarPat "a", VarPat "b"]` -- tuple pattern
- `EmptyListPat` -- empty list pattern []
- `ConsPat (VarPat "h", VarPat "t")` -- cons pattern (::)
- `RecordPat {x = VarPat "a"; y = VarPat "b"}` -- record pattern

IMPORTANT: Check if when guards appear in formatAst output. The Match formatter shows `(pat, expr)` pairs. If guards are NOT shown, note this in the test description. Use --expr mode for simple patterns, file mode (with type declarations) for constructor and record patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture --emit-ast output for all pattern constructs</name>
  <files>N/A (exploration only)</files>
  <action>
Run the LangThree binary to capture AST output showing each pattern type. Use --expr mode where possible, file mode for patterns needing type declarations.

**Expr-mode patterns (--emit-ast --expr):**

1. **Variable pattern:** `match 42 with | x -> x` -- shows VarPat "x"
2. **Wildcard pattern:** `match 42 with | _ -> 0` -- shows WildcardPat
3. **Int constant pattern:** `match 1 with | 0 -> "zero" | _ -> "other"` -- shows ConstPat (IntConst 0)
4. **Bool constant pattern:** `match true with | true -> 1 | false -> 0` -- shows ConstPat (BoolConst true/false)
5. **Tuple pattern:** `match (1, 2) with | (a, b) -> a + b` -- shows TuplePat
6. **Empty list pattern:** `match [] with | [] -> 0 | _ -> 1` -- shows EmptyListPat
7. **Cons pattern:** `match 1 :: [] with | x :: xs -> x | [] -> 0` -- shows ConsPat
8. **When guard:** `match 5 with | x when x > 0 -> "pos" | _ -> "neg"` -- check if guard appears in AST

**File-mode patterns (--emit-ast %input):**

9. **Constructor pattern (no arg):** needs `type Color = Red | Green` + match
10. **Constructor pattern (with arg):** needs `type Option 'a = Some of 'a | None` + match
11. **Record pattern:** needs `type Point = { x: int; y: int }` + match on record
12. **Nested pattern:** Constructor containing tuple or list pattern
13. **String constant pattern:** if the parser supports string patterns in match -- test with `match "hi" with | "hi" -> 1 | _ -> 0` (may not be supported, check)

Verify each output and note exact format. Pay special attention to when guard representation in AST.
  </action>
  <verify>All commands produce pattern AST nodes in the output with exit code 0</verify>
  <done>AST output captured for all pattern types</done>
</task>

<task type="auto">
  <name>Task 2: Create .flt test files and verify with fslit</name>
  <files>
    tests/flt/emit/ast-pat-variable.flt
    tests/flt/emit/ast-pat-wildcard.flt
    tests/flt/emit/ast-pat-const-int.flt
    tests/flt/emit/ast-pat-const-bool.flt
    tests/flt/emit/ast-pat-constructor.flt
    tests/flt/emit/ast-pat-constructor-arg.flt
    tests/flt/emit/ast-pat-tuple.flt
    tests/flt/emit/ast-pat-emptylist.flt
    tests/flt/emit/ast-pat-cons.flt
    tests/flt/emit/ast-pat-record.flt
    tests/flt/emit/ast-pat-nested.flt
    tests/flt/emit/ast-pat-when-guard.flt
    tests/flt/emit/ast-pat-string-const.flt
  </files>
  <action>
Create .flt test files. Use --expr format for simple patterns, %input format for patterns needing type declarations.

For --expr mode:
```
// Test: --emit-ast pattern: [description]
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --emit-ast --expr "[match expression]"
// --- Output:
[AST output]
```

For file mode:
```
// Test: --emit-ast pattern: [description]
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --emit-ast %input
// --- Input:
[type declarations and match expression]
// --- Output:
[AST output]
```

If string constant patterns are not supported by the parser, skip ast-pat-string-const.flt and document why.

After creating, run fslit:
```
/Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit tests/flt/emit/ast-pat-*.flt
```
  </action>
  <verify>
Run: `/Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit tests/flt/emit/ast-pat-*.flt`
All tests pass (exit code 0).
  </verify>
  <done>All pattern AST emit tests created and passing via fslit</done>
</task>

</tasks>

<verification>
1. Run fslit on all ast-pat tests -- all pass
2. Run fslit on ALL tests -- no regressions
3. Verify coverage: VarPat, WildcardPat, ConstPat (Int, Bool), ConstructorPat (with/without arg), TuplePat, EmptyListPat, ConsPat, RecordPat, nested patterns, when guards
</verification>

<success_criteria>
- Every pattern node type (VarPat, WildcardPat, ConstPat, ConstructorPat, TuplePat, EmptyListPat, ConsPat, RecordPat) has at least one test
- Nested pattern combinations tested
- When guard representation tested (even if AST doesn't show it, document this)
- All tests pass via fslit with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-coverage-fslit-testing/08-05-SUMMARY.md`
</output>
