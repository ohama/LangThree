---
phase: 08-full-coverage-fslit-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/emit/ast-decl-let.flt
  - tests/flt/emit/ast-decl-func.flt
  - tests/flt/emit/ast-decl-adt-simple.flt
  - tests/flt/emit/ast-decl-adt-parametric.flt
  - tests/flt/emit/ast-decl-adt-mutual.flt
  - tests/flt/emit/ast-decl-gadt.flt
  - tests/flt/emit/ast-decl-record.flt
  - tests/flt/emit/ast-decl-record-mutable.flt
  - tests/flt/emit/ast-decl-record-ops.flt
  - tests/flt/emit/ast-decl-exception.flt
  - tests/flt/emit/ast-decl-exception-data.flt
  - tests/flt/emit/ast-decl-module.flt
  - tests/flt/emit/ast-decl-namespace.flt
  - tests/flt/emit/ast-decl-open.flt
  - tests/flt/emit/ast-decl-match.flt
  - tests/flt/emit/ast-decl-trywith.flt
autonomous: true

must_haves:
  truths:
    - "Every declaration AST node type has at least one --emit-ast file-mode test"
    - "Record expressions (create, access, update, set) are tested"
    - "Module, namespace, open declarations are tested"
    - "Exception declarations (simple and with data) are tested"
    - "All tests pass via fslit"
  artifacts:
    - path: "tests/flt/emit/ast-decl-adt-simple.flt"
      provides: "TypeDecl AST test for simple ADT"
    - path: "tests/flt/emit/ast-decl-gadt.flt"
      provides: "TypeDecl AST test for GADT constructors"
    - path: "tests/flt/emit/ast-decl-record.flt"
      provides: "RecordDecl AST test"
    - path: "tests/flt/emit/ast-decl-module.flt"
      provides: "ModuleDecl + OpenDecl AST test"
    - path: "tests/flt/emit/ast-decl-trywith.flt"
      provides: "TryWith + Raise AST test"
  key_links: []
---

<objective>
Create --emit-ast tests for all declaration-level AST node types using file mode (%input).

Purpose: Verify the parser produces correct AST for every declaration construct (types, records, modules, exceptions).
Output: 16 .flt test files in tests/flt/emit/ covering all declaration AST nodes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Key info:
- LangThree binary: src/LangThree/bin/Release/net10.0/LangThree
- fslit binary: /Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit
- File mode uses `%input` in the command -- fslit replaces it with a temp file path
- No LetRecDecl in file mode -- let rec only exists as expression with `in`
- Type parameter syntax: `type Option 'a = ...` (not `type 'a option`)
- GADT syntax: `type Expr 'a = IntLit : int -> int Expr | ...`
- ADT inline: `type Color = Red | Green | Blue` (single-line with pipes)
- ADT with leading pipe: uses INDENT/DEDENT for multi-line
- Mutual recursive types: `type Foo = ... and Bar = ...`

Known AST output formats (verified by running binary):
- `TypeDecl "Color" [| Red | Green | Blue]`
- `TypeDecl "Expr 'a" [| IntLit : TEInt -> TEData ("Expr", [TEInt]) | ...]`
- `RecordDecl "Point" {x: TEInt; y: TEInt}`
- `ExceptionDecl "NotFound"`
- `ExceptionDecl "InvalidArg" of TEString`
- `ModuleDecl "M" [\n  LetDecl ("x", Number 42)\n]`
- `OpenDecl [M]`
- `NamespaceDecl` with `namespace` prefix
- `LetDecl ("name", Lambda ("param", body))` for function declarations
- `RecordExpr ({x = Number 1; y = Number 2})`
- `FieldAccess (Var "p", x)`
- `RecordUpdate (Var "p", {x = Number 10})`
- `TryWith (Raise (Constructor "NotFound"), [(ConstructorPat "NotFound", Number 0)])`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture actual --emit-ast output for all declaration constructs</name>
  <files>N/A (exploration only)</files>
  <action>
For each test, create a temp file, write the LangThree source, and run `--emit-ast` on it. Record actual output.

Test cases to capture:

1. **Simple let binding:** `let x = 42`
2. **Function declaration:** `let add x y = x + y`
3. **Simple ADT:** `type Color = Red | Green | Blue` + `let c = Red`
4. **Parametric ADT:** `type Option 'a = Some of 'a | None`
5. **Mutual recursive types:** `type Expr = Lit of int | Add of Expr` + `and Stmt = ExprStmt of Expr`
6. **GADT:** `type Expr 'a = IntLit : int -> int Expr | BoolLit : bool -> bool Expr`
7. **Record type:** `type Point = { x: int; y: int }` + `let p = { x = 1; y = 2 }`
8. **Record with mutable:** `type Counter = { mutable count: int }`
9. **Record operations:** record with field access (p.x), copy-update ({ p with x = 10 }), SetField if applicable
10. **Exception simple:** `exception NotFound`
11. **Exception with data:** `exception InvalidArg of string`
12. **Module with open:** `module M = ...` + `open M` + `let y = x`
13. **Namespace:** `namespace MyNs` + `let x = 42`
14. **Match in file mode:** type decl + function using match with constructor patterns
15. **Try-with:** exception decl + function using try/raise/with

Run each with `2>/dev/null` to suppress prelude warning. Verify exit code 0.
  </action>
  <verify>All commands produce correct AST output with exit code 0</verify>
  <done>Actual AST output captured for all declaration constructs</done>
</task>

<task type="auto">
  <name>Task 2: Create .flt test files and verify with fslit</name>
  <files>
    tests/flt/emit/ast-decl-let.flt
    tests/flt/emit/ast-decl-func.flt
    tests/flt/emit/ast-decl-adt-simple.flt
    tests/flt/emit/ast-decl-adt-parametric.flt
    tests/flt/emit/ast-decl-adt-mutual.flt
    tests/flt/emit/ast-decl-gadt.flt
    tests/flt/emit/ast-decl-record.flt
    tests/flt/emit/ast-decl-record-mutable.flt
    tests/flt/emit/ast-decl-record-ops.flt
    tests/flt/emit/ast-decl-exception.flt
    tests/flt/emit/ast-decl-exception-data.flt
    tests/flt/emit/ast-decl-module.flt
    tests/flt/emit/ast-decl-namespace.flt
    tests/flt/emit/ast-decl-open.flt
    tests/flt/emit/ast-decl-match.flt
    tests/flt/emit/ast-decl-trywith.flt
  </files>
  <action>
Create .flt test files using the file-mode format with `%input`:

```
// Test: --emit-ast [description]
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --emit-ast %input
// --- Input:
[source code]
// --- Output:
[actual AST output]
```

IMPORTANT: The `// --- Input:` section contains the file contents. Lines in the input section are NOT prefixed with `//` -- they are raw source lines. Check existing file-mode tests (e.g., tests/flt/emit/emit-ast-file.flt) for the exact format.

After creating, run fslit:
```
/Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit tests/flt/emit/ast-decl-*.flt
```
  </action>
  <verify>
Run: `/Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit tests/flt/emit/ast-decl-*.flt`
All tests pass (exit code 0).
  </verify>
  <done>All declaration AST emit tests created and passing via fslit</done>
</task>

</tasks>

<verification>
1. Run fslit on all ast-decl tests -- all pass
2. Run fslit on ALL tests -- no regressions
3. Verify coverage: LetDecl, TypeDecl (ADT, GADT, mutual), RecordDecl, RecordExpr, FieldAccess, RecordUpdate, ExceptionDecl, ModuleDecl, OpenDecl, NamespaceDecl, Match (file-mode), TryWith, Raise
</verification>

<success_criteria>
- Every declaration node type has at least one --emit-ast test
- Record operations (create, access, update) are tested
- Module system (module, namespace, open) is tested
- Exception system (declare, raise, try-with) is tested
- GADT declaration format is tested
- All tests pass via fslit with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-coverage-fslit-testing/08-03-SUMMARY.md`
</output>
