---
phase: 67-cli-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LangThree/Cli.fs
  - src/LangThree/Prelude.fs
  - src/LangThree/Program.fs
autonomous: true

must_haves:
  truths:
    - "langthree --prelude /path file.fun uses the specified Prelude directory"
    - "LANGTHREE_PRELUDE env var is used when --prelude is not given"
    - "Prelude auto-discovery still works when neither --prelude nor env var is set"
    - "All existing flags (--emit-tokens, --emit-ast, --emit-type, --expr, file) still work"
  artifacts:
    - path: "src/LangThree/Cli.fs"
      provides: "Check, Deps, Prelude DU cases in CliArgs"
      contains: "| Check"
    - path: "src/LangThree/Prelude.fs"
      provides: "resolvePreludeDir with priority chain"
      contains: "resolvePreludeDir"
    - path: "src/LangThree/Program.fs"
      provides: "Prelude path extraction before loadPrelude call"
      contains: "results.Contains Prelude"
  key_links:
    - from: "src/LangThree/Program.fs"
      to: "src/LangThree/Prelude.fs"
      via: "loadPrelude(preludePath) with explicit path parameter"
      pattern: "loadPrelude.*preludePath"
    - from: "src/LangThree/Prelude.fs"
      to: "System.Environment.GetEnvironmentVariable"
      via: "LANGTHREE_PRELUDE env var lookup in resolvePreludeDir"
      pattern: "LANGTHREE_PRELUDE"
---

<objective>
Add all three new Argu CLI flags (Check, Deps, Prelude) to Cli.fs, implement Prelude path resolution priority chain (CLI-03, CLI-04), and fix the critical Program.fs ordering bug where loadPrelude() is called before flag extraction.

Purpose: Foundation for all other CLI features -- --check and --deps both need Prelude loaded from the correct path.
Output: Modified Cli.fs, Prelude.fs, Program.fs with correct Prelude loading order.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-cli-extensions/67-RESEARCH.md
@src/LangThree/Cli.fs
@src/LangThree/Prelude.fs
@src/LangThree/Program.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Argu flags and implement Prelude path resolution</name>
  <files>src/LangThree/Cli.fs, src/LangThree/Prelude.fs</files>
  <action>
  1. In Cli.fs, add three new DU cases to CliArgs (before the File case):
     - `| Check` (parameterless flag, renders as --check)
     - `| Deps` (parameterless flag, renders as --deps)
     - `| Prelude of path: string` (valued flag, renders as --prelude)
     Add corresponding Usage strings:
     - Check -> "type-check without executing"
     - Deps -> "show file dependency tree"
     - Prelude _ -> "set Prelude directory path"

  2. In Prelude.fs, add a new public function `resolvePreludeDir` that implements the priority chain:
     ```
     let resolvePreludeDir (explicitPath: string option) : string =
         match explicitPath with
         | Some path ->
             if Directory.Exists path then path
             else failwithf "Prelude directory not found: %s" path
         | None ->
             match System.Environment.GetEnvironmentVariable("LANGTHREE_PRELUDE") with
             | null | "" -> findPreludeDir()
             | envPath ->
                 if Directory.Exists envPath then envPath
                 else failwithf "LANGTHREE_PRELUDE directory not found: %s" envPath
     ```

  3. Modify `loadPrelude` signature to accept an optional explicit path:
     `let loadPrelude (?explicitPath: string option) : PreludeResult =`
     Actually, simpler: change to `let loadPrelude (explicitPath: string option) : PreludeResult =`
     Replace `let preludeDir = findPreludeDir()` with `let preludeDir = resolvePreludeDir explicitPath`
  </action>
  <verify>
  `dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds.
  Grep Cli.fs for `| Check` and `| Deps` and `| Prelude`.
  Grep Prelude.fs for `resolvePreludeDir` and `LANGTHREE_PRELUDE`.
  </verify>
  <done>Cli.fs has Check, Deps, Prelude DU cases with Usage strings. Prelude.fs has resolvePreludeDir with priority chain. loadPrelude accepts string option parameter.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Program.fs Prelude loading order</name>
  <files>src/LangThree/Program.fs</files>
  <action>
  The critical fix: extract --prelude flag BEFORE calling loadPrelude().

  1. Move the `let prelude = ...` line (currently at line 57) to AFTER Argu parsing but extract the prelude path first:
     ```
     let results = parser.Parse(argv, raiseOnUsage = false)

     // Extract --prelude path before loading (must happen before loadPrelude)
     let preludePath =
         if results.Contains Prelude then Some (results.GetResult Prelude)
         else None
     let prelude = Prelude.loadPrelude(preludePath)
     let initialEnv = Map.fold (fun acc k v -> Map.add k v acc) prelude.Env Eval.initialBuiltinEnv
     ```

  2. For the `--deps` mode, Prelude loading is NOT needed (only parsing). Add the --deps branch BEFORE the prelude loading block, right after help check:
     ```
     if results.IsUsageRequested then
         printfn "%s" (parser.PrintUsage())
         0
     elif results.Contains Deps && results.Contains File then
         // --deps: show dependency tree (parse only, no prelude needed)
         // ... (placeholder - implemented in Plan 02)
         0
     else
     ```
     Wait -- this complicates the if/elif chain. Better approach: keep the linear if/elif chain but note that --deps doesn't use prelude. Since loadPrelude is called unconditionally for simplicity, this is acceptable. The --deps branch simply won't use the prelude variable.

     Actually, the simplest correct approach: keep loadPrelude where it is (after flag extraction), called unconditionally. All branches except --deps use it. The --deps branch ignores it. This avoids restructuring the entire if/elif chain.

  3. Verify no existing tests break: `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release`
  </action>
  <verify>
  `dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds.
  `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` passes.
  Run `dotnet run --project src/LangThree/LangThree.fsproj -- --help` and verify Check, Deps, Prelude appear in help text.
  Run `dotnet run --project src/LangThree/LangThree.fsproj -- --prelude Prelude tests/flt/file/list/list-basic.flt` and verify it works (uses explicit Prelude path).
  </verify>
  <done>Program.fs extracts --prelude before loadPrelude call. loadPrelude receives explicit path. All existing tests pass. --help shows new flags.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/LangThree/LangThree.fsproj -c Release` -- builds cleanly
2. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` -- all existing tests pass
3. `dotnet run --project src/LangThree/LangThree.fsproj -- --help` -- shows Check, Deps, Prelude in help
4. `dotnet run --project src/LangThree/LangThree.fsproj -- --prelude Prelude tests/flt/file/list/list-basic.flt` -- runs with explicit Prelude path
</verification>

<success_criteria>
- Cli.fs has Check, Deps, Prelude cases in CliArgs DU with Usage strings
- Prelude.fs has resolvePreludeDir implementing: --prelude > LANGTHREE_PRELUDE > auto-discovery
- Program.fs extracts --prelude value before calling loadPrelude
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/67-cli-extensions/67-01-SUMMARY.md`
</output>
