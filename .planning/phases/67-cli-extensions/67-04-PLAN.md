---
phase: 67-cli-extensions
plan: 04
type: execute
wave: 3
depends_on: ["67-02", "67-03"]
files_modified:
  - tests/flt/file/cli/cli-check.flt
  - tests/flt/file/cli/cli-deps.flt
  - tests/flt/file/cli/cli-prelude.flt
  - tests/flt/file/cli/cli-cache-diamond.flt
autonomous: true

must_haves:
  truths:
    - "flt tests verify --check succeeds on valid file and fails on type error"
    - "flt tests verify --deps shows dependency tree with imports"
    - "flt tests verify --prelude overrides Prelude directory"
    - "flt tests verify diamond dependency caching produces correct output"
  artifacts:
    - path: "tests/flt/file/cli/cli-check.flt"
      provides: "--check mode flt tests"
      contains: "--check"
    - path: "tests/flt/file/cli/cli-deps.flt"
      provides: "--deps mode flt tests"
      contains: "--deps"
    - path: "tests/flt/file/cli/cli-prelude.flt"
      provides: "--prelude flag flt test"
      contains: "--prelude"
    - path: "tests/flt/file/cli/cli-cache-diamond.flt"
      provides: "Diamond dependency caching flt test"
      contains: "cache-test"
  key_links:
    - from: "tests/flt/file/cli/*.flt"
      to: "src/LangThree/bin/Release/net10.0/LangThree"
      via: "bash -c commands invoking the CLI"
      pattern: "LangThree.*--check\\|LangThree.*--deps"
---

<objective>
Add flt integration tests covering CLI-01 (--check), CLI-02 (--deps), CLI-03 (--prelude), and CLI-05 (caching diamond dependency).

Purpose: The checker flagged missing flt integration tests as a BLOCKER. These tests verify the CLI features work end-to-end.
Output: 4 flt test files in tests/flt/file/cli/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-cli-extensions/67-01-SUMMARY.md
@.planning/phases/67-cli-extensions/67-02-SUMMARY.md
@.planning/phases/67-cli-extensions/67-03-SUMMARY.md
@tests/flt/file/import/file-import-basic.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flt integration tests for CLI features</name>
  <files>tests/flt/file/cli/cli-check.flt, tests/flt/file/cli/cli-deps.flt, tests/flt/file/cli/cli-prelude.flt, tests/flt/file/cli/cli-cache-diamond.flt</files>
  <action>
  Create tests/flt/file/cli/ directory and four flt test files. Use the same pattern as existing flt tests (see file-import-basic.flt for the bash -c + printf pattern for creating temp files).

  The LangThree binary path is: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree

  1. **cli-check.flt** -- Tests --check mode (CLI-01):
     - Test 1: --check on a valid file outputs "OK" to stderr and exits 0.
       Use `bash -c` to create a temp .fun file with valid code, run `--check` on it, capture stderr with `2>&1 1>/dev/null`.
     - Test 2: --check on a file with a type error exits 1 and shows error on stderr.
       Create a temp .fun file with `let x : int = "hello"`, run --check, capture stderr.
     Note: Since flt captures stdout by default, and --check outputs to stderr, use `2>&1 1>/dev/null` to redirect stderr to stdout for capture.

  2. **cli-deps.flt** -- Tests --deps mode (CLI-02):
     - Test 1: --deps on a file with no imports shows just the filename.
     - Test 2: --deps on a file that imports another file shows indented tree.
       Create two temp files: parent imports child. Run --deps on parent, verify 2-line output with indentation.

  3. **cli-prelude.flt** -- Tests --prelude flag (CLI-03):
     - Test: Run a simple .fun file using `--prelude Prelude` (explicit path). Verify it produces correct output.
       This confirms the --prelude flag works. (The Prelude directory is at repo root.)

  4. **cli-cache-diamond.flt** -- Tests diamond dependency caching (CLI-05):
     - Create 4 temp files: d.fun (leaf), b.fun (imports d), c.fun (imports d), a.fun (imports b and c).
     - Run a.fun and verify correct output. This tests that d.fun is cached on second import.
     - The output should be the final computed value (e.g., 87 = 43 + 44 where d=42, b=d+1, c=d+2, a=b+c).

  IMPORTANT: Check existing flt test patterns carefully. The `--- Command:`, `--- Input:`, `--- Output:` format must be exact. Study file-import-basic.flt as the canonical example for bash -c with temp file creation.

  Note on LANGTHREE_PRELUDE env var (CLI-04): This is hard to test in flt because flt doesn't support setting env vars in the test runner. Skip env var testing in flt -- it's covered by the manual verification in Plan 01.
  </action>
  <verify>
  1. `dotnet build src/LangThree/LangThree.fsproj -c Release` (ensure binary is fresh).
  2. `../fslit/dist/FsLit tests/flt/file/cli/` -- all 4 test files pass.
  3. `../fslit/dist/FsLit tests/flt/` -- all flt tests pass (no regressions).
  </verify>
  <done>4 flt test files in tests/flt/file/cli/ covering --check (valid + error), --deps (no imports + with imports), --prelude (explicit path), and diamond dependency caching. All pass.</done>
</task>

</tasks>

<verification>
1. `../fslit/dist/FsLit tests/flt/file/cli/` -- all CLI flt tests pass
2. `../fslit/dist/FsLit tests/flt/` -- all flt tests pass (no regressions)
</verification>

<success_criteria>
- cli-check.flt: --check on valid file shows OK on stderr, --check on type error shows error on stderr
- cli-deps.flt: --deps on file with no imports shows single line, --deps with imports shows indented tree
- cli-prelude.flt: --prelude flag works with explicit path
- cli-cache-diamond.flt: diamond dependency produces correct output
- All existing flt tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/67-cli-extensions/67-04-SUMMARY.md`
</output>
