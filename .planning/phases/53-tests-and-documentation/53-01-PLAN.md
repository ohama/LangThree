---
phase: 53-tests-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/expr/seq/nlseq-structural-terminator.flt
  - tests/flt/expr/seq/nlseq-multiline-app.flt
autonomous: true

must_haves:
  truths:
    - "A test file exists that confirms else/with/| at same indent are NOT preceded by spurious SEMICOLON"
    - "A test file exists that confirms multi-line function application (argument on indented continuation) works correctly"
    - "Both new test files pass when run with the FsLit runner"
  artifacts:
    - path: "tests/flt/expr/seq/nlseq-structural-terminator.flt"
      provides: "Regression test: structural terminators not preceded by SEMICOLON"
    - path: "tests/flt/expr/seq/nlseq-multiline-app.flt"
      provides: "Regression test: multi-line function application not broken by newline sequencing"
  key_links:
    - from: "nlseq-structural-terminator.flt"
      to: "IndentFilter SEMICOLON injection logic"
      via: "expected output — no duplicate output from spurious sequencing"
    - from: "nlseq-multiline-app.flt"
      to: "INDENT/DEDENT handling in IndentFilter"
      via: "expected output — indented arguments are continuation, not separate expressions"
---

<objective>
Add the 2 missing NLSEQ regression flt tests that cover (a) structural terminators (else/with/| not preceded by spurious SEMICOLON) and (b) multi-line function application (indented argument not split into separate expression). These are the only gaps identified in TST-33.

Purpose: Phase 50 created 5 NLSEQ tests covering multi-line bodies, match arms, while loops, pipe continuation, and module-level independence. The two regression scenarios — structural terminator safety and multi-line application safety — have no dedicated flt file, leaving TST-33 incomplete per the phase 53 success criteria.
Output: 2 new .flt files in tests/flt/expr/seq/, both passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-tests-and-documentation/53-RESEARCH.md

# Existing NLSEQ tests for style reference
@tests/flt/expr/seq/nlseq-basic.flt
@tests/flt/expr/seq/nlseq-in-match.flt
@tests/flt/expr/seq/nlseq-no-module.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nlseq-structural-terminator.flt regression test</name>
  <files>tests/flt/expr/seq/nlseq-structural-terminator.flt</files>
  <action>
Create the file `tests/flt/expr/seq/nlseq-structural-terminator.flt`.

This test verifies that `else` at the same indent as `then`-branch lines does NOT get a spurious SEMICOLON injected before it. If SEMICOLON were injected, the if/else would parse incorrectly and produce wrong output.

Use this exact content (verify the output by running the binary before writing):

```
// Test NLSEQ-06: structural terminators (else) at same indent are not preceded by spurious SEMICOLON
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let x = 5
let result =
    if x > 2 then
        println "big"
        42
    else
        0
// --- Output:
big
42
```

Before writing, run the input through the binary to confirm the exact output:
```
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /tmp/nlseq_terminator_check.l3
```

Write the input code to /tmp/nlseq_terminator_check.l3, run it, then write the flt file with the verified output.

The output must NOT include a trailing `()` for `result` — `result` is bound at module level via `let result = ...` but the last top-level binding printed is the one whose value is 42 (printed by the evaluator). Check actual binary output carefully.

Note: In LangThree file mode, each top-level `let` binding whose value is not `()` is printed. `println "big"` produces side effect "big" and returns `()`. The if/else returns 42 (the then-branch result). So `let result = ...` binds 42, which prints as `42`.
  </action>
  <verify>
Run: `../fslit/dist/FsLit tests/flt/expr/seq/nlseq-structural-terminator.flt`
The test must pass (exit 0, no diff shown).
  </verify>
  <done>nlseq-structural-terminator.flt exists, passes the FsLit runner, and the expected output exactly matches binary output.</done>
</task>

<task type="auto">
  <name>Task 2: Add nlseq-multiline-app.flt regression test</name>
  <files>tests/flt/expr/seq/nlseq-multiline-app.flt</files>
  <action>
Create the file `tests/flt/expr/seq/nlseq-multiline-app.flt`.

This test verifies that multi-line function application (function name on one line, arguments indented on following lines) is NOT broken by the NLSEQ SEMICOLON injection. The indented arguments use INDENT/DEDENT tokens, not same-level newlines, so no SEMICOLON should be injected between the function and its arguments.

Before writing, run the input through the binary to confirm the exact output:
```
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /tmp/nlseq_multiline_app_check.l3
```

Write this input to /tmp/nlseq_multiline_app_check.l3:
```
let add3 a b c = a + b + c
let result =
    add3
        1
        2
        3
```

Run the binary and capture the exact output. Then write the flt file with that verified output.

Expected structure of the flt file:
```
// Test NLSEQ-07: multi-line function application is not broken by newline sequencing
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let add3 a b c = a + b + c
let result =
    add3
        1
        2
        3
// --- Output:
[exact output from binary]
```

The result should be 6 (1+2+3). Confirm whether `let result = 6` prints `6` or is silent in file mode (module-level non-unit values are printed).
  </action>
  <verify>
Run: `../fslit/dist/FsLit tests/flt/expr/seq/nlseq-multiline-app.flt`
The test must pass (exit 0, no diff shown).

Also run the full NLSEQ suite to confirm no regressions:
`../fslit/dist/FsLit tests/flt/expr/seq/`
  </verify>
  <done>nlseq-multiline-app.flt exists, passes the FsLit runner, and the full seq/ suite still passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Both new flt files exist in `tests/flt/expr/seq/`
2. `../fslit/dist/FsLit tests/flt/expr/seq/` passes all tests (12 total: 10 existing + 2 new)
3. `../fslit/dist/FsLit tests/flt/` passes the full suite (591 tests)
4. TST-33 success criterion is now fully met:
   - Positive cases: nlseq-basic, nlseq-in-match, nlseq-in-while, nlseq-pipe-continuation (existing)
   - Regression — multi-line application: nlseq-multiline-app (NEW)
   - Regression — structural terminators: nlseq-structural-terminator (NEW)
   - Module-level not affected: nlseq-no-module (existing)
</verification>

<success_criteria>
- `tests/flt/expr/seq/nlseq-structural-terminator.flt` exists and passes
- `tests/flt/expr/seq/nlseq-multiline-app.flt` exists and passes
- Full flt suite passes (591 tests)
- TST-33 all regression cases covered
</success_criteria>

<output>
After completion, create `.planning/phases/53-tests-and-documentation/53-01-SUMMARY.md` with:
- What was built (2 flt regression tests)
- Test count before and after (589 → 591)
- Exact file paths created
- Confirmation that TST-33 is fully covered
</output>
