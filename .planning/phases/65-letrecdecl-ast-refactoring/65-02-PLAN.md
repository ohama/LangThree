---
phase: 65-letrecdecl-ast-refactoring
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - src/LangThree/TypeCheck.fs
  - src/LangThree/Bidir.fs
  - src/LangThree/Infer.fs
  - tests/flt/file/letrec/letrec-decl-param-annotation.flt
autonomous: true

must_haves:
  truths:
    - "let rec f (x : int) y = ... preserves and enforces the int annotation at module level"
    - "let rec f (x : int) = x ^^ hi is rejected with a type error"
    - "Expression-level let rec f (x : int) = ... in ... also enforces annotation"
    - "Unannotated let rec bindings still work (no regression)"
  artifacts:
    - path: "src/LangThree/TypeCheck.fs"
      provides: "LetRecDecl type checking uses paramTyOpt via elaborateTypeExpr"
      pattern: "elaborateTypeExpr.*paramTy"
    - path: "src/LangThree/Bidir.fs"
      provides: "LetRec synthesis uses paramTyOpt"
      pattern: "elaborateTypeExpr.*paramTy"
    - path: "src/LangThree/Infer.fs"
      provides: "LetRec inference uses paramTyOpt"
      pattern: "elaborateTypeExpr.*paramTy"
    - path: "tests/flt/file/letrec/letrec-decl-param-annotation.flt"
      provides: "flt tests for annotation enforcement and error cases"
  key_links:
    - from: "src/LangThree/TypeCheck.fs"
      to: "src/LangThree/Bidir.fs"
      via: "elaborateTypeExpr call to convert TypeExpr to Type"
      pattern: "elaborateTypeExpr"
---

<objective>
Wire the type annotation into the type checker so first-param annotations on `let rec` bindings are actually enforced, then add flt tests proving both enforcement and error rejection.

Purpose: Without this, the AST carries the type info (from Plan 01) but ignores it -- annotations would be cosmetic only.
Output: Working type enforcement + tests proving it.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-letrecdecl-ast-refactoring/65-RESEARCH.md
@.planning/phases/65-letrecdecl-ast-refactoring/65-01-SUMMARY.md
@src/LangThree/TypeCheck.fs
@src/LangThree/Bidir.fs
@src/LangThree/Infer.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce first-param type annotation in TypeCheck, Bidir, and Infer</name>
  <files>
    src/LangThree/TypeCheck.fs
    src/LangThree/Bidir.fs
    src/LangThree/Infer.fs
  </files>
  <action>
Three key logic changes -- all follow the same pattern: instead of always creating a fresh type variable for paramTy, use `elaborateTypeExpr` when annotation is present.

**TypeCheck.fs -- LetRecDecl (around line 852)**

Find the `funcTypes` binding where it maps over LetRecDecl bindings. Currently:
```fsharp
bindings |> List.map (fun (name, param, _body, _) ->
    let paramTy = Infer.freshVar()
```

Change to use `paramTyOpt` (the `_` from Plan 01 was a placeholder):
```fsharp
bindings |> List.map (fun (name, param, paramTyOpt, _body, _) ->
    let paramTy =
        match paramTyOpt with
        | Some tyExpr -> Bidir.elaborateTypeExpr tyExpr
        | None -> Infer.freshVar()
```

IMPORTANT: Verify `elaborateTypeExpr` is accessible from TypeCheck.fs. Search for existing uses of `Bidir.` in TypeCheck.fs to confirm the import pattern. If `elaborateTypeExpr` is not public in Bidir.fs, make it public (add `let` without `private`).

**Bidir.fs -- LetRec (around line 302)**

Currently:
```fsharp
| LetRec (name, param, body, expr, span) ->
    let funcTy = freshVar()
    let paramTy = freshVar()
```

Change to (Plan 01 already updated the destructuring):
```fsharp
| LetRec (name, param, paramTyOpt, body, expr, span) ->
    let funcTy = freshVar()
    let paramTy =
        match paramTyOpt with
        | Some tyExpr -> elaborateTypeExpr tyExpr
        | None -> freshVar()
```

**Infer.fs -- LetRec (around line 267)**

Same pattern as Bidir.fs. Find the LetRec case in the `infer` function. Currently:
```fsharp
| LetRec (name, param, body, expr, span) ->
```

Update to use `paramTyOpt`:
```fsharp
| LetRec (name, param, paramTyOpt, body, expr, span) ->
    ...
    let paramTy =
        match paramTyOpt with
        | Some tyExpr -> Bidir.elaborateTypeExpr tyExpr
        | None -> freshVar()
```

Verify that Infer.fs can call `Bidir.elaborateTypeExpr`. Check the existing module references and compilation order in the .fsproj to confirm Bidir.fs is compiled before or available to Infer.fs. If not, find the appropriate way to access elaborateTypeExpr (it may need to go through a different path).
  </action>
  <verify>
`dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds.
`../fslit/dist/FsLit tests/flt/` -- all existing tests still pass.
  </verify>
  <done>
- TypeCheck.fs uses `elaborateTypeExpr` for annotated first params in LetRecDecl
- Bidir.fs uses `elaborateTypeExpr` for annotated first params in LetRec
- Infer.fs uses `elaborateTypeExpr` for annotated first params in LetRec
- Unannotated params still get `freshVar()` (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add flt tests for annotation enforcement</name>
  <files>
    tests/flt/file/letrec/letrec-decl-param-annotation.flt
  </files>
  <action>
Create a new flt test file that verifies first-param type annotations are enforced on `let rec` bindings. Follow the existing flt test patterns in the letrec directory.

**Positive tests (annotation preserved, code type-checks):**

1. Module-level `let rec f (x : int) y = x + y and g (z : bool) = if z then 1 else 0` -- should compile and run correctly. Call `f 3 4` expecting `7`, call `g true` expecting `1`.

2. Expression-level `let rec f (x : int) = x + 1 in f 5` -- expecting `6`.

3. Unannotated params still work: `let rec f x = x + 1 in f 5` -- expecting `6` (regression check).

**Negative tests (annotation catches type errors):**

4. Module-level `let rec f (x : int) = x ^^ "hi"` -- should produce a type error because `x` is `int` but `^^` expects `string`. Use `-- ERROR:` directive (check existing error test patterns in the flt directory).

5. Expression-level `let rec f (x : int) = x ^^ "hi" in f 5` -- same type error.

6. Module-level mutual recursion with wrong call: `let rec f (x : int) = g x and g (y : bool) = ...` where `f` calls `g` with an `int` but `g` expects `bool` -- type error.

Look at existing flt test files (e.g., `tests/flt/file/letrec/`) for the exact syntax of `-- EXPECT:` and `-- ERROR:` directives.
  </action>
  <verify>
`../fslit/dist/FsLit tests/flt/file/letrec/letrec-decl-param-annotation.flt` -- all test cases pass (positive tests produce expected output, negative tests produce expected errors).

`../fslit/dist/FsLit tests/flt/` -- full suite still passes.
  </verify>
  <done>
- Positive tests confirm annotations are preserved and code runs correctly
- Negative tests confirm type checker rejects mismatches on annotated first params
- Mutual recursion annotation test confirms cross-binding type enforcement
- No regression in existing tests
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/LangThree/LangThree.fsproj -c Release` -- clean build
2. `../fslit/dist/FsLit tests/flt/file/letrec/letrec-decl-param-annotation.flt` -- new tests pass
3. `../fslit/dist/FsLit tests/flt/` -- full suite passes
4. Phase success criteria from roadmap:
   - SC1: `let rec f (x : int) y = ... and g (z : bool) = ...` compiles (positive test)
   - SC2: `let rec f (x : int) = x ^^ "hi"` rejected (negative test)
   - SC3: All existing flt tests pass (regression check)
   - SC4: `dotnet build` with no warnings (checked in Plan 01)
</verification>

<success_criteria>
- Type annotations on first params are enforced by type checker (not just carried)
- Type mismatches on annotated first params produce clear type errors
- All positive and negative flt tests pass
- No regression in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/65-letrecdecl-ast-refactoring/65-02-SUMMARY.md`
</output>
