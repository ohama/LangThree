---
phase: 10-unit-type
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/flt/expr/unit-literal.flt
  - tests/flt/expr/unit-fun-param.flt
  - tests/flt/expr/unit-let-wildcard-expr.flt
  - tests/flt/file/unit-let-wildcard-decl.flt
  - tests/flt/file/unit-mutable-set.flt
  - tests/flt/emit/type-expr/unit-type-keyword.flt
  - tests/flt/emit/type-decl/unit-return-type.flt
autonomous: true

must_haves:
  truths:
    - "fslit tests document all five unit type success criteria from the roadmap"
    - "() literal test passes"
    - "unit type --emit-type test passes"
    - "fun () -> param test passes"
    - "let _ = module-level mutation test passes"
    - "mutable field set returns unit type test passes"
  artifacts:
    - path: "tests/flt/expr/unit-literal.flt"
      provides: "() literal evaluation test"
    - path: "tests/flt/expr/unit-fun-param.flt"
      provides: "fun () -> body function test"
    - path: "tests/flt/expr/unit-let-wildcard-expr.flt"
      provides: "let _ = e1 in e2 expression-level test"
    - path: "tests/flt/file/unit-let-wildcard-decl.flt"
      provides: "let _ = expr module-level sequencing test"
    - path: "tests/flt/file/unit-mutable-set.flt"
      provides: "mutable field set returns unit, sequencing works"
    - path: "tests/flt/emit/type-expr/unit-type-keyword.flt"
      provides: "--emit-type shows unit for () literal"
    - path: "tests/flt/emit/type-decl/unit-return-type.flt"
      provides: "--emit-type shows unit -> int for fun () -> body"
  key_links:
    - from: "tests/flt/file/unit-mutable-set.flt"
      to: "roadmap success criterion 2"
      via: "let _ = record.field <- 42 pattern"
      pattern: "LARROW"
---

<objective>
Add fslit tests that document and verify all Phase 10 unit type features.

Purpose: Establish regression coverage for the unit type, ensuring future changes don't break the () literal, unit type display, fun () -> syntax, and let _ = sequencing.
Output: 7 new .flt test files covering all five roadmap success criteria.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-unit-type/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expression-level and emit unit type fslit tests</name>
  <files>
    tests/flt/expr/unit-literal.flt,
    tests/flt/expr/unit-fun-param.flt,
    tests/flt/expr/unit-let-wildcard-expr.flt,
    tests/flt/emit/type-expr/unit-type-keyword.flt,
    tests/flt/emit/type-decl/unit-return-type.flt
  </files>
  <action>
Create these 5 fslit test files. Use the existing test format: `// --- Command:`, `// --- Input:`, `// --- Output:` sections. Use the binary path `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree`.

**tests/flt/expr/unit-literal.flt:**
```
// Test: () literal evaluates to unit value
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "()"
// --- Output:
()
```

**tests/flt/expr/unit-fun-param.flt:**
```
// Test: fun () -> body creates a function accepting unit parameter
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "let f = fun () -> 42 in f ()"
// --- Output:
42
```

**tests/flt/expr/unit-let-wildcard-expr.flt:**
```
// Test: let _ = e1 in e2 discards e1 result and returns e2
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "let _ = 42 in 99"
// --- Output:
99
```

**tests/flt/emit/type-expr/unit-type-keyword.flt:**
```
// Test: () literal has type unit in --emit-type
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "()" --emit-type
// --- Output:
unit
```

**tests/flt/emit/type-decl/unit-return-type.flt:**
```
// Test: fun () -> body has type unit -> int in --emit-type file mode
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input --emit-type
// --- Input:
let f = fun () -> 42
// --- Output:
f: unit -> int
```

Run `dotnet test` to verify all tests pass after creation.
  </action>
  <verify>dotnet test /Users/ohama/vibe-coding/LangThree/ 2>&1 | tail -10</verify>
  <done>All tests pass including the 5 new ones. Test count increases by 5.</done>
</task>

<task type="auto">
  <name>Task 2: Create file-mode unit sequencing fslit tests</name>
  <files>
    tests/flt/file/unit-let-wildcard-decl.flt,
    tests/flt/file/unit-mutable-set.flt
  </files>
  <action>
Create these 2 fslit test files for file-mode (module-level) unit usage.

**tests/flt/file/unit-let-wildcard-decl.flt:**
```
// Test: let _ = expr at module level evaluates for side effects
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let x = 1
let _ = x
let result = 42
// --- Output:
42
```

**tests/flt/file/unit-mutable-set.flt:**
```
// Test: mutable field set returns unit, let _ = enables sequencing
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
type Counter = { mutable count: int }
let c = { count = 0 }
let _ = c.count <- 42
let result = c.count
// --- Output:
42
```

Run `dotnet test` to verify all tests pass after creation.
  </action>
  <verify>dotnet test /Users/ohama/vibe-coding/LangThree/ 2>&1 | tail -10</verify>
  <done>All tests pass including the 2 new file-mode tests. Total test count increases by 7 from baseline (186 total fslit tests).</done>
</task>

</tasks>

<verification>
```bash
# Run full suite and check count
dotnet test /Users/ohama/vibe-coding/LangThree/ 2>&1 | grep -E "passed|failed|Tests"

# Verify each new test individually using the fslit runner or direct invocation:
# Unit literal
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "()"
# Expected: ()

# Fun unit param
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "let f = fun () -> 42 in f ()"
# Expected: 42

# let _ in expr
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "let _ = 42 in 99"
# Expected: 99

# unit type display
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "()" --emit-type
# Expected: unit

# Mutable set + unit sequencing (write to temp file first)
echo 'type Counter = { mutable count: int }
let c = { count = 0 }
let _ = c.count <- 42
let result = c.count' > /tmp/unit-test.lt
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /tmp/unit-test.lt
# Expected: 42
```
</verification>

<success_criteria>
- 7 new .flt test files created covering all 5 roadmap success criteria for Phase 10
- All new tests pass
- All 196+ existing tests still pass (no regressions)
- Test count increases to 186 fslit tests total
</success_criteria>

<output>
After completion, create `.planning/phases/10-unit-type/10-02-SUMMARY.md`
</output>
