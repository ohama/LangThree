---
phase: 37-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/file/exception/failwith-basic.flt
  - tests/flt/file/exception/failwith-try-catch.flt
  - tests/flt/file/let/let-pat-decl.flt
  - tests/flt/file/import/file-import-module-qualified.flt
  - tests/flt/file/prelude/prelude-list-length-qualified.flt
  - tests/flt/file/prelude/prelude-list-map-qualified.flt
autonomous: true

must_haves:
  truths:
    - "failwith raises an uncaught exception with the message in stderr and exits 1"
    - "try failwith with e -> catches the exception and returns the handler value"
    - "let (a, b) = (1, 2) at module level destructures and binds both names"
    - "open 'file.fun' followed by Module.func call resolves without E0313"
    - "List.length [1;2;3] via Prelude qualified access returns 3"
    - "List.map (fun x -> x*2) [1;2;3] via Prelude qualified access returns [2;4;6]"
  artifacts:
    - path: "tests/flt/file/exception/failwith-basic.flt"
      provides: "TST-14 — uncaught failwith regression test"
    - path: "tests/flt/file/exception/failwith-try-catch.flt"
      provides: "TST-14 — inline try-with catches failwith regression test"
    - path: "tests/flt/file/let/let-pat-decl.flt"
      provides: "TST-15 — module-level tuple destructuring regression test"
    - path: "tests/flt/file/import/file-import-module-qualified.flt"
      provides: "TST-16 — imported file qualified access regression test"
    - path: "tests/flt/file/prelude/prelude-list-length-qualified.flt"
      provides: "TST-17 — List.length qualified access regression test"
    - path: "tests/flt/file/prelude/prelude-list-map-qualified.flt"
      provides: "TST-17 — List.map qualified access regression test"
  key_links:
    - from: "failwith-try-catch.flt"
      to: "Parser.fsy Option A TRY rules"
      via: "inline try failwith form parses without error"
    - from: "file-import-module-qualified.flt"
      to: "TypeCheck.fs fileImportTypeChecker"
      via: "bash -c wrapper creates lib file before binary runs"
    - from: "prelude-list-length-qualified.flt"
      to: "Prelude/List.fun module wrapper"
      via: "List.length resolves through Prelude module env"
---

<objective>
Add 6 flt regression tests covering the four features fixed in Phase 36: failwith raise/catch (TST-14), module-level tuple pattern destructuring (TST-15), imported-file qualified module access (TST-16), and Prelude qualified access for List (TST-17).

Purpose: Every Phase 36 bug fix now has a flt test that will catch regressions. Phase 26 (failwith) and Phase 28 (LetPatDecl) gain their first flt coverage.
Output: 6 new .flt files, all passing, total flt count rises from 468 to 474.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-bug-fixes/36-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failwith flt tests (TST-14)</name>
  <files>
    tests/flt/file/exception/failwith-basic.flt
    tests/flt/file/exception/failwith-try-catch.flt
  </files>
  <action>
Create two flt test files in `tests/flt/file/exception/`.

**failwith-basic.flt** — uncaught failwith exits 1 with error on stderr:
```
// Test: failwith raises uncaught exception with message
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = failwith "something went wrong"
// --- ExitCode: 1
// --- Stderr:
Error: LangThreeException (StringValue "something went wrong")
```

**failwith-try-catch.flt** — inline `try failwith with e ->` form (exercises Phase 36 Option A parser rule):
```
// Test: try failwith inline form caught by with handler
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = try failwith "boom" with e -> "caught"
// --- Output:
"caught"
```

No blank lines between `// --- Input:` lines and directives. Use `// --- Output:` (not `// --- Stdout:`).
  </action>
  <verify>
Run both tests individually with fslit or run the full suite:
`/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree tests/flt/file/exception/failwith-basic.flt` is not the run pattern — use the fslit runner.

Check the flt runner invocation from an existing test run. The typical command is:
`fslit tests/flt/` (or the project-specific runner). Alternatively:
- Manually: `echo 'let result = failwith "something went wrong"' | /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /dev/stdin` exits 1 with correct stderr.
- Manually: `echo 'let result = try failwith "boom" with e -> "caught"' | /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /dev/stdin` outputs `"caught"`.
  </verify>
  <done>failwith-basic.flt and failwith-try-catch.flt exist and both pass the flt runner without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add LetPatDecl and file-import qualified access flt tests (TST-15, TST-16)</name>
  <files>
    tests/flt/file/let/let-pat-decl.flt
    tests/flt/file/import/file-import-module-qualified.flt
  </files>
  <action>
**let-pat-decl.flt** — module-level tuple pattern destructuring (TST-15). Place in `tests/flt/file/let/`. The binding `let (a, b) = (1, 2)` alone produces no output (exit 0, empty stdout). Add a second binding to produce visible output:
```
// Test: module-level tuple pattern destructuring
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let (a, b) = (10, 20)
let result = a + b
// --- Output:
30
```

**file-import-module-qualified.flt** — TST-16. Uses `bash -c` wrapper to create the lib file at /tmp before running the binary. Follow the exact pattern from `tests/flt/file/import/file-import-qualified.flt` (which uses `bash -c 'printf "..." > /tmp/... && BINARY %input'`).

The lib file must define a module so qualified access is possible. Use `printf` with `\n` for newlines. Example structure:
```
// Test: open file import then qualified module access (Module.func)
// --- Command: bash -c 'printf "module Math =\n    let square x = x * x\n" > /tmp/flt-math-qual.fun && /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input'
// --- Input:
open "/tmp/flt-math-qual.fun"
let result = Math.square 7
// --- Output:
49
```

The indentation in the printf string: the `let` inside `module Math =` must have 4 spaces of indentation. Use `\n    let` (4 spaces after `\n`).
  </action>
  <verify>
- Manually: `echo 'let (a, b) = (10, 20)
let result = a + b' | /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /dev/stdin` → outputs `30`
- Manually: run the bash -c command for file-import-module-qualified.flt → outputs `49`
  </verify>
  <done>let-pat-decl.flt and file-import-module-qualified.flt exist and pass. No E0313 error from file-import-module-qualified.flt.</done>
</task>

<task type="auto">
  <name>Task 3: Add Prelude qualified access flt tests and verify full suite (TST-17)</name>
  <files>
    tests/flt/file/prelude/prelude-list-length-qualified.flt
    tests/flt/file/prelude/prelude-list-map-qualified.flt
  </files>
  <action>
Create two flt files in `tests/flt/file/prelude/`. These are simple file-mode tests (no bash -c wrapper needed — Prelude is loaded automatically by the binary).

**prelude-list-length-qualified.flt**:
```
// Test: Prelude List.length qualified access
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = List.length [10; 20; 30; 40; 50]
// --- Output:
5
```

**prelude-list-map-qualified.flt**:
```
// Test: Prelude List.map qualified access
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = List.map (fun x -> x * 3) [1; 2; 3]
// --- Output:
[3; 6; 9]
```

After creating these two files, run the full flt test suite to confirm all 474 tests pass (468 + 6 new). Use the project's flt runner. Check `tests/flt/run.sh` or look at how tests were run in Phase 36 verification. Typical invocation pattern from this project: `cd /Users/ohama/vibe-coding/LangThree && dotnet run --project src/LangThree -- --test tests/flt/` or `fslit tests/flt/`. Check Phase 36 VERIFICATION.md — it shows `fslit tests/flt/: 468/468 passed` — use the same runner.
  </action>
  <verify>
- `List.length [10;20;30;40;50]` → `5`
- `List.map (fun x -> x*3) [1;2;3]` → `[3; 6; 9]`
- Full flt suite: 474/474 passed (0 failures)
- `dotnet test`: 224/224 passed (0 failures, no regressions)
  </verify>
  <done>Both Prelude qualified access flt tests exist and pass. Full flt suite shows 474 tests passing. dotnet test still passes 224/224.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Count flt files: `find tests/flt -name "*.flt" | wc -l` → 474 (was 468)
2. Run full flt suite → 474/474 passed
3. Run `dotnet test` → 224/224 passed
4. Spot-check each new test file name matches its TST requirement:
   - TST-14: failwith-basic.flt + failwith-try-catch.flt
   - TST-15: let-pat-decl.flt
   - TST-16: file-import-module-qualified.flt
   - TST-17: prelude-list-length-qualified.flt + prelude-list-map-qualified.flt
</verification>

<success_criteria>
- 6 new .flt files created in the correct directories
- All 6 new tests pass individually
- Full flt suite: 474/474 passing
- dotnet test: 224/224 still passing
- TST-14 through TST-17 all covered
</success_criteria>

<output>
After completion, create `.planning/phases/37-test-coverage/37-01-SUMMARY.md`
</output>
