---
phase: 11-string-operations
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/LangThree/Program.fs
  - src/LangThree/Repl.fs
  - tests/flt/expr/str-length.flt
  - tests/flt/expr/str-concat.flt
  - tests/flt/expr/str-sub.flt
  - tests/flt/expr/str-contains.flt
  - tests/flt/expr/str-to-string.flt
  - tests/flt/expr/str-to-int.flt
  - tests/flt/emit/type-decl/type-decl-str-builtins.flt
autonomous: true

must_haves:
  truths:
    - "string_length \"hello\" evaluates to 5"
    - "string_sub \"hello\" 1 3 evaluates to \"ell\""
    - "to_string 42 evaluates to \"42\""
    - "to_string true evaluates to \"true\""
    - "string_to_int \"42\" evaluates to 42"
    - "string_contains \"hello\" \"ell\" evaluates to true"
    - "string_concat \"foo\" \"bar\" evaluates to \"foobar\""
    - "All 6 functions have correct types in --emit-type"
    - "All 196 F# unit tests still pass"
    - "All fslit tests pass (including 7 new ones)"
  artifacts:
    - path: "src/LangThree/Program.fs"
      provides: "initialEnv includes initialBuiltinEnv"
      contains: "initialBuiltinEnv"
    - path: "src/LangThree/Repl.fs"
      provides: "REPL initialEnv includes initialBuiltinEnv"
      contains: "initialBuiltinEnv"
    - path: "tests/flt/expr/str-length.flt"
      provides: "string_length integration test"
    - path: "tests/flt/emit/type-decl/type-decl-str-builtins.flt"
      provides: "--emit-type verification for string builtins"
  key_links:
    - from: "Program.fs initialEnv"
      to: "Eval.initialBuiltinEnv"
      via: "Map.fold merge"
      pattern: "initialBuiltinEnv"
    - from: "Repl.fs initialEnv"
      to: "Eval.initialBuiltinEnv"
      via: "Map.fold merge"
      pattern: "initialBuiltinEnv"
---

<objective>
Wire Eval.initialBuiltinEnv into Program.fs and Repl.fs so all 6 string functions are available at runtime. Write fslit integration tests proving each function works correctly and types correctly.

Purpose: Plan 01 created the BuiltinValue infrastructure and registered the functions, but they are not yet in scope at eval time because Program.fs and Repl.fs still call `Prelude.loadPrelude()` as the sole source of initialEnv. This plan completes the wiring and proves correctness with tests.

Output: Working string built-ins in --expr, --file, and REPL modes; 7 new .flt tests covering all 6 string functions + type checking.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-string-operations/11-01-SUMMARY.md
@.planning/ROADMAP.md
@src/LangThree/Program.fs
@src/LangThree/Repl.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire initialBuiltinEnv into Program.fs and Repl.fs</name>
  <files>
    src/LangThree/Program.fs
    src/LangThree/Repl.fs
  </files>
  <action>
**Program.fs:**

Find line: `let initialEnv = Prelude.loadPrelude()`

Replace with:
```fsharp
// Load prelude for evaluation modes (built-in string functions merged in)
let preludeEnv = Prelude.loadPrelude()
let initialEnv = Map.fold (fun acc k v -> Map.add k v acc) preludeEnv Eval.initialBuiltinEnv
```

This merge pattern adds all entries from `Eval.initialBuiltinEnv` into the prelude env. `Prelude.loadPrelude()` currently returns `Map.empty`, so the result is just `Eval.initialBuiltinEnv`. The merge pattern is forward-compatible if a real prelude is added later.

**Repl.fs:**

Find in `startRepl()`:
```fsharp
let initialEnv = Prelude.loadPrelude()
```

Replace with:
```fsharp
let preludeEnv = Prelude.loadPrelude()
let initialEnv = Map.fold (fun acc k v -> Map.add k v acc) preludeEnv Eval.initialBuiltinEnv
```

After both edits, run `dotnet build` to confirm zero errors and zero warnings.
  </action>
  <verify>
1. `dotnet build src/LangThree/LangThree.fsproj 2>&1 | grep -E "warning|error|Build"` — 0 errors, 0 warnings.
2. `src/LangThree/bin/Release/net10.0/LangThree --expr 'string_length "hello"'` — outputs `5`.
3. `src/LangThree/bin/Release/net10.0/LangThree --expr 'string_sub "hello" 1 3'` — outputs `"ell"`.
4. `src/LangThree/bin/Release/net10.0/LangThree --expr 'to_string 42'` — outputs `"42"`.
5. `src/LangThree/bin/Release/net10.0/LangThree --expr 'string_to_int "42"'` — outputs `42`.

Note: Build with `dotnet build -c Release` before testing binary, or use `dotnet run --project src/LangThree -- --expr '...'`.
  </verify>
  <done>
Program.fs and Repl.fs both merge initialBuiltinEnv into initialEnv. All 6 string functions evaluate correctly via --expr. Build has 0 errors and 0 warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 7 fslit tests for all 6 string functions + --emit-type</name>
  <files>
    tests/flt/expr/str-length.flt
    tests/flt/expr/str-concat.flt
    tests/flt/expr/str-sub.flt
    tests/flt/expr/str-contains.flt
    tests/flt/expr/str-to-string.flt
    tests/flt/expr/str-to-int.flt
    tests/flt/emit/type-decl/type-decl-str-builtins.flt
  </files>
  <action>
Create these 7 .flt test files. Use the full binary path as seen in existing tests: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree`.

**tests/flt/expr/str-length.flt:**
```
// Test: string_length returns the length of a string
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "string_length \"hello\""
// --- Output:
5
```

**tests/flt/expr/str-concat.flt:**
```
// Test: string_concat joins two strings
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "string_concat \"foo\" \"bar\""
// --- Output:
"foobar"
```

**tests/flt/expr/str-sub.flt:**
```
// Test: string_sub extracts a substring (start index + length)
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "string_sub \"hello\" 1 3"
// --- Output:
"ell"
```

**tests/flt/expr/str-contains.flt:**
```
// Test: string_contains checks if a string contains a substring
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "string_contains \"hello\" \"ell\""
// --- Output:
true
```

**tests/flt/expr/str-to-string.flt:**
```
// Test: to_string converts int and bool to string representation
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "to_string 42"
// --- Output:
"42"
```

**tests/flt/expr/str-to-int.flt:**
```
// Test: string_to_int parses a numeric string to int
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --expr "string_to_int \"42\""
// --- Output:
42
```

**tests/flt/emit/type-decl/type-decl-str-builtins.flt:**

This test uses --emit-type with a file to verify the type of a user binding that calls each string function. Note: string functions themselves are filtered from --emit-type output (they are in initialTypeEnv). Only user-defined bindings are shown. The file input must use module-level let bindings.

```
// Test: --emit-type shows correct types for bindings that use string built-ins
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --emit-type %input
// --- Input:
let a = string_length "hello"

let b = string_concat "foo" "bar"

let c = string_sub "hello" 1 3

let d = string_contains "hello" "ell"

let e = to_string 42

let f = string_to_int "42"
// --- Output:
a : int
b : string
c : string
d : bool
e : string
f : int
```

Note: --emit-type file mode outputs bindings in alphabetical order (see 08-04 decision). Variables a-f are already in alphabetical order so the output matches.

After creating the files, run all tests to confirm:
- Existing 196 F# tests still pass
- All fslit tests pass (including the 7 new ones)
  </action>
  <verify>
1. Run F# unit tests: `dotnet test tests/LangThree.Tests/ 2>&1 | tail -5` — all 196 pass.
2. Run fslit tests and check new tests pass. Look at how existing tests are run (check test runner).
   - Try: `dotnet test 2>&1 | grep -E "passed|failed|str-"` from repo root.
3. Spot-check each new test manually:
   - `src/LangThree/bin/Release/net10.0/LangThree --expr 'string_length "hello"'` → `5`
   - `src/LangThree/bin/Release/net10.0/LangThree --expr 'string_sub "hello" 1 3'` → `"ell"`
   - `src/LangThree/bin/Release/net10.0/LangThree --expr 'to_string true'` → `"true"`
  </verify>
  <done>
7 new .flt files created. All 196 F# unit tests pass. All fslit tests pass. All 5 success criteria from the phase roadmap are satisfied:
1. string_length "hello" returns 5 ✓
2. string_sub "hello" 1 3 returns "ell" ✓
3. to_string 42 returns "42", to_string true returns "true" ✓
4. string_to_int "42" returns 42 ✓
5. All string functions have correct types in --emit-type ✓
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/LangThree/LangThree.fsproj` — 0 errors, 0 warnings
2. `dotnet test` from repo root — all tests pass (196 F# + 193+ fslit)
3. Manual checks:
   - `LangThree --expr 'string_length "hello"'` → `5`
   - `LangThree --expr 'string_sub "hello" 1 3'` → `"ell"`
   - `LangThree --expr 'to_string 42'` → `"42"`
   - `LangThree --expr 'to_string true'` → `"true"`
   - `LangThree --expr 'string_to_int "42"'` → `42`
</verification>

<success_criteria>
All 5 phase success criteria satisfied:
1. string_length "hello" returns 5
2. string_sub "hello" 1 3 returns "ell"
3. to_string 42 returns "42", to_string true returns "true"
4. string_to_int "42" returns 42
5. All string functions have correct types verified via --emit-type file mode

Additionally: 0 regressions (all existing tests pass), 7 new .flt tests added.
</success_criteria>

<output>
After completion, create `.planning/phases/11-string-operations/11-02-SUMMARY.md`
</output>
