---
phase: 61-hashtable-tuple-test-conversion
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - tests/flt/file/hashtable/hashtable-forin.flt
  - tests/flt/file/hashtable/hashtable-dot-api.flt
  - tests/flt/file/hashtable/hashtable-keys-tryget.flt
  - tests/flt/file/property/property-count-consistency.flt
  - tests/flt/file/string/str-concat-module.flt
autonomous: true

must_haves:
  truths:
    - "All flt tests pass (full suite: ../fslit/dist/FsLit tests/flt/)"
    - "No flt file contains the stale path /Users/ohama/vibe/LangThree"
    - "No flt test uses kv.Key, kv.Value, ht.TryGetValue, ht.Count, or ht.Keys dot notation"
    - "hashtable-forin.flt uses for (k, v) in ht do syntax"
  artifacts:
    - path: "tests/flt/file/hashtable/hashtable-forin.flt"
      provides: "Tuple destructuring for-in test"
      contains: "for (k, v) in"
    - path: "tests/flt/file/hashtable/hashtable-dot-api.flt"
      provides: "Module function hashtable API test"
      contains: "Hashtable.tryGetValue"
    - path: "tests/flt/file/hashtable/hashtable-keys-tryget.flt"
      provides: "Module function keys/tryGet test"
      contains: "Hashtable.keys"
  key_links:
    - from: "tests/flt/file/hashtable/hashtable-forin.flt"
      to: "ForInExpr TuplePat support (Plan 01)"
      via: "for (k, v) in ht do syntax"
      pattern: "for \\(k, v\\) in"
    - from: "tests/flt/file/hashtable/hashtable-dot-api.flt"
      to: "Prelude/Hashtable.fun module functions (Phase 60)"
      via: "Hashtable.tryGetValue, Hashtable.count, Hashtable.keys"
      pattern: "Hashtable\\.(tryGetValue|count|keys)"
---

<objective>
Fix all 39 stale binary paths in flt tests and convert 3 dot-notation hashtable tests to module function style.

Purpose: TST-01 -- ensures full flt test suite passes with correct paths and no KeyValuePair dot-notation usage remains.
Output: All flt tests pass. No stale paths. Hashtable tests use module functions and tuple destructuring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-hashtable-tuple-test-conversion/61-RESEARCH.md
@.planning/phases/61-hashtable-tuple-test-conversion/61-01-SUMMARY.md

Key test files to read before converting:
@tests/flt/file/hashtable/hashtable-forin.flt
@tests/flt/file/hashtable/hashtable-dot-api.flt
@tests/flt/file/hashtable/hashtable-keys-tryget.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all stale binary paths in flt tests</name>
  <files>39 flt files under tests/flt/ (batch sed operation)</files>
  <action>
1. Run this sed command to fix all stale paths at once:
   ```bash
   find /Users/ohama/vibe-coding/LangThree/tests/flt -name "*.flt" -exec \
     sed -i '' 's|/Users/ohama/vibe/LangThree|/Users/ohama/vibe-coding/LangThree|g' {} \;
   ```

2. Verify no stale paths remain:
   ```bash
   grep -r "vibe/LangThree" /Users/ohama/vibe-coding/LangThree/tests/flt --include="*.flt" | wc -l
   ```
   Expected: 0

3. Do NOT run the full test suite yet -- the 3 hashtable dot-notation tests will fail until Task 2 converts them.
  </action>
  <verify>`grep -r "/Users/ohama/vibe/LangThree" tests/flt --include="*.flt" | wc -l` returns 0.</verify>
  <done>All 39 flt files use the correct path `/Users/ohama/vibe-coding/LangThree`.</done>
</task>

<task type="auto">
  <name>Task 2: Convert 3 hashtable dot-notation tests to module functions</name>
  <files>tests/flt/file/hashtable/hashtable-forin.flt, tests/flt/file/hashtable/hashtable-dot-api.flt, tests/flt/file/hashtable/hashtable-keys-tryget.flt</files>
  <action>
Read each file first, then apply these conversions. The expected output sections should NOT change -- only the LangThree source code inside each test changes.

**hashtable-forin.flt:**
- Replace `for kv in ht do` with `for (k, v) in ht do`
- Replace `kv.Key` with `k` and `kv.Value` with `v`
- Keep all expected output lines identical

**hashtable-dot-api.flt:**
- Replace `ht.TryGetValue("x")` with `Hashtable.tryGetValue ht "x"`
- Replace `ht.TryGetValue("missing")` with `Hashtable.tryGetValue ht "missing"`
- Replace `ht.Count` with `Hashtable.count ht`
- Replace `ht.Keys` with `Hashtable.keys ht`
- Keep all expected output lines identical

**hashtable-keys-tryget.flt:**
- Replace `ht.Keys` with `Hashtable.keys ht`
- Replace `ht.TryGetValue("a")` with `Hashtable.tryGetValue ht "a"`
- Replace `ht.TryGetValue("z")` with `Hashtable.tryGetValue ht "z"`
- Replace `ht.Count` with `Hashtable.count ht`
- Keep all expected output lines identical

IMPORTANT: Read the actual file content before editing. The research shows approximate conversions but the exact code may differ slightly. Preserve test structure (Command line, Input section, Output section) exactly.

IMPORTANT: After converting, check that no dot-notation patterns remain in any hashtable test:
```bash
grep -n '\.Key\|\.Value\|\.TryGetValue\|\.Count\|\.Keys' tests/flt/file/hashtable/*.flt
```
Expected: no matches.
  </action>
  <verify>
1. `grep -n '\.Key\|\.Value\|\.TryGetValue\|\.Count\|\.Keys' tests/flt/file/hashtable/*.flt` returns no matches.
2. `../fslit/dist/FsLit tests/flt/` -- full test suite passes (all tests PASS).
  </verify>
  <done>All 3 hashtable tests converted to module function style. Full flt test suite passes. No KeyValuePair dot notation remains in hashtable tests.</done>
</task>

</tasks>

<verification>
1. `grep -r "/Users/ohama/vibe/LangThree" tests/flt --include="*.flt" | wc -l` returns 0
2. `grep -n '\.Key\|\.Value\|\.TryGetValue\|\.Count\|\.Keys' tests/flt/file/hashtable/*.flt` returns no matches
3. `../fslit/dist/FsLit tests/flt/` -- full suite passes with zero failures
4. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` -- F# unit tests still pass
</verification>

<success_criteria>
- Zero stale binary paths in any flt file
- Zero dot-notation usage in hashtable flt tests
- Full flt test suite passes (all files)
- F# unit tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/61-hashtable-tuple-test-conversion/61-02-SUMMARY.md`
</output>
