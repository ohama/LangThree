---
phase: 61-hashtable-tuple-test-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LangThree/Ast.fs
  - src/LangThree/Parser.fsy
  - src/LangThree/Bidir.fs
  - src/LangThree/Eval.fs
  - src/LangThree/TypeCheck.fs
  - src/LangThree/Infer.fs
  - src/LangThree/Format.fs
autonomous: true

must_haves:
  truths:
    - "for (k, v) in ht do ... destructures hashtable entries as tuples"
    - "for x in [1;2;3] do ... still works (single VarPat unchanged)"
    - "for x in ht do ... binds x to a tuple value (backward compatible)"
    - "HashtableValue iteration produces TupleValue, not RecordValue KeyValuePair"
  artifacts:
    - path: "src/LangThree/Ast.fs"
      provides: "ForInExpr with var: Pattern"
      contains: "ForInExpr of var: Pattern"
    - path: "src/LangThree/Parser.fsy"
      provides: "FOR TuplePattern IN rules"
      contains: "TuplePattern"
    - path: "src/LangThree/Bidir.fs"
      provides: "TTuple element type for THashtable; pattern binding in loop env"
      contains: "TTuple"
    - path: "src/LangThree/Eval.fs"
      provides: "TupleValue for hashtable iteration; matchPattern for loop env"
      contains: "TupleValue"
  key_links:
    - from: "src/LangThree/Ast.fs"
      to: "all other files"
      via: "ForInExpr union case shape"
      pattern: "ForInExpr.*Pattern"
    - from: "src/LangThree/Eval.fs"
      to: "matchPattern"
      via: "loop env binding"
      pattern: "matchPattern.*elemVal"
---

<objective>
Change ForInExpr to support pattern destructuring in for-in loops, and make hashtable iteration produce tuples instead of KeyValuePair records.

Purpose: Enables `for (k, v) in ht do ...` syntax (STR-01), which is prerequisite for removing dot notation on KeyValuePair.
Output: Modified AST, Parser, Bidir, Eval, TypeCheck, Infer, Format files. Existing non-hashtable for-in loops continue working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-hashtable-tuple-test-conversion/61-RESEARCH.md

Key source files to read before starting:
@src/LangThree/Ast.fs (ForInExpr definition ~line 119)
@src/LangThree/Parser.fsy (ForInExpr rules ~lines 253-256; TuplePattern ~line 396)
@src/LangThree/Bidir.fs (ForInExpr type-check ~lines 231-267; LetPat pattern binding for reference)
@src/LangThree/Eval.fs (ForInExpr eval ~lines 1044-1062; matchPattern ~lines 904-956)
@src/LangThree/TypeCheck.fs (rewriteModuleAccess ForInExpr arm)
@src/LangThree/Infer.fs (ForInExpr arm — uses _ for var)
@src/LangThree/Format.fs (ForInExpr arm — debug output)
</context>

<tasks>

<task type="auto">
  <name>Task 1: AST + Parser changes for ForInExpr Pattern support</name>
  <files>src/LangThree/Ast.fs, src/LangThree/Parser.fsy</files>
  <action>
1. In Ast.fs, change the ForInExpr union case from:
   `ForInExpr of var: string * collection: Expr * body: Expr * span: Span`
   to:
   `ForInExpr of var: Pattern * collection: Expr * body: Expr * span: Span`

2. In Parser.fsy, update existing FOR IDENT IN rules to wrap the IDENT in VarPat:
   - `FOR IDENT IN Expr DO SeqExpr` -> `ForInExpr(VarPat($2, symSpan parseState 2), $4, $6, ruleSpan parseState 1 6)`
   - Same for the INDENT/DEDENT variant

3. In Parser.fsy, add new TuplePattern rules AFTER the IDENT rules:
   - `FOR TuplePattern IN Expr DO SeqExpr { ForInExpr($2, $4, $6, ruleSpan parseState 1 6) }`
   - `FOR TuplePattern IN Expr DO INDENT SeqExpr DEDENT { ForInExpr($2, $4, $7, ruleSpan parseState 1 8) }`

Note: TuplePattern is already defined in the grammar (~line 395/396). No new tokens needed.
  </action>
  <verify>Run `dotnet build src/LangThree/LangThree.fsproj -c Release` — expect build errors in Bidir.fs/Eval.fs/etc (pattern match on ForInExpr changed shape) but Parser.fsy and Ast.fs should compile. If build fails on Parser.fsy, check for shift/reduce conflicts.</verify>
  <done>Ast.fs has `ForInExpr of var: Pattern`, Parser.fsy has both IDENT (wrapped in VarPat) and TuplePattern rules for FOR-IN.</done>
</task>

<task type="auto">
  <name>Task 2: Update Bidir, Eval, TypeCheck, Infer, Format for Pattern-based ForInExpr</name>
  <files>src/LangThree/Bidir.fs, src/LangThree/Eval.fs, src/LangThree/TypeCheck.fs, src/LangThree/Infer.fs, src/LangThree/Format.fs</files>
  <action>
**Bidir.fs** (type-checker):
1. Find the ForInExpr arm (~lines 231-267). Change destructure from `ForInExpr(var, ...)` to `ForInExpr(pat, ...)`.
2. For the THashtable case: change `(s1, TData("KeyValuePair", [keyTy; valTy]))` to `(s1, TTuple [keyTy; valTy])`.
3. For loop env binding: instead of `Map.add var (Scheme([], elemTy)) env2`, use the same pattern-binding approach as LetPat. Read the LetPat arm to find how it binds patterns (likely calls `synthPat` or inline logic). Apply the same to bind `pat` against `elemTy` into `loopEnv`.
4. IMPORTANT: Do NOT remove the `TData("KeyValuePair", ...)` field access arm (~lines 636-642). Leave it in place — it will be removed after test conversion in Plan 02.

**Eval.fs** (runtime):
1. Find the ForInExpr arm (~lines 1044-1062). Change destructure from `ForInExpr(var, ...)` to `ForInExpr(pat, ...)`.
2. For the HashtableValue case: change from `RecordValue("KeyValuePair", fields)` to `TupleValue [kv.Key; kv.Value]`.
3. For loop env binding: replace `Map.add var elemVal env` with:
   ```fsharp
   let loopEnv =
       match matchPattern pat elemVal with
       | Some bindings -> List.fold (fun e (n, v) -> Map.add n v e) env bindings
       | None -> failwith "for-in: pattern match failed"
   ```
   `matchPattern` already exists in Eval.fs (~lines 904-956) and handles VarPat and TuplePat.

**TypeCheck.fs**:
1. Find `rewriteModuleAccess` ForInExpr arm. Update destructure pattern. `var` (now Pattern) passes through unchanged — patterns don't contain module references.
2. Find any other ForInExpr destructure sites (e.g., `collectModuleRefs`). Update pattern shape. The var field is typically ignored with `_`.

**Infer.fs**:
1. ForInExpr arm already uses `_` for var. Confirm it still compiles. May need no change if wildcard is used.

**Format.fs**:
1. Update ForInExpr arm. For VarPat, format as before (just the variable name). For TuplePat, format as `(k, v)`.
  </action>
  <verify>
1. `dotnet build src/LangThree/LangThree.fsproj -c Release` — must compile cleanly with zero errors.
2. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` — all F# unit tests pass.
3. Quick smoke test with flt runner on a non-hashtable test to confirm for-in still works:
   `../fslit/dist/FsLit tests/flt/file/array/` — expect PASS (array for-in unchanged).
  </verify>
  <done>Project builds cleanly. Existing for-in loops (arrays, lists) work unchanged. HashtableValue iteration produces TupleValue. ForInExpr accepts Pattern for loop variable binding.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/LangThree/LangThree.fsproj -c Release` compiles without errors
2. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` all unit tests pass
3. `../fslit/dist/FsLit tests/flt/file/array/` array for-in tests pass (regression check)
4. `../fslit/dist/FsLit tests/flt/file/list/` list for-in tests pass (regression check)
</verification>

<success_criteria>
- ForInExpr AST uses Pattern instead of string
- Parser accepts both `for x in ...` and `for (k, v) in ...` syntax
- Hashtable iteration produces TTuple/TupleValue (not KeyValuePair)
- All non-hashtable for-in tests continue to pass
- Build is clean with no warnings from Parser.fsy
</success_criteria>

<output>
After completion, create `.planning/phases/61-hashtable-tuple-test-conversion/61-01-SUMMARY.md`
</output>
