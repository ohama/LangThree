---
phase: 74-builtin-instances-tests
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - tests/flt/file/typeclass/typeclass-builtin-show.flt
  - tests/flt/file/typeclass/typeclass-builtin-show-containers.flt
  - tests/flt/file/typeclass/typeclass-builtin-eq.flt
  - tests/flt/file/typeclass/typeclass-builtin-eq-error.flt
  - tests/flt/file/typeclass/typeclass-constrained-fn-builtin.flt
autonomous: true

must_haves:
  truths:
    - "show 42 evaluates to string '42' without explicit instance in user code"
    - "show true evaluates to string 'true' without explicit instance in user code"
    - "show 'x' evaluates correctly without explicit instance in user code"
    - "show \"hello\" evaluates to 'hello' without explicit instance in user code"
    - "eq 1 1 evaluates to true"
    - "eq 1 2 evaluates to false"
    - "eq on functions produces a type error (NoInstance)"
    - "A constrained function using built-in Show works end-to-end"
    - "At least 5 new flt tests all pass"
  artifacts:
    - path: "tests/flt/file/typeclass/typeclass-builtin-show.flt"
      provides: "Built-in Show instances for int, bool, string, char"
    - path: "tests/flt/file/typeclass/typeclass-builtin-eq.flt"
      provides: "Built-in Eq instances for int"
    - path: "tests/flt/file/typeclass/typeclass-builtin-eq-error.flt"
      provides: "Eq on functions produces type error"
    - path: "tests/flt/file/typeclass/typeclass-constrained-fn-builtin.flt"
      provides: "Constrained function using built-in Show instances"
  key_links:
    - from: "tests/flt/file/typeclass/typeclass-builtin-show.flt"
      to: "Prelude/Typeclass.fun"
      via: "show available from prelude without user instance declaration"
      pattern: "show"
---

<objective>
Create 5+ flt integration tests validating that built-in Show and Eq instances work end-to-end, covering all Phase 74 success criteria.

Purpose: Lock in the behavior of built-in typeclass instances so regressions are caught.
Output: 5+ new .flt test files, all passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-builtin-instances-tests/74-01-SUMMARY.md
@tests/flt/file/typeclass/typeclass-runtime-basic.flt (pattern for runtime flt tests)
@tests/flt/file/typeclass/typeclass-infer-errors.flt (pattern for error flt tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flt tests for built-in Show instances (primitives)</name>
  <files>tests/flt/file/typeclass/typeclass-builtin-show.flt</files>
  <action>
Create `tests/flt/file/typeclass/typeclass-builtin-show.flt` testing that `show` works for primitive types WITHOUT any user-declared typeclass or instance:

Test program (no typeclass/instance declarations — they come from prelude):
```
let result = show 42
```
Expected stdout: `"42"` (StringValue is wrapped in quotes by formatValue)

Then create additional test cases. The flt format is one test per file, so if multiple primitives need testing, either:
- Test them all in one file by having the last binding be the one that prints, OR
- Create separate files per type

Recommended: one file testing `show` on int (the primary case from SC1). The other primitives (bool, string, char) can go in a second test or be combined.

**Pattern:** Follow the existing `typeclass-runtime-basic.flt` format:
```
// Test: description
// --- Command: /path/to/binary %input
// --- Input:
<langthree code>
// --- Stdout:
<expected output>
```

**Important:** First run the binary on a temp file to capture exact output, THEN write the flt file with that output. Do not guess formatValue output.

Verify the binary path by checking an existing runtime test:
```bash
head -2 tests/flt/file/typeclass/typeclass-runtime-basic.flt
```
Use the same binary path.
  </action>
  <verify>`../fslit/dist/FsLit tests/flt/file/typeclass/typeclass-builtin-show.flt` passes</verify>
  <done>flt test confirms `show 42` works from prelude without user instance declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Create remaining flt tests (containers, Eq, Eq error, constrained function)</name>
  <files>
tests/flt/file/typeclass/typeclass-builtin-show-containers.flt
tests/flt/file/typeclass/typeclass-builtin-eq.flt
tests/flt/file/typeclass/typeclass-builtin-eq-error.flt
tests/flt/file/typeclass/typeclass-constrained-fn-builtin.flt
  </files>
  <action>
Create 4 more flt test files. For each, first verify exact output by running the binary on a temp file, then write the flt.

**Test 2a: `typeclass-builtin-show-containers.flt`**
Tests `show` on list and/or Option types (if Plan 01 added instances for these). If Plan 01 could NOT add list/option instances (parser limitation), skip this test and create an alternative test instead (e.g., `typeclass-builtin-show-bool-char.flt` testing `show true` and `show 'x'`).

Test program:
```
let result = show [1; 2; 3]
```
OR if list Show not available:
```
let a = show true
let b = show 'x'
let result = show "hello"
```

**Test 2b: `typeclass-builtin-eq.flt`**
Tests `eq` on integers from prelude.
```
let result = eq 1 1
```
Expected stdout: `true`

Also test `eq 1 2` -> `false`. Since flt prints last binding:
```
let result = eq 1 2
```
Expected: `false`

Pick one (e.g., `eq 1 1` -> `true`) for the primary test.

**Test 2c: `typeclass-builtin-eq-error.flt`**
Tests that `eq (fun x -> x) (fun x -> x)` produces a type error. Use `--check` mode.
```
let result = eq (fun x -> x) (fun x -> x)
```
Expected stderr should contain a NoInstance error mentioning Eq.

Follow the pattern from `typeclass-infer-errors.flt` for error test format.

**Test 2d: `typeclass-constrained-fn-builtin.flt`**
Tests a user-defined constrained function that uses the built-in Show:
```
let show_twice x = show x + show x

let result = show_twice 42
```
Expected stdout: `"4242"` (two string concatenations, result is StringValue "4242" which formatValue wraps in quotes)

**For each test:** Run the binary first to capture exact output. Write the flt file matching that output exactly.
  </action>
  <verify>
1. `../fslit/dist/FsLit tests/flt/file/typeclass/` — all typeclass tests pass (existing 12 + new 5 = 17)
2. `../fslit/dist/FsLit tests/flt/` — all tests pass (671 + 5 = 676)
  </verify>
  <done>5+ new flt tests cover: built-in Show primitives, built-in Show containers (or additional primitives), built-in Eq, Eq type error on functions, and constrained function with built-in instances. All tests pass.</done>
</task>

</tasks>

<verification>
1. `../fslit/dist/FsLit tests/flt/file/typeclass/` — all 17 typeclass tests pass
2. `../fslit/dist/FsLit tests/flt/` — all tests pass (676+ total)
3. At least 5 NEW test files exist in `tests/flt/file/typeclass/` with `builtin` or `constrained-fn-builtin` in their names
</verification>

<success_criteria>
- 5+ new flt test files created and passing
- Tests cover: typeclass declaration (existing), instance declaration (existing), constrained function with built-in (new), built-in Show (new), built-in Eq (new)
- No regressions in existing 671 tests
- Phase 74 success criteria SC1, SC2, SC3 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/74-builtin-instances-tests/74-02-SUMMARY.md`
</output>
