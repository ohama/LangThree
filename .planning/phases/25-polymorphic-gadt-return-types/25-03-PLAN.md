---
phase: 25-polymorphic-gadt-return-types
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tutorial/14-gadt.md
autonomous: true

must_haves:
  truths:
    - "Tutorial shows the new polymorphic pattern without annotation requirement"
    - "Tutorial explains type variable annotation `'a` as an alternative to concrete annotation"
    - "Tutorial's 'why annotation is required' section updated to reflect that annotation is now optional"
    - "Tutorial's existing examples (concrete annotations, recursive GADT) still accurate"
  artifacts:
    - path: "tutorial/14-gadt.md"
      provides: "Updated GADT tutorial with polymorphic return type section"
      contains: "OCaml 스타일 다형적"
  key_links:
    - from: "tutorial/14-gadt.md section on annotations"
      to: "new polymorphic section"
      via: "sequential reading flow"
---

<objective>
Update tutorial/14-gadt.md to document the new polymorphic GADT return type feature. The tutorial currently says annotation is always required (E0401 without it). Update to reflect that annotation is now optional (synth delegation) and explain the type variable annotation syntax `'a`.

Purpose: Users reading the tutorial after Phase 25 ships should understand the full capability: annotation-free polymorphic eval AND explicit type variable annotation for controlled polymorphism.
Output: Updated tutorial Ch14 with a new "다형적 반환 타입" section and updated annotation guidance.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@tutorial/14-gadt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add polymorphic return type section and update annotation guidance in Ch14</name>
  <files>tutorial/14-gadt.md</files>
  <action>
Make the following targeted changes to tutorial/14-gadt.md:

**Change 1: Update the "왜 타입 주석이 필요한가" section (around line 147-179)**

The section currently says annotation is required and E0401 appears without it. Update to explain:
- Annotation is no longer required in most cases
- When annotation is absent, synth mode delegates to check with a fresh type variable → polymorphic behavior
- When annotation is `'a`, it explicitly requests polymorphic return
- When annotation is a concrete type like `int`, it constrains all branches to return `int`
- E0401 is removed from this behavior

Replace the existing E0401 warning example with an explanation of the three modes:

```
### 타입 주석의 역할

GADT match에서 타입 주석은 **선택사항**입니다. 주석의 존재 여부와 종류에 따라 동작이 달라집니다:

**주석 없음 (권장 — 다형적 반환)**
주석 없이 match를 쓰면 컴파일러가 결과 타입을 자동으로 추론합니다. 각 분기는 독립적으로 정제됩니다:

```
type Expr 'a = IntLit : int -> int Expr | BoolLit : bool -> bool Expr

let eval e = match e with | IntLit n -> n | BoolLit b -> b

let r1 = eval (IntLit 42)    // r1 : int = 42
let r2 = eval (BoolLit true) // r2 : bool = true
```

**타입 변수 주석 `'a` (명시적 다형성)**
`'a`를 주석으로 쓰면 "이 match는 다형적 결과를 반환한다"는 의도를 명시적으로 표현합니다:

```
let result = (match IntLit 42 with | IntLit n -> n | BoolLit b -> if b then 1 else 0 : 'a)
// result : int = 42
```

**구체적 타입 주석 `: int` (단일 타입 강제)**
구체적 타입을 지정하면 모든 분기가 그 타입을 반환해야 합니다. 재귀 평가기에서 유용합니다:

```
let rec eval e = (match e with | IntLit n -> n | BoolLit b -> if b then 1 else 0 : int)
```
```

**Change 2: Update the "규칙:" callout at line 179**

Change from:
```
**규칙:** GADT를 match할 때는 항상 `(match e with | ... : ResultType)` 형태로 씁니다.
```

To:
```
**참고:** 주석 없이도 동작하지만, 재귀 함수에서 반환 타입을 명시해야 할 때는 `(match e with | ... : ResultType)` 형태를 씁니다.
```

**Change 3: Add a new section "다형적 반환 타입 (OCaml 스타일)" after the existing "재귀 GADT" section (around line 181)**

Insert a new section demonstrating the `eval : 'a Expr -> 'a` pattern:

```markdown
## 다형적 반환 타입

가장 강력한 GADT 패턴 중 하나는 함수의 반환 타입이 입력 GADT의 타입 매개변수와 일치하는 것입니다. OCaml에서 `eval : 'a expr -> 'a`로 알려진 패턴입니다.

```
$ cat poly-eval.l3
type Expr 'a = IntLit : int -> int Expr | BoolLit : bool -> bool Expr

let eval e = match e with | IntLit n -> n | BoolLit b -> b

let r1 = eval (IntLit 42)
let r2 = eval (BoolLit true)

$ langthree poly-eval.l3
42
true
```

`eval`의 타입은 `'a Expr -> 'a`입니다. `IntLit 42`를 넘기면 `int`를, `BoolLit true`를 넘기면 `bool`을 반환합니다. 단일 함수가 입력의 GADT 타입 매개변수에 따라 다른 반환 타입을 가집니다.

이것이 가능한 이유: 컴파일러는 `match e with | IntLit n -> n | BoolLit b -> b`를 볼 때, 각 분기에서 타입 정제를 통해 독립적으로 타입을 확인합니다. `IntLit` 분기에서는 `n : int`이므로 결과가 `int`, `BoolLit` 분기에서는 `b : bool`이므로 결과가 `bool`. 두 분기가 서로 다른 타입을 반환해도 되는 것은 GADT의 타입 정제 덕분입니다 — 일반 ADT나 non-GADT match에서는 모든 분기가 같은 타입을 반환해야 합니다.
```

Preserve all other existing content in the tutorial exactly.
  </action>
  <verify>grep -n "다형적 반환 타입\|poly-eval\|주석 없음" tutorial/14-gadt.md | head -10</verify>
  <done>Tutorial contains new "다형적 반환 타입" section, updated annotation guidance, and the `eval : 'a Expr -> 'a` example.</done>
</task>

</tasks>

<verification>
1. `grep -c "다형적 반환 타입" tutorial/14-gadt.md` — returns at least 1
2. `grep "E0401" tutorial/14-gadt.md` — should return 0 (E0401 reference removed from annotation guidance) OR the remaining reference explains it was a previous behavior
3. Tutorial reads coherently: annotation-free section leads into type-var annotation, then concrete annotation
</verification>

<success_criteria>
- COV-04: tutorial/14-gadt.md updated with polymorphic GADT section
- "다형적 반환 타입" section present with poly-eval.l3 example
- Annotation guidance updated to reflect annotation is now optional
- Existing content (재귀 GADT, GADT 완전성) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/25-polymorphic-gadt-return-types/25-03-SUMMARY.md`
</output>
