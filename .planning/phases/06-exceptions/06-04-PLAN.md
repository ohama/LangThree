---
phase: 06-exceptions
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - tests/LangThree.Tests/ExceptionTests.fs
  - tests/LangThree.Tests/LangThree.Tests.fsproj
autonomous: true

must_haves:
  truths:
    - "User can declare custom exceptions with data"
    - "User can raise exceptions with raise"
    - "User can catch exceptions with try-with expressions"
    - "User can pattern match on exception types in handlers"
    - "User can use when guards in exception handlers to filter cases"
    - "When guards work in standard match expressions too"
  artifacts:
    - path: "tests/LangThree.Tests/ExceptionTests.fs"
      provides: "Comprehensive exception tests for EXC-01 through EXC-05"
      min_lines: 100
  key_links:
    - from: "tests/LangThree.Tests/ExceptionTests.fs"
      to: "src/LangThree/Program.fs"
      via: "parseModule + typeCheckModule + eval pipeline"
      pattern: "parseModule|typeCheckModule"
---

<objective>
Create comprehensive integration tests for all exception features (EXC-01 through EXC-05) including when guards in standard match expressions.

Purpose: Verify all five exception requirements work end-to-end through the full pipeline (parse -> type check -> evaluate). Also verify when guards work in standard match expressions since they share the MatchClause infrastructure.

Output: ExceptionTests.fs with tests covering all requirements and edge cases. All tests pass. Any bugs found during testing are fixed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-exceptions/06-RESEARCH.md
@.planning/phases/06-exceptions/06-01-SUMMARY.md
@.planning/phases/06-exceptions/06-02-SUMMARY.md
@.planning/phases/06-exceptions/06-03-SUMMARY.md
@tests/LangThree.Tests/ModuleTests.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExceptionTests.fs with comprehensive test coverage</name>
  <files>tests/LangThree.Tests/ExceptionTests.fs, tests/LangThree.Tests/LangThree.Tests.fsproj</files>
  <action>
Create tests/LangThree.Tests/ExceptionTests.fs following the pattern established in ModuleTests.fs (use the same test helpers for parsing, type checking, and evaluation).

Add ExceptionTests.fs to the .fsproj file (before Program.fs, after the last test file).

**EXC-01: Exception declarations:**
- Nullary exception: `exception DivideByZero`
- Exception with data: `exception ParseError of string`
- Exception with tuple data: `exception NetworkError of string * int`
- Verify these parse and type-check without errors

**EXC-02: raise:**
- `raise DivideByZero` -- nullary exception
- `raise (ParseError "bad input")` -- exception with data
- `raise` in if-else branch: `if x = 0 then raise DivideByZero else x` -- verifies raise gets type of the other branch
- Type error: `raise 42` -- should fail type checking (int is not exn)

**EXC-03: try-with expressions:**
- Basic: `try raise DivideByZero with | DivideByZero -> 0`
- With data: `try raise (ParseError "bad") with | ParseError msg -> msg`
- No exception thrown: `try 42 with | DivideByZero -> 0` -- should return 42
- Multi-line try-with with indentation

**EXC-04: Pattern matching on exception types:**
- Multiple exception handlers: `try ... with | DivideByZero -> 0 | ParseError msg -> 1`
- Wildcard handler: `try ... with | _ -> 0`
- Nested try-with

**EXC-05: when guards in exception handlers:**
- `try ... with | NetworkError (msg, code) when code >= 500 -> "server" | NetworkError (msg, code) when code >= 400 -> "client" | _ -> "other"`

**When guards in standard match (bonus -- shares MatchClause infrastructure):**
- `match x with | n when n < 0 -> "negative" | n when n = 0 -> "zero" | _ -> "positive"`
- Guard with pattern binding: `match lst with | x :: _ when x > 0 -> "positive head" | _ -> "other"`

**Edge cases:**
- Exception constructor used as pattern in match (not just try-with)
- Exception in module: declare exception inside module, raise with qualified name
- Nested try-with (inner catch doesn't match, outer does)

Test structure: Use Expecto `testList "Exceptions" [...]` with subtests for each requirement.
Each test should use the `parseModule` -> `typeCheckModule` -> `eval` pipeline (look at ModuleTests.fs for the pattern).

Fix any bugs discovered during testing. Common issues:
- Constructor registration in eval environment for ExceptionDecl
- Qualified module access for exception constructors
- IndentFilter issues with try-with blocks
- Pattern matching DataValue in exception handlers
  </action>
  <verify>Run `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj 2>&1 | tail -30`. ALL tests (existing + new) must pass.</verify>
  <done>ExceptionTests.fs exists with tests for EXC-01 through EXC-05 plus when guards in match. All tests pass. Any bugs found are fixed.</done>
</task>

</tasks>

<verification>
1. `dotnet test` -- all tests pass (existing + exception tests)
2. EXC-01: Exception declarations parse and type-check (nullary and with data)
3. EXC-02: raise throws exceptions, type errors on non-exn argument
4. EXC-03: try-with catches exceptions, passthrough when no exception
5. EXC-04: Multiple exception patterns, wildcard handler, nested try-with
6. EXC-05: when guards filter exception handlers by condition
7. When guards work in standard match expressions
</verification>

<success_criteria>
- All 5 exception requirements (EXC-01 through EXC-05) have passing tests
- When guards work in both match and try-with
- All existing tests still pass (zero regressions)
- Edge cases tested: modules, nesting, type errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-exceptions/06-04-SUMMARY.md`
</output>
