---
phase: 53-tests-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tutorial/22-practical-programming.md
  - tutorial/SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "tutorial/22-practical-programming.md exists with sections for all three v6.0 features"
    - "tutorial/SUMMARY.md lists chapter 22 under the 실용 프로그래밍 section after chapter 21"
    - "All code examples in chapter 22 produce exactly the output shown when run through the LangThree binary"
  artifacts:
    - path: "tutorial/22-practical-programming.md"
      provides: "Tutorial chapter covering newline sequencing, for-in loops, Option/Result utilities"
      min_lines: 150
    - path: "tutorial/SUMMARY.md"
      provides: "mdBook table of contents with chapter 22 entry"
      contains: "22-practical-programming.md"
  key_links:
    - from: "tutorial/22-practical-programming.md"
      to: "LangThree binary"
      via: "all code examples run through binary to verify expected output before writing"
    - from: "tutorial/SUMMARY.md"
      to: "tutorial/22-practical-programming.md"
      via: "mdBook link entry after 21-imperative-ergonomics.md"
---

<objective>
Write tutorial chapter 22 (실용 프로그래밍 / Practical Programming) covering all three v6.0 features: newline implicit sequencing, for-in collection loops, and Option/Result utility functions. Update SUMMARY.md to include the new chapter.

Purpose: TST-36 requires a tutorial chapter explaining the three v6.0 features to users. Without it, the features are implemented but undocumented in the user-facing tutorial.
Output: tutorial/22-practical-programming.md with verified code examples; tutorial/SUMMARY.md updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-tests-and-documentation/53-RESEARCH.md

# Style reference — read these to match chapter format exactly
@tutorial/SUMMARY.md
@tutorial/21-imperative-ergonomics.md

# For-in loop examples (check exact output format)
@tests/flt/expr/loop/loop-for-in-basic.flt

# Option/Result combinator examples (check exact output format)
@tests/flt/file/prelude/prelude-option-map.flt
@tests/flt/file/prelude/prelude-option-bind.flt
@tests/flt/file/prelude/prelude-result-map.flt
@tests/flt/file/prelude/prelude-result-to-option.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all tutorial code examples against the binary</name>
  <files></files>
  <action>
Before writing the chapter, run each planned code example through the LangThree binary to get exact expected output. Write each example to a temp file and run it.

Binary path: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree`

Examples to verify (write to /tmp/, run, record exact stdout):

**Example 1 — Newline sequencing: multi-line function body**
```
let greet name =
    println ("Hello, " ^ name)
    println "Welcome to LangThree"
let _ = greet "Alice"
```

**Example 2 — Newline sequencing: multi-line if/else body**
```
let check x =
    if x > 0 then
        println "positive"
        x * 2
    else
        println "non-positive"
        0
let result = check 5
```

**Example 3 — For-in loop: list iteration**
```
let nums = [1; 2; 3]
let _ =
    for n in nums do
        println (to_string n)
```

**Example 4 — For-in loop: array iteration**
```
let arr = array_create 3 0
let _ = arr.[0] <- 10
let _ = arr.[1] <- 20
let _ = arr.[2] <- 30
let _ =
    for x in arr do
        println (to_string x)
```

**Example 5 — Option utilities: optionMap and optionBind**
```
let doubled = optionMap (fun x -> x * 2) (Some 21)
let chained = optionBind (fun x -> if x > 10 then Some (x + 1) else None) doubled
let _ = println (to_string doubled)
let _ = println (to_string chained)
```

**Example 6 — Option utilities: optionDefaultValue and optionFilter**
```
let safe = optionDefaultValue 0 (Some 42)
let fallback = optionDefaultValue 0 None
let filtered = optionFilter (fun x -> x > 5) (Some 10)
let rejected = optionFilter (fun x -> x > 5) (Some 3)
let _ = println (to_string safe)
let _ = println (to_string fallback)
let _ = println (to_string filtered)
let _ = println (to_string rejected)
```

**Example 7 — Result utilities: resultMap and resultToOption**
```
let r = Ok 42
let mapped = resultMap (fun x -> x * 2) r
let asOption = resultToOption mapped
let errCase = resultToOption (Error "oops")
let _ = println (to_string mapped)
let _ = println (to_string asOption)
let _ = println (to_string errCase)
```

**Example 8 — Composition: newline sequencing + for-in + Option utilities together**
```
let process items =
    let _ =
        for item in items do
            let result = optionMap (fun x -> x * 2) item
            println (to_string result)
    ()
let _ = process [Some 1; None; Some 3]
```

For each example: write to a temp file, run binary, record exact output (including any trailing `()`). These exact outputs go in the chapter.

CRITICAL: Do NOT invent expected outputs. Only use what the binary actually produces. Pay attention to:
- `for-in` loops return unit — if loop is not last expression, unit may not appear
- `println` returns `()` — if it's last in a sequence, `()` may appear in output
- `let _ = ...` suppresses the binding's value from being printed at module level
- `to_string` on Option values: check whether output is `Some 42` or `Some(42)` — use what the binary actually outputs
  </action>
  <verify>
All temp files run successfully (exit code 0 or expected error). No binary invocation produces unexpected output format. All outputs recorded.
  </verify>
  <done>All 8 code examples have been run through the binary and exact expected outputs are known.</done>
</task>

<task type="auto">
  <name>Task 2: Write tutorial/22-practical-programming.md and update SUMMARY.md</name>
  <files>tutorial/22-practical-programming.md, tutorial/SUMMARY.md</files>
  <action>
Write `tutorial/22-practical-programming.md` following the style of chapter 21 (`tutorial/21-imperative-ergonomics.md`) exactly:
- Title format: `# 22장: 실용 프로그래밍 (Practical Programming)`
- Korean prose, English identifiers/code
- Section headers: Korean with English in parentheses
- Code blocks: triple-backtick with no language tag, `$ cat filename.l3` then `$ langthree filename.l3` then output
- No trailing whitespace in code blocks

The chapter must cover these sections in order:

**Section 1: 뉴라인 암묵적 시퀀싱 (Newline Implicit Sequencing)**
- Explain: v6.0 removes need for explicit `;` in multi-line bodies — same-indent newlines auto-sequence
- Show Example 1 (multi-line function body with multiple printlns — no `;` needed)
- Show Example 2 (if/else body with multiple lines per branch)
- Explain when SEMICOLON is NOT injected: structural terminators (else/with/|) and indented continuation arguments

**Section 2: 컬렉션 for-in 루프 (For-In Collection Loops)**
- Explain: `for x in collection do body` iterates over lists and arrays without calling List.iter
- Show Example 3 (list iteration)
- Show Example 4 (array iteration)
- Mention: loop variable is immutable (attempting `x <- ...` produces E0320), empty collection executes body zero times

**Section 3: Option/Result 유틸리티 (Option/Result Utilities)**
- Explain: v6.0 adds 8 new prelude functions for transforming Option and Result values without manual pattern matching
- Show Example 5 (optionMap + optionBind chain)
- Show Example 6 (optionDefaultValue + optionFilter)
- Show Example 7 (resultMap + resultToOption)
- Include a summary table of all 8 functions (name, signature in words, description)

**Section 4: 종합 예제 (Composition Example)**
- Show Example 8 (newline sequencing + for-in + Option utilities together)
- Brief explanation of how the features compose

**Summary table** at end listing all new v6.0 syntax/functions.

Use ONLY the outputs verified in Task 1. Do not write any expected output from memory.

---

Also update `tutorial/SUMMARY.md`:
- Find the line: `- [명령형 에르고노믹스](21-imperative-ergonomics.md)`
- Add immediately after it: `- [실용 프로그래밍](22-practical-programming.md)`
- No other changes to SUMMARY.md
  </action>
  <verify>
1. `tutorial/22-practical-programming.md` exists and has > 150 lines
2. `tutorial/SUMMARY.md` contains `22-practical-programming.md`
3. Run a few key examples again to confirm chapter outputs are still correct:
   `echo 'let doubled = optionMap (fun x -> x * 2) (Some 21)' > /tmp/verify.l3 && /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /tmp/verify.l3`
4. Chapter follows style of chapter 21 (Korean prose, no syntax highlighting tags on code blocks)
  </verify>
  <done>tutorial/22-practical-programming.md is present with verified examples for all three v6.0 features. SUMMARY.md lists chapter 22 after chapter 21. TST-36 is satisfied.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `tutorial/22-practical-programming.md` exists with sections on all 3 v6.0 features
2. `tutorial/SUMMARY.md` includes the chapter 22 entry under 실용 프로그래밍 section
3. All code examples in the chapter produce exactly the output shown (verified against binary)
4. Chapter style matches chapter 21 (Korean prose, English code, ` ``` ` blocks with `$ cat` / `$ langthree` format)
5. TST-36 is fully satisfied
</verification>

<success_criteria>
- tutorial/22-practical-programming.md exists, >= 150 lines, covers all 3 v6.0 features
- tutorial/SUMMARY.md lists 22-practical-programming.md after 21-imperative-ergonomics.md
- Every code example in the chapter has been run through the binary and the output matches exactly
- Korean prose with English code/identifiers throughout
</success_criteria>

<output>
After completion, create `.planning/phases/53-tests-and-documentation/53-02-SUMMARY.md` with:
- Chapter 22 file path and line count
- List of v6.0 features documented
- Confirmation that all code examples were verified against the binary
- SUMMARY.md update confirmation
</output>
