---
phase: 52-option-result-prelude
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Prelude/Option.fun
  - Prelude/Result.fun
  - tests/flt/file/prelude/prelude-option-iter.flt
  - tests/flt/file/prelude/prelude-option-filter.flt
  - tests/flt/file/prelude/prelude-option-default-value.flt
  - tests/flt/file/prelude/prelude-result-iter.flt
  - tests/flt/file/prelude/prelude-result-to-option.flt
  - tests/flt/file/prelude/prelude-result-default-value.flt
autonomous: true

must_haves:
  truths:
    - "optionIter applies the function to Some x and produces unit; does nothing for None"
    - "optionFilter returns Some x when predicate holds, None otherwise; passes None through"
    - "optionDefaultValue extracts the value from Some x, returns the default for None"
    - "optionIsSome and optionIsNone return the same results as isSome and isNone"
    - "resultIter applies the function to Ok x and produces unit; does nothing for Error"
    - "resultToOption converts Ok x to Some x and Error _ to None"
    - "resultDefaultValue extracts the value from Ok x, returns the default for Error"
    - "All existing prelude tests remain green — optionDefault and resultDefault still work"
  artifacts:
    - path: "Prelude/Option.fun"
      provides: "optionIter, optionFilter, optionDefaultValue, optionIsSome, optionIsNone added inside module Option"
      contains: "let optionIter"
    - path: "Prelude/Result.fun"
      provides: "resultIter, resultToOption, resultDefaultValue added inside module Result"
      contains: "let resultIter"
    - path: "tests/flt/file/prelude/prelude-option-iter.flt"
      provides: "flt test for optionIter"
    - path: "tests/flt/file/prelude/prelude-result-to-option.flt"
      provides: "flt test for resultToOption"
  key_links:
    - from: "Prelude/Result.fun"
      to: "Prelude/Option.fun"
      via: "alphabetical load order — Some/None constructors available via open Option"
      pattern: "Some x|None"
---

<objective>
Add six new utility functions to Prelude/Option.fun and Prelude/Result.fun, plus flt integration tests for each, completing the Option/Result combinator set required by OPTRES-01 through OPTRES-04.

Purpose: Users can transform, filter, iterate, and bridge Option and Result values using library functions instead of manual pattern matching boilerplate.
Output: Updated Option.fun and Result.fun with new functions, six new flt test files, all 582+ existing tests still passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-option-result-prelude/52-RESEARCH.md
@Prelude/Option.fun
@Prelude/Result.fun
@tests/flt/file/prelude/prelude-option-default.flt
@tests/flt/file/prelude/prelude-option-map.flt
@tests/flt/file/prelude/prelude-result-default.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new functions to Prelude/Option.fun and Prelude/Result.fun</name>
  <files>
    Prelude/Option.fun
    Prelude/Result.fun
  </files>
  <action>
Edit Prelude/Option.fun: inside the `module Option =` block, after the existing `let (<|>)` line and before the closing of the module, add these five new let bindings in this exact order:

```
    let optionIter f = fun opt -> match opt with | Some x -> f x | None -> ()
    let optionFilter pred = fun opt -> match opt with | Some x -> if pred x then Some x else None | None -> None
    let optionDefaultValue def = fun opt -> match opt with | Some x -> x | None -> def
    let optionIsSome opt = match opt with | Some _ -> true | None -> false
    let optionIsNone opt = match opt with | Some _ -> false | None -> true
```

Do NOT remove or rename any existing functions (optionDefault, isSome, isNone, (<|>)) — all five above are additions only.

Edit Prelude/Result.fun: inside the `module Result =` block, after the existing `let isError` line and before the closing of the module, add these three new let bindings in this exact order:

```
    let resultIter f = fun r -> match r with | Ok x -> f x | Error _ -> ()
    let resultToOption r = match r with | Ok x -> Some x | Error _ -> None
    let resultDefaultValue def = fun r -> match r with | Ok x -> x | Error _ -> def
```

Critical: `resultToOption` uses `Some` and `None` constructors. These are available in Result.fun because Option.fun loads first (alphabetically) and `open Option` applies globally. Do NOT add `open Option` inside Result.fun — the constructors are already in scope.

Do NOT remove or rename any existing functions (resultDefault, isOk, isError).

After editing both files, run the build to confirm no syntax errors:
```
dotnet build /Users/ohama/vibe-coding/LangThree/src/LangThree/LangThree.fsproj -c Release
```
  </action>
  <verify>
    `dotnet build src/LangThree/LangThree.fsproj -c Release` exits 0.
    Run `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` — all unit tests pass.
    Run `../fslit/dist/FsLit tests/flt/file/prelude/prelude-option-default.flt` — existing test still passes (confirms optionDefault not broken).
    Run `../fslit/dist/FsLit tests/flt/file/prelude/prelude-result-default.flt` — existing test still passes (confirms resultDefault not broken).
  </verify>
  <done>
    Build exits 0. All existing prelude flt tests pass. Prelude/Option.fun contains `let optionIter`, `let optionFilter`, `let optionDefaultValue`, `let optionIsSome`, `let optionIsNone`. Prelude/Result.fun contains `let resultIter`, `let resultToOption`, `let resultDefaultValue`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write flt integration tests for the six new functions</name>
  <files>
    tests/flt/file/prelude/prelude-option-iter.flt
    tests/flt/file/prelude/prelude-option-filter.flt
    tests/flt/file/prelude/prelude-option-default-value.flt
    tests/flt/file/prelude/prelude-option-is-some-none.flt
    tests/flt/file/prelude/prelude-result-iter.flt
    tests/flt/file/prelude/prelude-result-to-option.flt
    tests/flt/file/prelude/prelude-result-default-value.flt
  </files>
  <action>
Create each flt file using the exact header format from existing prelude tests. The command line is always:
`/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`

**prelude-option-iter.flt** — tests optionIter side effect:
```
// Test: Prelude optionIter applies function to Some, skips None
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = optionIter (fun x -> println (to_string x)) (Some 42)
let _ = optionIter (fun x -> println (to_string x)) None
// --- Output:
42
```

(Note: None branch produces no output — only "42" appears.)

**prelude-option-filter.flt** — tests optionFilter predicate:
```
// Test: Prelude optionFilter keeps Some when pred holds, drops otherwise
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let r1 = optionFilter (fun x -> x > 0) (Some 42)
let r2 = optionFilter (fun x -> x > 0) (Some (-1))
let r3 = optionFilter (fun x -> x > 0) None
let result = (r1, r2, r3)
// --- Output:
(Some 42, None, None)
```

**prelude-option-default-value.flt** — tests optionDefaultValue alias:
```
// Test: Prelude optionDefaultValue extracts value or returns default
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let r1 = optionDefaultValue 99 (Some 42)
let r2 = optionDefaultValue 99 None
let result = (r1, r2)
// --- Output:
(42, 99)
```

**prelude-option-is-some-none.flt** — tests optionIsSome and optionIsNone aliases:
```
// Test: Prelude optionIsSome and optionIsNone predicates
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let r1 = optionIsSome (Some 42)
let r2 = optionIsSome None
let r3 = optionIsNone (Some 42)
let r4 = optionIsNone None
let result = (r1, r2, r3, r4)
// --- Output:
(true, false, false, true)
```

**prelude-result-iter.flt** — tests resultIter side effect:
```
// Test: Prelude resultIter applies function to Ok, skips Error
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = resultIter (fun x -> println (to_string x)) (Ok 42)
let _ = resultIter (fun x -> println (to_string x)) (Error "oops")
// --- Output:
42
```

(Note: Error branch produces no output — only "42" appears.)

**prelude-result-to-option.flt** — tests resultToOption bridge:
```
// Test: Prelude resultToOption converts Ok to Some and Error to None
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let r1 = resultToOption (Ok 42)
let r2 = resultToOption (Error "oops")
let result = (r1, r2)
// --- Output:
(Some 42, None)
```

**prelude-result-default-value.flt** — tests resultDefaultValue alias:
```
// Test: Prelude resultDefaultValue extracts Ok value or returns default
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let r1 = resultDefaultValue 99 (Ok 42)
let r2 = resultDefaultValue 99 (Error "oops")
let result = (r1, r2)
// --- Output:
(42, 99)
```

After creating all seven files, run the full prelude flt suite to confirm all pass:
```
../fslit/dist/FsLit /Users/ohama/vibe-coding/LangThree/tests/flt/file/prelude/
```
  </action>
  <verify>
    `../fslit/dist/FsLit tests/flt/file/prelude/` — all prelude flt tests pass, including the seven new ones.
    The seven new .flt files exist in tests/flt/file/prelude/.
    Run full flt suite `../fslit/dist/FsLit tests/flt/` to confirm no regressions in other areas.
  </verify>
  <done>
    All seven new flt tests pass. Full flt suite passes (582+ tests, with 7 new ones added). Every OPTRES requirement is covered: optionIter (OPTRES-02), optionFilter (OPTRES-02), optionDefaultValue (OPTRES-01), optionIsSome/optionIsNone (OPTRES-02), resultIter (OPTRES-03), resultToOption (OPTRES-04), resultDefaultValue (OPTRES-04).
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `dotnet build src/LangThree/LangThree.fsproj -c Release` exits 0
2. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` — all F# unit tests pass
3. `../fslit/dist/FsLit tests/flt/` — full flt suite passes with 7 new tests added
4. Spot-check backward compatibility: `prelude-option-default.flt` and `prelude-result-default.flt` still pass (old names work)
5. Prelude/Option.fun contains all 5 new functions inside `module Option =`
6. Prelude/Result.fun contains all 3 new functions inside `module Result =`
</verification>

<success_criteria>
- optionIter, optionFilter, optionDefaultValue, optionIsSome, optionIsNone defined in Prelude/Option.fun
- resultIter, resultToOption, resultDefaultValue defined in Prelude/Result.fun
- Seven new flt tests pass, one per new function (optionIsSome and optionIsNone share one test file)
- All pre-existing tests pass — zero regressions
- OPTRES-01, OPTRES-02, OPTRES-03, OPTRES-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-result-prelude/52-01-SUMMARY.md` following the summary template.
</output>
