---
phase: 25-polymorphic-gadt-return-types
plan: 05
type: execute
wave: 2
depends_on: ["25-04"]
files_modified:
  - tutorial/14-gadt.md
  - docs/14-gadt.html
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Tutorial 14-gadt.md no longer shows non-working code as working"
    - "The poly-eval.l3 example in the tutorial compiles and runs correctly"
    - "Section '다형적 반환 타입' accurately reflects implemented capability after plan 25-04"
  artifacts:
    - path: "tutorial/14-gadt.md"
      provides: "Accurate polymorphic return section with working example"
      contains: "let eval e = match e with | IntLit n -> n | BoolLit b -> b"
    - path: "docs/14-gadt.html"
      provides: "Rebuilt HTML from updated markdown"
  key_links:
    - from: "tutorial/14-gadt.md poly-eval.l3 example"
      to: "actual runtime behavior"
      via: "code that compiles and runs after plan 25-04 fix"
      pattern: "BoolLit b -> b"
---

<objective>
Update tutorial/14-gadt.md so the polymorphic return examples accurately document the
capability delivered by plans 25-01 and 25-04. Specifically:
- The "주석 없음" section and "다형적 반환 타입" section both showed
  `let eval e = match e with | IntLit n -> n | BoolLit b -> b` as working — which was
  false before plan 25-04. After plan 25-04 this code does work, so the examples become
  accurate. Update surrounding prose to match the now-correct behavior and rebuild HTML.

Purpose: Close COV-04 gap. Tutorial must document what LangThree actually does.

Output: tutorial/14-gadt.md with accurate examples, docs/14-gadt.html rebuilt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-polymorphic-gadt-return-types/25-04-SUMMARY.md
@tutorial/14-gadt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct tutorial/14-gadt.md polymorphic return sections</name>
  <files>tutorial/14-gadt.md</files>
  <action>
Read the full tutorial/14-gadt.md. The inaccuracies are in two places:

**Inaccuracy 1 — "주석 없음 (권장 — 다형적 반환)" section (around lines 151-182):**

Currently:
- Line 151: "주석 없음 (권장 — 다형적 반환)" header implies two-branch cross-type works without annotation
- Lines 154-161: Code block shows `let eval e = match e with | IntLit n -> n | BoolLit b -> b` with `r1`, `r2` calls
- Line 180: Claims "LangThree는 주석 없이도 다형적 반환이 가능하며, 명시적 주석은 반환 타입을 하나로 고정하고 싶을 때 사용합니다"

After plan 25-04 this is now accurate — keep the code example as-is (it works now).
However, verify the surrounding prose is consistent. If any sentence says this "is not
yet supported" or "future work", remove that caveat. The prose should match the working
state.

**Inaccuracy 2 — "다형적 반환 타입" section (around lines 208-228):**

Currently (lines 212-228):
```
$ cat poly-eval.l3
type Expr 'a = IntLit : int -> int Expr | BoolLit : bool -> bool Expr

let eval e = match e with | IntLit n -> n | BoolLit b -> b

let r1 = eval (IntLit 42)
let r2 = eval (BoolLit true)

$ langthree poly-eval.l3
42
true
```

After plan 25-04 this code now works correctly, so the example is accurate.
Verify line 226 states `eval`의 타입은 `'a Expr -> 'a` — keep this (it's now true).
Remove any disclaimers that say this fails or is unsupported.

**Check for stale disclaimers anywhere in the file:**
Search for text like "미구현", "구현되지 않음", "future work", "불가", "실패", "E0301"
in the context of the polymorphic return feature. If any such disclaimers exist in the
polymorphic return sections (lines 147-228), remove or correct them. Disclaimers in
other sections (e.g., GADT limitations section if any) may be appropriate and should
be left alone.

**Annotation guidance correction (lines 147-183 area):**
The three-way annotation breakdown is:
1. "주석 없음" → polymorphic return (cross-type, now works) — KEEP
2. "타입 변수 주석 'a" → explicit polymorphism — KEEP
3. "구체적 타입 주석 ': int'" → single type forced — KEEP

Ensure the framing of option 1 is accurate: it should say annotations are optional
and the compiler infers polymorphic return per-branch. No disclaimers needed since
plan 25-04 makes this work.

Make only the minimal changes needed: correct inaccurate sentences, keep the rest as-is.
  </action>
  <verify>
  After editing, verify the two example code blocks compile:
    # Test poly-eval example from "다형적 반환 타입" section
    cat > /tmp/poly-eval-check.l3 << 'EOF'
  type Expr 'a = IntLit : int -> int Expr | BoolLit : bool -> bool Expr
  let eval e = match e with | IntLit n -> n | BoolLit b -> b
  let r1 = eval (IntLit 42)
  let r2 = eval (BoolLit true)
  EOF
    ./src/LangThree/bin/Release/net10.0/LangThree /tmp/poly-eval-check.l3
  Expected output: 42 then true (no E0301).

  Search the file for "E0301" or "미구현" to confirm no stale disclaimers remain
  in the polymorphic return sections.
  </verify>
  <done>
  - Tutorial shows `let eval e = match e with | IntLit n -> n | BoolLit b -> b` as working (accurate after plan 25-04)
  - No sentences claiming this pattern fails or is unsupported
  - The "주석 없음" section and "다형적 반환 타입" section both present accurate, working examples
  - Prose claims like `eval : 'a Expr -> 'a` are accurate
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild docs/14-gadt.html from updated markdown</name>
  <files>docs/14-gadt.html</files>
  <action>
Determine how the HTML docs are built. Check:
  cat tutorial/book.toml     # mdBook config
  ls docs/                   # Check if it's mdBook output or a simple script
  which mdbook               # Check if mdbook is installed

If mdBook is available and configured:
  cd /Users/ohama/vibe-coding/LangThree && mdbook build tutorial/ --dest-dir ../docs

If a build script exists (e.g., Makefile, build-docs.sh):
  cat Makefile | grep -i docs
  ./build-docs.sh

If neither is available, generate a minimal HTML update by wrapping the markdown in
a pre tag or using pandoc:
  pandoc tutorial/14-gadt.md -o docs/14-gadt.html --standalone 2>/dev/null || \
  echo "Manual HTML regeneration needed — mdbook not installed"

If HTML cannot be regenerated automatically (no mdbook/pandoc), create a note in the
plan summary that HTML needs manual rebuild. Do NOT block plan completion on this.
The markdown is authoritative; HTML is a derived artifact.

After rebuilding (or attempting), verify docs/14-gadt.html was modified more recently
than before:
  ls -la docs/14-gadt.html
  </action>
  <verify>
  If HTML was regenerated: `grep -c "BoolLit b -> b" docs/14-gadt.html` returns > 0
  (the updated content is reflected in the HTML).
  If HTML could not be regenerated: note in summary that mdbook is required.
  </verify>
  <done>
  - docs/14-gadt.html updated (or build method documented if tooling unavailable)
  - HTML reflects the corrected tutorial content
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. The two example code blocks in tutorial sections compile and produce correct output
2. `grep -n "E0301\|미구현\|future work\|불가" tutorial/14-gadt.md` returns no hits in lines 147-230
3. docs/14-gadt.html modified timestamp is after tutorial/14-gadt.md (or build method noted)
4. Full test suite still passes: `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj 2>&1 | tail -5`
</verification>

<success_criteria>
- COV-04 satisfied: tutorial accurately documents the cross-type polymorphic return capability
- Tutorial poly-eval.l3 example compiles and runs with output "42\ntrue"
- No inaccurate disclaimers remain in the polymorphic return sections
- HTML docs updated
</success_criteria>

<output>
After completion, create `.planning/phases/25-polymorphic-gadt-return-types/25-05-SUMMARY.md`
</output>
