---
phase: 64-declaration-type-annotations
plan: 03
type: execute
wave: 3
depends_on: ["64-01", "64-02"]
files_modified:
  - tests/flt/file/let/let-annot-param-mixed.flt
  - tests/flt/file/let/let-annot-return-type.flt
  - tests/flt/file/let/let-annot-param-return.flt
  - tests/flt/file/let/let-annot-mutual-rec.flt
  - tests/flt/file/let/let-annot-generic-type.flt
autonomous: true

must_haves:
  truths:
    - "Mixed annotated+plain params work in let declarations (PARAM-01)"
    - "Annotated params work in let rec ... and ... mutual recursion (PARAM-02)"
    - "Return type annotation works in let declarations (RET-01)"
    - "Param annotation + return type combined work (RET-02)"
    - "Type annotations are erased at runtime — results match non-annotated equivalents"
  artifacts:
    - path: "tests/flt/file/let/let-annot-param-mixed.flt"
      provides: "PARAM-01 integration test"
    - path: "tests/flt/file/let/let-annot-return-type.flt"
      provides: "RET-01 integration test"
    - path: "tests/flt/file/let/let-annot-param-return.flt"
      provides: "RET-02 integration test"
    - path: "tests/flt/file/let/let-annot-mutual-rec.flt"
      provides: "PARAM-02 integration test"
    - path: "tests/flt/file/let/let-annot-generic-type.flt"
      provides: "Generic type annotation integration test"
  key_links:
    - from: "flt test files"
      to: "LangThree binary"
      via: "FsLit runner executing tests"
      pattern: "FsLit tests/flt/file/let/"
---

<objective>
Add comprehensive flt integration tests covering all four requirements (PARAM-01, PARAM-02, RET-01, RET-02) and the five success criteria from the roadmap.

Purpose: Verify that declaration type annotations parse and execute correctly across all let-forms.
Output: Five flt test files in tests/flt/file/let/ covering mixed params, return types, combined annotations, mutual recursion, and generic types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-declaration-type-annotations/64-RESEARCH.md
@.planning/phases/64-declaration-type-annotations/64-01-SUMMARY.md
@.planning/phases/64-declaration-type-annotations/64-02-SUMMARY.md
@tests/flt/file/let/let-rec-multiparam.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flt tests for PARAM-01 and PARAM-02 (annotated params)</name>
  <files>tests/flt/file/let/let-annot-param-mixed.flt, tests/flt/file/let/let-annot-mutual-rec.flt</files>
  <action>
Create two flt test files. Use the exact FsLit format from existing tests (see let-rec-multiparam.flt for reference). The Command line must use the full path: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`

**File 1: `tests/flt/file/let/let-annot-param-mixed.flt`** (PARAM-01)
Tests:
- `let f (x : int) y (z : bool) = if z then x else y` with `f 10 20 true` -> `10`
- `let g (a : string) (b : string) = a ^^ b` with `g "hello" " world"` -> `hello world`
- `let h x (y : int) = x + y` with `h 3 4` -> `7` (plain first, annotated second)
- All-annotated: `let k (x : int) (y : int) = x * y` with `k 6 7` -> `42`

Each test case is a separate `// --- Input:` / `// --- Output:` block within the same file.

**File 2: `tests/flt/file/let/let-annot-mutual-rec.flt`** (PARAM-02)
Tests:
- `let rec isEven (n : int) = if n = 0 then true else isOdd (n - 1) and isOdd (n : int) = if n = 0 then false else isEven (n - 1)` with `isEven 4` -> `true`
- Mixed: `let rec f (x : int) = if x <= 0 then 0 else x + g (x - 1) and g y = f (y - 1)` with `f 3` -> `3` (first binding annotated, second plain)
  </action>
  <verify>Run `../fslit/dist/FsLit tests/flt/file/let/let-annot-param-mixed.flt` and `../fslit/dist/FsLit tests/flt/file/let/let-annot-mutual-rec.flt` — all test cases pass.</verify>
  <done>PARAM-01 and PARAM-02 have passing flt integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create flt tests for RET-01, RET-02, and generic type annotations</name>
  <files>tests/flt/file/let/let-annot-return-type.flt, tests/flt/file/let/let-annot-param-return.flt, tests/flt/file/let/let-annot-generic-type.flt</files>
  <action>
Create three flt test files.

**File 1: `tests/flt/file/let/let-annot-return-type.flt`** (RET-01)
Tests:
- `let add x : int = x + 1` with `add 41` -> `42`
- `let f x y : int = x + y` with `f 10 20` -> `30` (multi-param + return type)
- `let x : int = 42` with `x` -> `42` (no params, return type only)

**File 2: `tests/flt/file/let/let-annot-param-return.flt`** (RET-02)
Tests:
- `let isPos (x : int) : bool = x > 0` with `isPos 5` -> `true` and `isPos (-3)` -> `false`
- `let f (x : int) y (z : bool) : int = if z then x else y` with `f 10 20 true` -> `10`
- `let rec fact (n : int) : int = if n <= 1 then 1 else n * fact (n - 1)` with `fact 5` -> `120` (let rec with both param and return type)

**File 3: `tests/flt/file/let/let-annot-generic-type.flt`** (Phase 63 integration)
Tests:
- `let id (x : 'a) : 'a = x` with `id 42` -> `42` and `id "hello"` -> `hello` (generic type vars in annotations)
- Uses angle bracket generics if applicable: `let first (pair : 'a * 'b) : 'a = match pair with (a, _) -> a` with `first (1, "two")` -> `1`

NOTE: For each test block, every input section should end with a `let _ = println <result-expr>` line, since flt tests capture stdout. The output must include the final `()` that println produces. Actually, check the existing test format — if `let result = expr` at top level prints the result without println, follow that convention instead. (Looking at let-rec-multiparam.flt: it uses `let result = add 3 4` and expects `7` — so top-level let binding prints the value. Use this pattern.)
  </action>
  <verify>Run `../fslit/dist/FsLit tests/flt/file/let/let-annot-return-type.flt`, `../fslit/dist/FsLit tests/flt/file/let/let-annot-param-return.flt`, and `../fslit/dist/FsLit tests/flt/file/let/let-annot-generic-type.flt` — all pass. Then run `../fslit/dist/FsLit tests/flt/file/let/` to verify all let tests pass together.</verify>
  <done>RET-01, RET-02, and generic type annotation tests pass. Full let test directory passes.</done>
</task>

</tasks>

<verification>
1. `../fslit/dist/FsLit tests/flt/file/let/` — all tests pass (both new and existing)
2. `../fslit/dist/FsLit tests/flt/` — full suite passes (no regressions anywhere)
3. All five roadmap success criteria are covered:
   - SC1: let-annot-param-mixed.flt
   - SC2: let-annot-return-type.flt
   - SC3: let-annot-param-return.flt
   - SC4: let-annot-mutual-rec.flt
   - SC5: Erasure verified by runtime producing correct values (no type errors)
</verification>

<success_criteria>
- All five flt test files exist and pass
- Tests cover all four requirements (PARAM-01, PARAM-02, RET-01, RET-02)
- Tests cover all five roadmap success criteria
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/64-declaration-type-annotations/64-03-SUMMARY.md`
</output>
