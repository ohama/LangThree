---
phase: 63-angle-bracket-generics
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - tests/flt/file/adt/adt-angle-bracket.flt
  - tests/flt/file/adt/adt-angle-bracket-multiarg.flt
  - tests/flt/file/alias/alias-angle-bracket.flt
  - tests/flt/file/function/lambda-annot-angle-bracket.flt
autonomous: true

must_haves:
  truths:
    - "ADT with angle bracket type params declares and pattern-matches correctly"
    - "Multi-arg generic type Map<string, int> parses in type expressions"
    - "Type alias with angle bracket params works end-to-end"
    - "Lambda annotation with angle bracket type parses and executes"
    - "Mixed postfix and angle bracket syntax composes correctly"
    - "Existing postfix syntax tests still pass (GEN-03)"
  artifacts:
    - path: "tests/flt/file/adt/adt-angle-bracket.flt"
      provides: "GEN-01 test: angle bracket ADT declaration"
    - path: "tests/flt/file/adt/adt-angle-bracket-multiarg.flt"
      provides: "GEN-01 test: multi-arg angle bracket ADT"
    - path: "tests/flt/file/alias/alias-angle-bracket.flt"
      provides: "GEN-01 bonus: angle bracket type alias"
    - path: "tests/flt/file/function/lambda-annot-angle-bracket.flt"
      provides: "GEN-02 test: angle bracket type in lambda annotation"
  key_links:
    - from: "adt-angle-bracket.flt"
      to: "Parser.fsy TypeDeclaration angle bracket rule"
      via: "flt test exercises the new grammar rule"
      pattern: "type.*<.*>.*="
---

<objective>
Add flt integration tests covering all angle bracket generic syntax scenarios.

Purpose: Verify GEN-01 (type declarations), GEN-02 (type expressions in annotations), and GEN-03 (postfix backward compatibility) with end-to-end tests that parse, type-check, and evaluate.

Output: 4 new flt test files exercising angle bracket generics in ADTs, aliases, lambda annotations, and mixed syntax.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-angle-bracket-generics/63-01-SUMMARY.md
@tests/flt/file/adt/adt-parametric.flt
@tests/flt/file/alias/alias-simple.flt
@tests/flt/emit/ast-expr/ast-expr-lambda-annot.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ADT and alias angle bracket flt tests</name>
  <files>
    tests/flt/file/adt/adt-angle-bracket.flt
    tests/flt/file/adt/adt-angle-bracket-multiarg.flt
    tests/flt/file/alias/alias-angle-bracket.flt
  </files>
  <action>
Create `tests/flt/file/adt/adt-angle-bracket.flt` — GEN-01 basic test:
- Declare `type Result<'a> = Ok of 'a | Error of string` using angle bracket syntax
- Pattern match on Ok and Error constructors
- Verify output shows the extracted value
- Also test mixed syntax: declare a function that returns `Result<'a>` and pattern-matches a `Result<'a> list` (angle bracket + postfix composition)

Create `tests/flt/file/adt/adt-angle-bracket-multiarg.flt` — GEN-01 multi-arg test:
- Declare `type Either<'a, 'b> = Left of 'a | Right of 'b` with two type params
- Pattern match on both constructors
- Verify correct values extracted

Create `tests/flt/file/alias/alias-angle-bracket.flt` — type alias with angle bracket params:
- Declare `type IntList<'a> = 'a list` or similar alias using angle bracket syntax
- Use the alias in a value expression to confirm it parses and evaluates

Follow the existing flt test format exactly:
```
// Test: description
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
(code here)
// --- Output:
(expected output)
```

IMPORTANT: The flt runner strips trailing newlines from input. Ensure the last line of input is a complete expression. Use `let _ = println ...` for print output (include `()` in expected output for the unit return).
  </action>
  <verify>
```bash
../fslit/dist/FsLit tests/flt/file/adt/adt-angle-bracket.flt
../fslit/dist/FsLit tests/flt/file/adt/adt-angle-bracket-multiarg.flt
../fslit/dist/FsLit tests/flt/file/alias/alias-angle-bracket.flt
```
All three pass.
  </verify>
  <done>Three flt test files created and passing, covering GEN-01 (single-arg ADT, multi-arg ADT, alias with angle brackets).</done>
</task>

<task type="auto">
  <name>Task 2: Create lambda annotation and mixed-syntax flt tests, run full regression</name>
  <files>
    tests/flt/file/function/lambda-annot-angle-bracket.flt
  </files>
  <action>
Create `tests/flt/file/function/lambda-annot-angle-bracket.flt` — GEN-02 test:
- Declare an ADT with angle bracket syntax: `type Box<'a> = Box of 'a`
- Write a lambda with angle bracket type annotation: `fun (x : Box<int>) -> match x with Box v -> v`
- Apply it and verify the output
- Also test nested generic in annotation: `fun (x : Box<int> list) -> ...` (mixed angle bracket + postfix)

Follow the exact flt test format (// Test:, // --- Command:, // --- Input:, // --- Output:).

After creating the test file, run the full flt regression suite to confirm GEN-03 (backward compatibility):
```bash
../fslit/dist/FsLit tests/flt/
```
All tests including existing postfix type tests must pass.
  </action>
  <verify>
```bash
../fslit/dist/FsLit tests/flt/file/function/lambda-annot-angle-bracket.flt
../fslit/dist/FsLit tests/flt/
```
New test passes. Full suite passes with zero regressions.
  </verify>
  <done>Lambda annotation angle bracket test passes. Full flt regression suite passes, confirming GEN-03 backward compatibility.</done>
</task>

</tasks>

<verification>
1. All 4 new flt tests pass individually
2. `../fslit/dist/FsLit tests/flt/` -- full regression suite passes with zero failures
3. GEN-01 covered: adt-angle-bracket.flt, adt-angle-bracket-multiarg.flt
4. GEN-02 covered: lambda-annot-angle-bracket.flt
5. GEN-03 covered: full regression (all existing postfix tests unchanged)
</verification>

<success_criteria>
- 4 new flt test files exist and pass
- Full flt test suite passes with zero regressions
- All three requirements (GEN-01, GEN-02, GEN-03) have test coverage
- Mixed angle bracket + postfix syntax tested (Result<'a> list)
</success_criteria>

<output>
After completion, create `.planning/phases/63-angle-bracket-generics/63-02-SUMMARY.md`
</output>
