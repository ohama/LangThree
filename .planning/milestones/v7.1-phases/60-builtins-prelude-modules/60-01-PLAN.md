---
phase: 60-builtins-prelude-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LangThree/Eval.fs
  - src/LangThree/TypeCheck.fs
autonomous: true

must_haves:
  truths:
    - "string_endswith, string_startswith, string_trim builtins exist and work at runtime"
    - "hashtable_trygetvalue returns (bool * value) tuple, hashtable_count returns int"
    - "All 5 new builtins have type schemes so .fun files can reference them without type errors"
    - "Existing builtins and tests are unaffected"
  artifacts:
    - path: "src/LangThree/Eval.fs"
      provides: "5 new builtin entries in initialBuiltinEnv"
      contains: "string_endswith|string_startswith|string_trim|hashtable_trygetvalue|hashtable_count"
    - path: "src/LangThree/TypeCheck.fs"
      provides: "5 new type schemes in initialTypeEnv"
      contains: "string_endswith|string_startswith|string_trim|hashtable_trygetvalue|hashtable_count"
  key_links:
    - from: "src/LangThree/Eval.fs"
      to: "src/LangThree/TypeCheck.fs"
      via: "builtin name must match exactly between initialBuiltinEnv and initialTypeEnv"
      pattern: "string_endswith|hashtable_trygetvalue"
---

<objective>
Add 5 new builtin functions to the LangThree interpreter (BLT-01 through BLT-05) with matching type schemes.

Purpose: These builtins are the runtime foundation that Prelude module wrappers (Plan 02) will call. Without them, String.endsWith, Hashtable.tryGetValue etc. cannot exist.
Output: Eval.fs and TypeCheck.fs updated with 5 new entries each. Project builds and existing tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-builtins-prelude-modules/60-RESEARCH.md
@src/LangThree/Eval.fs
@src/LangThree/TypeCheck.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 builtin functions to Eval.fs</name>
  <files>src/LangThree/Eval.fs</files>
  <action>
Add 3 string builtins after the `string_contains` entry in `initialBuiltinEnv`, before `to_string`:

1. `string_endswith` (BLT-01): Takes StringValue s, returns BuiltinValue taking StringValue suffix, returns BoolValue (s.EndsWith(suffix)). Fail with descriptive error on non-string args.

2. `string_startswith` (BLT-02): Same pattern as endswith but uses s.StartsWith(prefix).

3. `string_trim` (BLT-03): Takes single StringValue s, returns StringValue (s.Trim()). Single-arg builtin (not curried).

Add 2 hashtable builtins after the `hashtable_remove` entry, before `// Phase 55: StringBuilder builtins`:

4. `hashtable_trygetvalue` (BLT-04): Takes BuiltinValue (fun htVal -> BuiltinValue (fun keyVal -> ...)). Match htVal as HashtableValue ht. Call ht.TryGetValue(keyVal): on (true, v) return TupleValue [BoolValue true; v], on (false, _) return TupleValue [BoolValue false; TupleValue []].

5. `hashtable_count` (BLT-05): Takes HashtableValue ht, returns IntValue ht.Count.

Use exact code patterns from 60-RESEARCH.md "Code Examples" section. Include Phase 60 comments for each entry.
  </action>
  <verify>
`dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds.
`dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` passes (no regressions).
  </verify>
  <done>All 5 builtin entries exist in initialBuiltinEnv. Build succeeds. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add 5 type schemes to TypeCheck.fs</name>
  <files>src/LangThree/TypeCheck.fs</files>
  <action>
Add 3 string type schemes after the `string_contains` entry in `initialTypeEnv`:

1. `string_endswith`: Scheme([], TArrow(TString, TArrow(TString, TBool)))
2. `string_startswith`: Scheme([], TArrow(TString, TArrow(TString, TBool)))
3. `string_trim`: Scheme([], TArrow(TString, TString))

Add 2 hashtable type schemes after the `hashtable_remove` entry:

4. `hashtable_trygetvalue`: Scheme([0; 1], TArrow(THashtable (TVar 0, TVar 1), TArrow(TVar 0, TTuple [TBool; TVar 1])))
   CRITICAL: Both type vars 0 and 1 must be in the quantifier list. The return type TTuple [TBool; TVar 1] uses TVar 1 for the value type.

5. `hashtable_count`: Scheme([0; 1], TArrow(THashtable (TVar 0, TVar 1), TInt))

Include Phase 60 comments. Use exact schemes from 60-RESEARCH.md.
  </action>
  <verify>
`dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds.
`dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` passes.
  </verify>
  <done>All 5 type schemes exist in initialTypeEnv matching their Eval.fs counterparts exactly. Build and tests pass.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/LangThree/LangThree.fsproj -c Release` -- clean build
2. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` -- all existing tests pass
3. Grep Eval.fs for all 5 builtin names -- all present
4. Grep TypeCheck.fs for all 5 builtin names -- all present
</verification>

<success_criteria>
- Eval.fs contains string_endswith, string_startswith, string_trim, hashtable_trygetvalue, hashtable_count in initialBuiltinEnv
- TypeCheck.fs contains matching type schemes for all 5 in initialTypeEnv
- Project builds cleanly with no warnings related to new code
- All existing F# unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/60-builtins-prelude-modules/60-01-SUMMARY.md`
</output>
