---
phase: 60-builtins-prelude-modules
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - Prelude/String.fun
  - Prelude/Hashtable.fun
  - Prelude/StringBuilder.fun
  - tests/flt/file/string/string-module-endswith-startswith-trim.flt
  - tests/flt/file/string/string-module-length-contains.flt
  - tests/flt/file/hashtable/hashtable-module-trygetvalue-count.flt
  - tests/flt/file/string/stringbuilder-module-add.flt
autonomous: true

must_haves:
  truths:
    - "String.endsWith, String.startsWith, String.trim work via module-qualified calls"
    - "String.length and String.contains expose existing builtins via module"
    - "Hashtable.tryGetValue returns (true, value) or (false, ()) tuple via module call"
    - "Hashtable.count returns entry count via module call"
    - "StringBuilder.add replaces StringBuilder.append (renamed to avoid List.append conflict)"
    - "All flt integration tests pass for new module functions"
  artifacts:
    - path: "Prelude/String.fun"
      provides: "String module with endsWith, startsWith, trim, length, contains"
      contains: "endsWith|startsWith|trim|length|contains"
    - path: "Prelude/Hashtable.fun"
      provides: "Hashtable module with tryGetValue, count"
      contains: "tryGetValue|count"
    - path: "Prelude/StringBuilder.fun"
      provides: "StringBuilder module with add (not append)"
      contains: "let add sb s"
    - path: "tests/flt/file/string/string-module-endswith-startswith-trim.flt"
      provides: "flt test for BLT-01, BLT-02, BLT-03, MOD-01"
    - path: "tests/flt/file/hashtable/hashtable-module-trygetvalue-count.flt"
      provides: "flt test for BLT-04, BLT-05, MOD-02"
    - path: "tests/flt/file/string/stringbuilder-module-add.flt"
      provides: "flt test for MOD-03"
  key_links:
    - from: "Prelude/String.fun"
      to: "src/LangThree/Eval.fs"
      via: "string_endswith, string_startswith, string_trim builtin calls"
      pattern: "string_endswith|string_startswith|string_trim"
    - from: "Prelude/Hashtable.fun"
      to: "src/LangThree/Eval.fs"
      via: "hashtable_trygetvalue, hashtable_count builtin calls"
      pattern: "hashtable_trygetvalue|hashtable_count"
    - from: "Prelude/StringBuilder.fun"
      to: "src/LangThree/Eval.fs"
      via: "stringbuilder_append builtin (renamed wrapper only)"
      pattern: "stringbuilder_append"
---

<objective>
Update 3 Prelude .fun modules to expose new builtins as module functions, and create flt integration tests proving all requirements work end-to-end.

Purpose: Module-qualified functions (String.endsWith, Hashtable.tryGetValue, StringBuilder.add) are the user-facing API that replaces dot notation. Tests validate the full stack from module call through builtin to .NET runtime.
Output: 3 updated .fun files, 4 new flt test files, all passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-builtins-prelude-modules/60-RESEARCH.md
@.planning/phases/60-builtins-prelude-modules/60-01-SUMMARY.md
@Prelude/String.fun
@Prelude/Hashtable.fun
@Prelude/StringBuilder.fun
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 3 Prelude .fun module files</name>
  <files>Prelude/String.fun, Prelude/Hashtable.fun, Prelude/StringBuilder.fun</files>
  <action>
**String.fun (MOD-01):** Replace entire file with:
```
module String =
    let concat sep lst = string_concat_list sep lst
    let endsWith s suffix = string_endswith s suffix
    let startsWith s prefix = string_startswith s prefix
    let trim s = string_trim s
    let length s = string_length s
    let contains s needle = string_contains s needle
```
CRITICAL: No blank lines inside the module body. No `open String` at the end. Each function is a one-liner wrapper calling the corresponding builtin.

**Hashtable.fun (MOD-02):** Add two new lines at the end of the existing module body:
```
    let tryGetValue ht key  = hashtable_trygetvalue ht key
    let count ht            = hashtable_count ht
```
Keep all existing entries (create, get, set, containsKey, keys, remove). No blank lines.

**StringBuilder.fun (MOD-03):** Replace `let append sb s = stringbuilder_append sb s` with `let add sb s = stringbuilder_append sb s`. The underlying builtin name stays `stringbuilder_append` -- only the module function name changes from `append` to `add`.

Final StringBuilder.fun content:
```
module StringBuilder =
    let create () = stringbuilder_create ()
    let add sb s  = stringbuilder_append sb s
    let toString sb = stringbuilder_tostring sb
```
  </action>
  <verify>
`dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds (Prelude files are loaded at runtime, but build must still pass).
Run a quick smoke test: `echo 'let _ = println (String.trim "  hi  ")' | src/LangThree/bin/Release/net10.0/LangThree /dev/stdin` should print `hi` then `()`.
  </verify>
  <done>String.fun has 6 functions (concat, endsWith, startsWith, trim, length, contains). Hashtable.fun has 8 functions (existing 6 + tryGetValue, count). StringBuilder.fun uses `add` not `append`.</done>
</task>

<task type="auto">
  <name>Task 2: Create flt integration tests for all new module functions</name>
  <files>
    tests/flt/file/string/string-module-endswith-startswith-trim.flt
    tests/flt/file/string/string-module-length-contains.flt
    tests/flt/file/hashtable/hashtable-module-trygetvalue-count.flt
    tests/flt/file/string/stringbuilder-module-add.flt
  </files>
  <action>
Create 4 flt test files. All must use `// --- Output:` (NOT `// --- Stdout:`). All must include `()` as the last line of expected output when the final statement is `let _ = println ...`. Use the Command line: `// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`

**Test 1: string-module-endswith-startswith-trim.flt** (BLT-01, BLT-02, BLT-03, MOD-01)
Tests:
- String.endsWith "hello.txt" ".txt" -> true
- String.endsWith "hello.txt" ".doc" -> false
- String.startsWith "hello" "he" -> true
- String.startsWith "hello" "wo" -> false
- String.trim "  hi  " -> "hi"
Expected output: true, false, true, false, hi, () (each on own line)

**Test 2: string-module-length-contains.flt** (MOD-01, existing builtins)
Tests:
- String.length "abc" -> 3
- String.length "" -> 0
- String.contains "hello" "ell" -> true
- String.contains "hello" "xyz" -> false
Expected output: 3, 0, true, false, () (each on own line)

**Test 3: hashtable-module-trygetvalue-count.flt** (BLT-04, BLT-05, MOD-02)
Tests:
- Create hashtable, set "a"->1, "b"->2
- Hashtable.tryGetValue ht "a" -> (true, 1)
- Hashtable.tryGetValue ht "z" -> (false, ())
- Hashtable.count ht -> 2
Expected output: (true, 1), (false, ()), 2, () (each on own line)

**Test 4: stringbuilder-module-add.flt** (MOD-03)
Tests:
- Create StringBuilder, add "hello", add " world", toString -> "hello world"
Expected output: hello world, ()

Use `to_string` to convert bool/int/tuple values to string for println. String values print directly via println without to_string.

IMPORTANT flt format rules:
- `// --- Input:` section contains the LangThree code
- `// --- Output:` section contains expected output
- Last line of Input must be a complete parseable declaration (no trailing blank line)
- Use `let _ = println ...` pattern for each output line
  </action>
  <verify>
Run all 4 new flt tests:
```
../fslit/dist/FsLit tests/flt/file/string/string-module-endswith-startswith-trim.flt
../fslit/dist/FsLit tests/flt/file/string/string-module-length-contains.flt
../fslit/dist/FsLit tests/flt/file/hashtable/hashtable-module-trygetvalue-count.flt
../fslit/dist/FsLit tests/flt/file/string/stringbuilder-module-add.flt
```
All 4 must PASS. If any fails, run with `-v` flag to see diff and fix.

Also run the full flt suite to check no regressions: `../fslit/dist/FsLit tests/flt/`
  </verify>
  <done>4 flt test files exist and pass. Full flt suite passes with no regressions. All 8 requirements (BLT-01..05, MOD-01..03) are covered by tests.</done>
</task>

</tasks>

<verification>
1. All 4 new flt tests pass individually
2. Full flt test suite passes: `../fslit/dist/FsLit tests/flt/`
3. Prelude/String.fun has endsWith, startsWith, trim, length, contains functions
4. Prelude/Hashtable.fun has tryGetValue, count functions
5. Prelude/StringBuilder.fun uses `add` not `append`
6. No `open String` or `open Hashtable` or `open StringBuilder` at end of any modified .fun file
</verification>

<success_criteria>
- String.endsWith "hello.txt" ".txt" returns true in flt test
- String.startsWith "hello" "he" returns true in flt test
- String.trim "  hi  " returns "hi" in flt test
- String.length "abc" returns 3 in flt test
- String.contains "hello" "ell" returns true in flt test
- Hashtable.tryGetValue returns (true, 1) for existing key and (false, ()) for missing key in flt test
- Hashtable.count returns correct count in flt test
- StringBuilder.add works (append renamed) in flt test
- Full flt suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/60-builtins-prelude-modules/60-02-SUMMARY.md`
</output>
