# Milestone v2.1: Bug Fixes & Test Hardening

**Status:** ✅ SHIPPED 2026-03-25
**Phases:** 33-35
**Total Plans:** 5

## Overview

Fix runtime regressions from v2.0 and fill test coverage gaps for Phases 29-32. TCO regression fixed, test isolation improved, 21 new flt tests added, compile warnings eliminated.

## Phases

### Phase 33: TCO Fix + Test Isolation

**Goal**: The interpreter correctly tail-call-optimizes recursive functions and the test suite runs deterministically
**Depends on**: Phase 32 (v2.0 complete)
**Plans**: 2 plans

Plans:
- [x] 33-01: Fix TCO in LetRec/LetRecDecl BuiltinValue wrappers (tailPos false→true)
- [x] 33-02: Fix MatchCompile global counter: make freshTestVar local to compileMatch

**Details:**
- Eval.fs lines 777, 1039: `false` → `true` in BuiltinValue wrapper eval calls
- MatchCompile.fs: eliminated module-level `nextTestVar`, `freshTestVar`, `resetTestVarCounter`
- `compile` function accepts `freshTestVar: unit -> TestVar` as parameter
- 214/214 tests pass deterministically on consecutive runs

### Phase 34: Test Coverage

**Goal**: Every feature shipped in Phases 29-32 has flt regression tests that execute and pass
**Depends on**: Phase 33
**Plans**: 2 plans

Plans:
- [x] 34-01: Add flt tests for char (Phase 29) and parser improvements (Phase 30)
- [x] 34-02: Add flt tests for file import (Phase 31) and file I/O + system builtins (Phase 32)

**Details:**
- 21 new flt tests: 6 char + 5 parser + 2 import + 8 fileio
- Total flt tests: 447 → 468, all passing
- File import tests use bash -c wrapper (open processed at type-check time)
- get_args and eprint skipped (not feasible in flt framework)

### Phase 35: Compile Warning Cleanup

**Goal**: `dotnet build` and `dotnet test` produce zero warnings
**Depends on**: Phase 34
**Plans**: 1 plan

Plans:
- [x] 35-01: Fix all FS0025 incomplete pattern match warnings in IntegrationTests.fs and RecordTests.fs

**Details:**
- 10 incomplete pattern match sites fixed (9 IntegrationTests.fs + 1 RecordTests.fs)
- Added wildcard catch-all arms for NamedModule/NamespacedModule/EmptyModule
- 0 FS warnings in build output

---

## Milestone Summary

**Key Decisions:**
- TCO fix: use tailPos=true inside BuiltinValue closure (not modify BuiltinValue signature)
- MatchCompile: local counter per compileMatch invocation (not testSequenced wrapper)
- File import flt: bash -c wrapper pre-creates dependency files before binary runs
- Warning fix: wildcard failtest arms (not explicit all-case coverage)

**Issues Resolved:**
- TCO regression from Phase 30 LetRec→BuiltinValue conversion
- Non-deterministic test failure from MatchCompile global counter race
- 20 FS0025 compile warnings in test files

**Issues Deferred:**
- MSBuild SDK GenerateProgramFile warning (not an FS compiler warning)
- List.length broken in flt programs (E0313); prelude length used as workaround
- Qualified module access from imported files fails (E0313)

**Technical Debt Incurred:**
- get_args and eprint not testable in flt framework (unit-tested only)

---

_For current project status, see .planning/ROADMAP.md_
