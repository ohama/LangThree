# Milestone v1.7: F#-Style List Syntax

**Status:** SHIPPED 2026-03-22
**Phases:** 23-24
**Total Plans:** 4

## Overview

v1.6 offside rule로 implicit `in` 지원을 추가하고, v1.7에서 리스트 구분자를 콤마에서 세미콜론으로 변경하여 F# 문법 호환성을 완성.

## Phases

### Phase 23: IndentFilter Offside Rule Refactoring (COMPLETE)

**Goal:** F# LexFilter 방식의 CtxtLetDecl + offside column 기반 implicit `in` 구현
**Depends on:** v1.5 (complete)
**Requirements:** CTX-01~03, TOK-01~03, COV-01~06
**Plans:** 1 plan

Plans:
- [x] 23-01: F#-Style Offside Rule (context stack, offside IN, 5 tests)

**Details:**
- IndentFilter에 InLetDecl(blockLet, offsideCol), InExprBlock, InModule 컨텍스트 추가
- LetSeqDepth counter를 context stack으로 교체
- offside column 도달 시 IN 토큰 삽입
- 모듈 컨텍스트에서는 blockLet=false → IN 삽입하지 않음
- 635 tests passing (196 F# + 439 fslit)

### Phase 24: List Separator Semicolon (COMPLETE)

**Goal:** 리스트 구분자를 콤마에서 세미콜론으로 변경 [1, 2, 3] → [1; 2; 3]
**Depends on:** Phase 23
**Plans:** 3 plans

Plans:
- [x] 24-01: Core language changes (Parser.fsy SemiExprList, Format.fs, Eval.fs)
- [x] 24-02: Update test files to semicolon list syntax (439 tests pass)
- [x] 24-03: Update tutorial files to semicolon list syntax (16 .md files)

**Details:**
- Parser: SemiExprList rule for [e1; e2; e3], ExprList (COMMA) kept for tuples only
- Format.fs/Eval.fs: List output uses "; " separator, Tuple stays ", "
- 127 files modified, all tests passing

---

## Milestone Summary

**Key Decisions:**
- InLetDecl(blockLet, offsideCol) replaces LetSeqDepth counter
- SemiExprList is separate from ExprList; lists use SEMICOLON, tuples keep COMMA
- SEMICOLON token reused from record syntax (no lexer changes)

**Issues Resolved:**
- Single-line match/try as let RHS blocking offside rule
- Expression-level multi-param let + relaxed offside InMatch block

**Issues Deferred:**
- Expression-level `let rec ... and ...` (saved as TODO)
- Computation expressions (future milestone)
- do binding (future milestone)

**Technical Debt Incurred:**
- None

---
_For current project status, see .planning/ROADMAP.md_
