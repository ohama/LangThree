---
phase: 56-hashset-queue
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/LangThree/Prelude/HashSet.fun
  - src/LangThree/Prelude/Queue.fun
  - tests/flt/file/hashset/hashset-basic.flt
  - tests/flt/file/hashset/hashset-strings.flt
  - tests/flt/file/queue/queue-basic.flt
  - tests/flt/file/queue/queue-error.flt
autonomous: true

must_haves:
  truths:
    - "HashSet.create() returns a new empty HashSet"
    - "HashSet.add hs v returns bool (true=new, false=duplicate)"
    - "HashSet.contains hs v returns bool"
    - "HashSet.count hs returns integer count"
    - "Queue.create() returns a new empty Queue"
    - "Queue.enqueue q v returns unit"
    - "Queue.dequeue q () returns front element"
    - "Queue.count q returns integer count"
    - "All flt tests pass with // --- Output: marker"
    - "Empty dequeue test passes (error is raised)"
  artifacts:
    - path: "src/LangThree/Prelude/HashSet.fun"
      provides: "HashSet module with create/add/contains/count"
      contains: "module HashSet ="
    - path: "src/LangThree/Prelude/Queue.fun"
      provides: "Queue module with create/enqueue/dequeue/count"
      contains: "module Queue ="
    - path: "tests/flt/file/hashset/hashset-basic.flt"
      provides: "COLL-02 integer test coverage"
      contains: "// --- Output:"
    - path: "tests/flt/file/hashset/hashset-strings.flt"
      provides: "COLL-02 string element coverage"
      contains: "// --- Output:"
    - path: "tests/flt/file/queue/queue-basic.flt"
      provides: "COLL-03 integer FIFO test coverage"
      contains: "// --- Output:"
    - path: "tests/flt/file/queue/queue-error.flt"
      provides: "COLL-03 empty dequeue error coverage"
      contains: "// --- Output:"
  key_links:
    - from: "Prelude/HashSet.fun"
      to: "HashSet constructor + method dispatch"
      via: "HashSet () and hs.Add / hs.Contains / hs.Count"
      pattern: "HashSet \\(\\)"
    - from: "Prelude/Queue.fun"
      to: "Queue constructor + method dispatch"
      via: "Queue () and q.Enqueue / q.Dequeue / q.Count"
      pattern: "Queue \\(\\)"
---

<objective>
Add Prelude module wrappers for HashSet and Queue, then write flt integration tests covering integers, strings, and the empty-dequeue error case.

Purpose: Completes Phase 56 by making HashSet and Queue available as functional-style module APIs (HashSet.add, Queue.enqueue, etc.) and verifying all requirements with executable test files.
Output: Prelude/HashSet.fun, Prelude/Queue.fun, four flt test files under tests/flt/file/hashset/ and tests/flt/file/queue/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-hashset-queue/56-RESEARCH.md
@.planning/phases/56-hashset-queue/56-01-SUMMARY.md
@src/LangThree/Prelude/StringBuilder.fun
@tests/flt/file/string/stringbuilder-basic.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prelude/HashSet.fun and Prelude/Queue.fun</name>
  <files>src/LangThree/Prelude/HashSet.fun, src/LangThree/Prelude/Queue.fun</files>
  <action>
Create `src/LangThree/Prelude/HashSet.fun` with the following exact content (no raw builtins — uses constructor + dot dispatch directly):

```
module HashSet =
    let create ()     = HashSet ()
    let add hs v      = hs.Add v
    let contains hs v = hs.Contains v
    let count hs      = hs.Count
```

Create `src/LangThree/Prelude/Queue.fun` with the following exact content:

```
module Queue =
    let create ()      = Queue ()
    let enqueue q v    = q.Enqueue v
    let dequeue q ()   = q.Dequeue ()
    let count q        = q.Count
```

Then verify that the interpreter loads these files at startup. Check how other Prelude files are loaded (look at how StringBuilder.fun or Char.fun is registered) — if Prelude files must be listed in a manifest or loaded explicitly, add the new files to that list. The most common pattern: Prelude files in `src/LangThree/Prelude/` are auto-discovered by glob or listed in Program.fs / a prelude loader.
  </action>
  <verify>dotnet build /Users/ohama/vibe/LangThree/src/LangThree/LangThree.fsproj -c Release 2>&1 | tail -5</verify>
  <done>Build passes. Running `echo 'let hs = HashSet.create ()\nlet _ = println (to_string (HashSet.add hs 1))' | /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree /dev/stdin` prints "true".</done>
</task>

<task type="auto">
  <name>Task 2: Write flt integration tests for HashSet and Queue</name>
  <files>
    tests/flt/file/hashset/hashset-basic.flt,
    tests/flt/file/hashset/hashset-strings.flt,
    tests/flt/file/queue/queue-basic.flt,
    tests/flt/file/queue/queue-error.flt
  </files>
  <action>
Create directory `tests/flt/file/hashset/` and `tests/flt/file/queue/` (mkdir if needed).

**tests/flt/file/hashset/hashset-basic.flt** — integers, COLL-02 core:
```
// Test: HashSet basic usage with integers (COLL-02)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let hs = HashSet ()
let r1 = hs.Add(1)
let r2 = hs.Add(2)
let r3 = hs.Add(1)
let _ = println (to_string r1)
let _ = println (to_string r2)
let _ = println (to_string r3)
let _ = println (to_string (hs.Contains(1)))
let _ = println (to_string (hs.Contains(9)))
let _ = println (to_string hs.Count)
// --- Output:
true
true
false
true
false
2
()
```

**tests/flt/file/hashset/hashset-strings.flt** — string elements:
```
// Test: HashSet with string elements (COLL-02)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let hs = HashSet ()
let _ = hs.Add("apple")
let _ = hs.Add("banana")
let _ = hs.Add("apple")
let _ = println (to_string hs.Count)
let _ = println (to_string (hs.Contains("apple")))
let _ = println (to_string (hs.Contains("cherry")))
// --- Output:
2
true
false
()
```

**tests/flt/file/queue/queue-basic.flt** — integers, COLL-03 core:
```
// Test: Queue basic FIFO usage with integers (COLL-03)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let q = Queue ()
let _ = q.Enqueue(10)
let _ = q.Enqueue(20)
let _ = q.Enqueue(30)
let _ = println (to_string q.Count)
let v1 = q.Dequeue ()
let _ = println (to_string v1)
let v2 = q.Dequeue ()
let _ = println (to_string v2)
let _ = println (to_string q.Count)
// --- Output:
3
10
20
1
()
```

**tests/flt/file/queue/queue-error.flt** — empty dequeue raises catchable exception:
```
// Test: Queue.Dequeue on empty queue raises exception (COLL-03)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let q = Queue ()
let result =
    try
        let _ = q.Dequeue ()
        "no error"
    with
    | e -> "caught: " ^^ e
let _ = println result
// --- Output:
caught: Queue.Dequeue: queue is empty
()
```

IMPORTANT: Use `// --- Output:` (not `// --- Stdout:`). Each test file's last `let _ = println ...` will produce a trailing `()` in the output — include it. Run the tests after writing to confirm.
  </action>
  <verify>/Users/ohama/.local/bin/FsLit /Users/ohama/vibe/LangThree/tests/flt/file/hashset/ && /Users/ohama/.local/bin/FsLit /Users/ohama/vibe/LangThree/tests/flt/file/queue/</verify>
  <done>All four flt tests pass (0 failures). FsLit reports PASS for hashset-basic, hashset-strings, queue-basic, and queue-error.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/LangThree/LangThree.fsproj -c Release` succeeds
2. `/Users/ohama/.local/bin/FsLit tests/flt/file/hashset/` — all tests PASS
3. `/Users/ohama/.local/bin/FsLit tests/flt/file/queue/` — all tests PASS
4. HashSet module API works: `HashSet.add`, `HashSet.contains`, `HashSet.count`
5. Queue module API works: `Queue.enqueue`, `Queue.dequeue`, `Queue.count`
</verification>

<success_criteria>
- Prelude/HashSet.fun and Prelude/Queue.fun loaded at interpreter startup
- HashSet.create/add/contains/count all function correctly via module API
- Queue.create/enqueue/dequeue/count all function correctly via module API
- All 4 flt tests pass (hashset-basic, hashset-strings, queue-basic, queue-error)
- COLL-02 and COLL-03 requirements satisfied
- Phase 56 success criteria met:
  1. HashSet() creates set, Add(v) returns bool, Contains(v) checks membership, Count returns size
  2. Queue() creates queue, Enqueue(v) adds to back, Dequeue() removes from front, Count returns size
  3. flt tests verify both types with integers, strings, and empty dequeue error
</success_criteria>

<output>
After completion, create `.planning/phases/56-hashset-queue/56-02-SUMMARY.md`
</output>
