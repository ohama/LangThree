---
phase: 58-language-constructs
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - tests/flt/file/string/str-slice.flt
  - tests/flt/file/list/list-comprehension.flt
  - tests/flt/file/hashset/hashset-forin.flt
  - tests/flt/file/queue/queue-forin.flt
  - tests/flt/file/mutablelist/mutablelist-forin.flt
  - tests/flt/file/hashtable/hashtable-forin.flt
autonomous: true

must_haves:
  truths:
    - "String slicing flt tests pass: s.[1..3], s.[2..], s.[0..0], s.[len..] edge cases"
    - "List comprehension flt tests pass: over list, over range, over array"
    - "for-in flt tests pass for HashSet, Queue, MutableList"
    - "for-in flt test passes for Hashtable with kv.Key and kv.Value access"
    - "All new flt tests pass via FsLit runner"
    - "Existing flt tests still pass (no regressions)"
  artifacts:
    - path: "tests/flt/file/string/str-slice.flt"
      provides: "String slicing test cases (LANG-01)"
      contains: "s.[1..3]"
    - path: "tests/flt/file/list/list-comprehension.flt"
      provides: "List comprehension test cases (LANG-02)"
      contains: "for x in"
    - path: "tests/flt/file/hashtable/hashtable-forin.flt"
      provides: "Hashtable for-in with .Key/.Value test (LANG-03, PROP-05)"
      contains: "kv.Key"
  key_links:
    - from: "tests/flt/file/string/str-slice.flt"
      to: "src/LangThree/bin/Release/net10.0/LangThree"
      via: "FsLit runner executing interpreter"
      pattern: "// --- Output:"
---

<objective>
Write flt integration tests for all three Phase 58 language constructs: string slicing (LANG-01), list comprehension (LANG-02), and native collection for-in iteration including hashtable .Key/.Value access (LANG-03, PROP-05).

Purpose: Verifies the implementations from Plan 02 work correctly end-to-end through the interpreter.
Output: 6 new flt test files, all passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/58-language-constructs/58-RESEARCH.md
@.planning/phases/58-language-constructs/58-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write string slicing and list comprehension flt tests</name>
  <files>
    tests/flt/file/string/str-slice.flt
    tests/flt/file/list/list-comprehension.flt
  </files>
  <action>
**FsLit test format rules:**
- Header: `// Test: description`
- Command: `// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`
- Input section: `// --- Input:` (actual code lines, NOT commented)
- Output section: `// --- Output:` (NOT `// --- Stdout:`)
- Final `let _ = println expr` outputs include trailing `()` line

**tests/flt/file/string/str-slice.flt** — test LANG-01 string slicing:

```
// Test: String slicing s.[start..stop] and s.[start..] (LANG-01)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let s = "hello"
let _ = println (s.[1..3])
let _ = println (s.[2..])
let _ = println (s.[0..0])
let _ = println (s.[4..])
let t = "abcdef"
let _ = println (t.[0..2])
let _ = println (t.[3..5])
// --- Output:
ell
llo
h
o
abc
def
```

Note: `s.[5..]` on "hello" (length 5) would be `s.[5..4]` = "" but that's a edge case; test with in-bounds indices.

**tests/flt/file/list/list-comprehension.flt** — test LANG-02 list comprehension:

```
// Test: List comprehension [for x in coll -> expr] and range variant (LANG-02)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let doubled = [for x in [1;2;3] -> x * 2]
let _ = println (to_string doubled)
let squares = [for i in 0..4 -> i * i]
let _ = println (to_string squares)
let strs = [for s in ["a";"b";"c"] -> s ^^ "!"]
let _ = println (to_string strs)
// --- Output:
[2; 4; 6]
[0; 1; 4; 9; 16]
[a!; b!; c!]
```

Note: to_string on ListValue of StringValue renders without quotes in LangThree format. Verify actual output by running the interpreter — adjust expected output to match exactly what to_string produces.

Run each test file individually with FsLit to verify expected output before finalizing:
`/Users/ohama/.local/bin/FsLit tests/flt/file/string/str-slice.flt`
`/Users/ohama/.local/bin/FsLit tests/flt/file/list/list-comprehension.flt`

If actual output differs from expected, update the `// --- Output:` section to match.
  </action>
  <verify>/Users/ohama/.local/bin/FsLit tests/flt/file/string/str-slice.flt && /Users/ohama/.local/bin/FsLit tests/flt/file/list/list-comprehension.flt</verify>
  <done>Both flt files pass FsLit with exit code 0. String slicing and list comprehension produce correct output.</done>
</task>

<task type="auto">
  <name>Task 2: Write native collection for-in flt tests (HashSet, Queue, MutableList, Hashtable)</name>
  <files>
    tests/flt/file/hashset/hashset-forin.flt
    tests/flt/file/queue/queue-forin.flt
    tests/flt/file/mutablelist/mutablelist-forin.flt
    tests/flt/file/hashtable/hashtable-forin.flt
  </files>
  <action>
Write four flt test files. Use the binary path `/Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree`.

**tests/flt/file/hashset/hashset-forin.flt** — LANG-03 HashSet for-in:

```
// Test: for-in iteration over HashSet (LANG-03)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let hs = HashSet ()
let _ = hs.Add(10)
let _ = hs.Add(20)
let _ = hs.Add(30)
let _ = for x in hs do println (to_string x)
// --- Output:
10
20
30
```

Note: HashSet iteration order is not guaranteed. If tests are order-sensitive, use a sorted check or just verify a single-element HashSet. For a single element test this is safe.

Consider a simpler single-element test to avoid ordering issues:
```
let hs2 = HashSet ()
let _ = hs2.Add(42)
let _ = for x in hs2 do println (to_string x)
```

Use single-element for deterministic output, or if FsLit supports, use a multi-element but verify actual order.

**tests/flt/file/queue/queue-forin.flt** — LANG-03 Queue for-in:

```
// Test: for-in iteration over Queue (LANG-03)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let q = Queue ()
let _ = q.Enqueue(1)
let _ = q.Enqueue(2)
let _ = q.Enqueue(3)
let _ = for x in q do println (to_string x)
// --- Output:
1
2
3
```

Queue iteration is FIFO-ordered, so this test is deterministic.

**tests/flt/file/mutablelist/mutablelist-forin.flt** — LANG-03 MutableList for-in:

```
// Test: for-in iteration over MutableList (LANG-03)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let ml = MutableList ()
let _ = ml.Add(5)
let _ = ml.Add(10)
let _ = ml.Add(15)
let _ = for x in ml do println (to_string x)
// --- Output:
5
10
15
```

MutableList iteration is insertion-ordered, deterministic.

**tests/flt/file/hashtable/hashtable-forin.flt** — LANG-03 + PROP-05 Hashtable for-in with .Key/.Value:

```
// Test: for-in iteration over Hashtable with kv.Key and kv.Value (LANG-03, PROP-05)
// --- Command: /Users/ohama/vibe/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let ht = Hashtable.create ()
let _ = Hashtable.set ht "name" "Alice"
let _ = for kv in ht do
  let _ = println kv.Key
  println kv.Value
// --- Output:
name
Alice
```

Single-entry hashtable ensures deterministic output. The `.Key` and `.Value` accesses work via RecordValue("KeyValuePair", ...) dispatch.

After writing all four files, run them through FsLit. If actual output differs from expected (e.g., to_string formatting differences, different constructor output), update the `// --- Output:` sections to match the actual interpreter output exactly.

Run each:
```
/Users/ohama/.local/bin/FsLit tests/flt/file/hashset/hashset-forin.flt
/Users/ohama/.local/bin/FsLit tests/flt/file/queue/queue-forin.flt
/Users/ohama/.local/bin/FsLit tests/flt/file/mutablelist/mutablelist-forin.flt
/Users/ohama/.local/bin/FsLit tests/flt/file/hashtable/hashtable-forin.flt
```

Also run existing tests to check for regressions:
`/Users/ohama/.local/bin/FsLit tests/flt/`
  </action>
  <verify>/Users/ohama/.local/bin/FsLit tests/flt/file/hashset/hashset-forin.flt && /Users/ohama/.local/bin/FsLit tests/flt/file/queue/queue-forin.flt && /Users/ohama/.local/bin/FsLit tests/flt/file/mutablelist/mutablelist-forin.flt && /Users/ohama/.local/bin/FsLit tests/flt/file/hashtable/hashtable-forin.flt</verify>
  <done>All four for-in flt tests pass. Full test suite (/Users/ohama/.local/bin/FsLit tests/flt/) shows no regressions.</done>
</task>

</tasks>

<verification>
/Users/ohama/.local/bin/FsLit tests/flt/
</verification>

<success_criteria>
- 6 new flt test files created covering LANG-01, LANG-02, LANG-03, PROP-05
- All 6 new tests pass FsLit
- Full test suite passes with no regressions
- Phase 58 success criteria 1-5 all satisfied:
  1. s.[1..3] and s.[2..] produce correct substrings
  2. [for x in [1;2;3] -> x * 2] = [2; 4; 6] and [for i in 0..4 -> i * i] = [0; 1; 4; 9; 16]
  3. HashSet, Queue, MutableList for-in iterates correctly
  4. Hashtable for-in with kv.Key and kv.Value works
  5. flt tests verify all three constructs including edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/58-language-constructs/58-03-SUMMARY.md`
</output>
