# Milestone v8.0: Declaration Type Annotations

**Status:** SHIPPED 2026-03-30
**Phases:** 63-64
**Total Plans:** 5

## Overview

FunLexYacc 호환성을 위해 module-level 함수 선언에 F# 스타일 타입 어노테이션과 앵글 브래킷 제네릭 구문을 추가한다. 먼저 타입 표현식 문법에 앵글 브래킷 제네릭을 도입하여 어노테이션에서 참조할 수 있게 하고, 이어서 파라미터 타입 어노테이션과 반환 타입 어노테이션을 선언 문법에 추가한다.

## Phases

### Phase 63: Angle Bracket Generics

**Goal:** 타입 표현식에서 앵글 브래킷 제네릭 구문을 사용할 수 있다
**Depends on:** None
**Plans:** 2 plans
**Verification:** 6/6 success criteria passed

Plans:
- [x] 63-01: Add TypeArgList and angle bracket rules to Parser.fsy type grammar
- [x] 63-02: Add flt integration tests for angle bracket generic syntax

**Key accomplishments:**
- LT/GT 토큰 재활용으로 앵글 브래킷 파싱 (새 토큰 불필요)
- AngleBracketTypeParams (comma-separated) 별도 규칙으로 LALR 충돌 회피
- 단일/다중 타입 인자 지원: `Result<'a>`, `Either<'a, 'b>`, `Map<string, int>`
- 후위 구문과 혼합 가능: `Result<'a> list`

---

### Phase 64: Declaration Type Annotations

**Goal:** `let` 선언에서 파라미터 타입 어노테이션과 반환 타입 어노테이션을 모두 사용할 수 있다
**Depends on:** Phase 63
**Plans:** 3 plans
**Verification:** 5/5 success criteria passed

Plans:
- [x] 64-01: Add MixedParamList parser rule and desugar to Lambda/LambdaAnnot chain
- [x] 64-02: Add return type annotation syntax to let declarations and wrap body in Annot node
- [x] 64-03: Add flt integration tests for annotated declarations including mutual recursion

**Key accomplishments:**
- MixedParamList가 ParamList를 대체 — plain/annotated 파라미터 혼합 허용
- desugarMixedParams: Choice<string, string * TypeExpr> list → Lambda/LambdaAnnot 체인
- 반환 타입은 Annot(body, typeExpr)로 래핑 — 런타임 소거
- Decl, Expr let, Expr let rec, LetRecDeclaration, LetRecContinuation 모든 let-form 지원

---

## Milestone Summary

**Key Decisions:**
- Reuse LT/GT tokens for angle brackets — LALR(1) disambiguates by parser state
- AngleBracketTypeParams (comma-separated) separate from TypeParams (space-separated)
- MixedParamList subsumes ParamList — remove old productions to resolve reduce/reduce conflicts
- Return type annotation wraps body as Annot node (erased at runtime)
- No ParamList + return type productions: MixedParamList handles all cases via Choice1Of2

**Issues Resolved:**
- FunLexYacc type annotation incompatibility (~656 occurrences across 18 files now parseable)
- Angle bracket generic syntax gap (Result<'a> was unparseable)

**Issues Deferred:**
- Named DU fields (`of loc: SrcLoc * msg: string`) — only used in FunLexYacc ErrorInfo.fun
- `let private` access modifier — used in FunLexYacc but deferred

**Technical Debt Incurred:**
- None identified

---
*Archived: 2026-03-30*
