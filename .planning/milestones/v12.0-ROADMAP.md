# Milestone v12.0: Type Class Extensions

**Status:** IN PROGRESS
**Phases:** 89-92

## Phases

### Phase 89: Constrained Instance Infrastructure (CI-01, CI-04, CI-05)
**Goal:** InstanceInfo에 제약 + 타입 변수 추가, 유니피케이션 기반 인스턴스 해결
- InstanceInfo에 `Constraints: Constraint list`, `TypeVars: int list` 필드 추가
- Bidir.generalize에서 인스턴스 해결을 `Unify.unify` 기반으로 변경
- 재귀적 제약 해결 (subgoal 처리)
- TypeCheck.fs: 제약이 있는 InstanceDecl 처리

### Phase 90: Constrained Instance Prelude (CI-02, CI-03)
**Goal:** Show/Eq의 list, option 등 컨테이너 인스턴스 구현
- Prelude/Typeclass.fun에 `Show 'a => Show (list 'a)` 추가
- Prelude/Typeclass.fun에 `Eq 'a => Eq (list 'a)` 추가
- Option, tuple 등 추가 인스턴스
- flt 테스트

### Phase 91: Superclass Constraints (SC-01, SC-02, SC-03)
**Goal:** 타입 클래스 계층 지원
- Parser: `Eq 'a =>` 구문을 typeclass 선언 앞에 허용
- ClassInfo에 `Superclasses: string list` 추가
- 인스턴스 선언 시 슈퍼클래스 인스턴스 자동 요구
- 제약 전파: Ord 제약 → Eq 제약도 자동 추가

### Phase 92: Automatic Deriving (DRV-01, DRV-02, DRV-03, DRV-04)
**Goal:** `deriving Show, Eq` 자동 인스턴스 생성
- Parser: TypeDecl에 `deriving` 절 추가
- Show 도출: 생성자 패턴 매칭 → 이름 문자열 코드 생성
- Eq 도출: 모든 생성자 쌍에 대한 동등성 비교 코드 생성
- 인자가 있는 생성자: 제약 조건부 인스턴스로 생성
