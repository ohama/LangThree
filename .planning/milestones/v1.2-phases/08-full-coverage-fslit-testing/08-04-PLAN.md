---
phase: 08-full-coverage-fslit-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/emit/type-decl-let.flt
  - tests/flt/emit/type-decl-func.flt
  - tests/flt/emit/type-decl-adt.flt
  - tests/flt/emit/type-decl-adt-parametric.flt
  - tests/flt/emit/type-decl-gadt.flt
  - tests/flt/emit/type-decl-record.flt
  - tests/flt/emit/type-decl-record-ops.flt
  - tests/flt/emit/type-decl-exception.flt
  - tests/flt/emit/type-decl-module.flt
  - tests/flt/emit/type-decl-match-func.flt
  - tests/flt/emit/type-decl-trywith.flt
  - tests/flt/emit/type-decl-polymorphic.flt
autonomous: true

must_haves:
  truths:
    - "Every declaration construct has --emit-type test showing inferred types"
    - "Polymorphic type variables appear correctly normalized in output"
    - "ADT constructor types show correctly in binding types"
    - "All tests pass via fslit"
  artifacts:
    - path: "tests/flt/emit/type-decl-adt.flt"
      provides: "ADT constructor type inference test"
    - path: "tests/flt/emit/type-decl-gadt.flt"
      provides: "GADT constructor type inference test"
    - path: "tests/flt/emit/type-decl-record.flt"
      provides: "Record type inference test"
    - path: "tests/flt/emit/type-decl-polymorphic.flt"
      provides: "Polymorphic function type inference test"
  key_links: []
---

<objective>
Create --emit-type tests for all declaration-level constructs using file mode (%input).

Purpose: Verify the type checker infers correct types for module-level bindings across all construct types.
Output: 12 .flt test files in tests/flt/emit/ covering type inference for declarations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Key info:
- LangThree binary: src/LangThree/bin/Release/net10.0/LangThree
- fslit binary: /Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit
- --emit-type in file mode outputs `name : type` for each user-defined top-level binding
- It filters out prelude bindings (only shows user-defined names)
- Type format: `x : int`, `f : int -> int`, `p : Point`, `c : Color`
- Polymorphic: `id : 'a -> 'a` (normalized type variables)
- Warning on stderr for non-exhaustive match -- check if this affects output
- Exception constructors may appear as bindings with type `exn` or function type
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture --emit-type output for all declaration constructs</name>
  <files>N/A (exploration only)</files>
  <action>
For each test, create a temp file and run `--emit-type` on it. Record actual output.

Test cases:

1. **Simple let bindings:** `let x = 42` + `let y = true` -> `x : int`, `y : bool`
2. **Function declarations:** `let add x y = x + y` -> `add : int -> int -> int`
3. **ADT with constructors:** `type Color = Red | Green | Blue` + `let c = Red` -> `c : Color`
4. **Parametric ADT:** `type Option 'a = Some of 'a | None` + `let x = Some 42` -> `x : int Option` (verify exact format)
5. **GADT:** `type Expr 'a = IntLit : int -> int Expr | BoolLit : bool -> bool Expr` + `let e = IntLit 42` -> check type
6. **Record:** `type Point = { x: int; y: int }` + `let p = { x = 1; y = 2 }` -> `p : Point`
7. **Record operations:** record type + field access binding + copy-update binding -> types for each
8. **Exception:** `exception NotFound` + check if constructor appears in type output
9. **Module with open:** module + open + binding -> types visible after open
10. **Match function:** ADT + function using match -> function type (e.g., `Color -> string`)
11. **Try-with:** exception + try-with function -> function type
12. **Polymorphic function:** `let id x = x` -> `id : 'a -> 'a`

Run with `2>/dev/null` for clean stdout. Note: --emit-type may send warnings to stderr.
  </action>
  <verify>All commands produce expected type output with exit code 0</verify>
  <done>Type output captured for all declaration constructs</done>
</task>

<task type="auto">
  <name>Task 2: Create .flt test files and verify with fslit</name>
  <files>
    tests/flt/emit/type-decl-let.flt
    tests/flt/emit/type-decl-func.flt
    tests/flt/emit/type-decl-adt.flt
    tests/flt/emit/type-decl-adt-parametric.flt
    tests/flt/emit/type-decl-gadt.flt
    tests/flt/emit/type-decl-record.flt
    tests/flt/emit/type-decl-record-ops.flt
    tests/flt/emit/type-decl-exception.flt
    tests/flt/emit/type-decl-module.flt
    tests/flt/emit/type-decl-match-func.flt
    tests/flt/emit/type-decl-trywith.flt
    tests/flt/emit/type-decl-polymorphic.flt
  </files>
  <action>
Create .flt test files using file-mode format:

```
// Test: --emit-type [description]
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree --emit-type %input
// --- Input:
[source code]
// --- Output:
[name : type output lines]
```

Note: --emit-type may output multiple lines (one per binding). The `// --- Output:` section should contain all output lines.

If --emit-type sends warnings to stderr (e.g., non-exhaustive match), use `// --- Stderr:` to match them OR ensure the test source avoids warnings.

After creating, run fslit:
```
/Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit tests/flt/emit/type-decl-*.flt
```
  </action>
  <verify>
Run: `/Users/ohama/vibe-coding/fslit/FsLit/bin/Debug/net10.0/FsLit tests/flt/emit/type-decl-*.flt`
All tests pass (exit code 0).
  </verify>
  <done>All declaration type emit tests created and passing via fslit</done>
</task>

</tasks>

<verification>
1. Run fslit on all type-decl tests -- all pass
2. Run fslit on ALL tests -- no regressions
3. Verify coverage: basic types (int, bool, string), ADT types (Color), parametric types (int Option), function types (int -> int), record types (Point), polymorphic types ('a -> 'a)
</verification>

<success_criteria>
- Type inference for all declaration constructs tested
- Type output formats verified: simple types, arrow types, ADT types, record types, polymorphic types
- All tests pass via fslit with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-coverage-fslit-testing/08-04-SUMMARY.md`
</output>
