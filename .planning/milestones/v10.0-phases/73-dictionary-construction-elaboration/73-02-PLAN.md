---
phase: 73-dictionary-construction-elaboration
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - tests/flt/file/typeclass/typeclass-runtime-basic.flt
  - tests/flt/file/typeclass/typeclass-runtime-higher-order.flt
  - tests/flt/file/typeclass/typeclass-runtime-multi-method.flt
autonomous: true

must_haves:
  truths:
    - "show 42 evaluates to string 42 at runtime"
    - "List.map show [1; 2; 3] evaluates to list of stringified ints"
    - "Multi-method typeclass instance works at runtime"
  artifacts:
    - path: "tests/flt/file/typeclass/typeclass-runtime-basic.flt"
      provides: "Basic runtime test for show 42"
    - path: "tests/flt/file/typeclass/typeclass-runtime-higher-order.flt"
      provides: "Higher-order test for List.map show"
    - path: "tests/flt/file/typeclass/typeclass-runtime-multi-method.flt"
      provides: "Multi-method class instance runtime test"
  key_links:
    - from: "tests/flt/file/typeclass/typeclass-runtime-basic.flt"
      to: "src/LangThree/Elaborate.fs"
      via: "elaborateTypeclasses transforms InstanceDecl so show is callable"
      pattern: "show 42"
---

<objective>
Create flt integration tests that verify typeclass methods execute correctly at runtime, covering the Phase 73 success criteria: basic method call, higher-order usage with List.map, and multi-method instances.

Purpose: Lock in runtime behavior with regression tests. The Phase 72 tests used --check mode (type-check only); these new tests use full execution mode to verify the elaboration pass works end-to-end.

Output: 3 new flt test files in tests/flt/file/typeclass/
</objective>

<execution_context>
@./.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-dictionary-construction-elaboration/73-01-SUMMARY.md

Key codebase facts:
- flt test format: `// --- Command:` specifies the binary + args, `// --- Input:` is the LangThree source, `// --- Stdout:` or `// --- Stderr:` is expected output.
- For runtime tests, the command is the binary with `%input` (no --check flag). The program runs and the last let-binding value is printed to stdout.
- LangThree uses `+` for string concatenation (not `++` which is list append).
- `to_string` is a built-in: `'a -> string`, works on int/bool/string/char.
- `List.map` is available in prelude.
- The binary path: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree`
- Phase 72 Plan 03 established the pattern: one test case per file, Command uses full binary path + `%input`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runtime flt tests for typeclass method dispatch</name>
  <files>
    tests/flt/file/typeclass/typeclass-runtime-basic.flt
    tests/flt/file/typeclass/typeclass-runtime-higher-order.flt
    tests/flt/file/typeclass/typeclass-runtime-multi-method.flt
  </files>
  <action>
Create 3 flt test files:

**typeclass-runtime-basic.flt** -- Success criterion 1: `show 42` evaluates to `"42"`
```
// Test: instance Show int — show 42 evaluates to "42" at runtime
// --- Command: {BINARY_PATH} %input
// --- Input:
typeclass Show 'a =
    | show : 'a -> string

instance Show int =
    let show x = to_string x

let result = show 42
// --- Stdout:
"42"
```
Note: The program prints the last let-binding value. `show 42` returns a StringValue, which is printed as `"42"` (with quotes, matching LangThree's formatValue for strings).

**typeclass-runtime-higher-order.flt** -- Success criterion 2: `List.map show [1; 2; 3]` evaluates to `["1"; "2"; "3"]`
```
// Test: higher-order usage — List.map show [1; 2; 3] evaluates to ["1"; "2"; "3"]
// --- Command: {BINARY_PATH} %input
// --- Input:
typeclass Show 'a =
    | show : 'a -> string

instance Show int =
    let show x = to_string x

let map_show lst = List.map show lst

let result = map_show [1; 2; 3]
// --- Stdout:
["1"; "2"; "3"]
```

**typeclass-runtime-multi-method.flt** -- Multi-method class with 2+ methods both callable
```
// Test: multi-method typeclass — both methods callable at runtime
// --- Command: {BINARY_PATH} %input
// --- Input:
typeclass Describable 'a =
    | describe : 'a -> string
    | tag : 'a -> string

instance Describable int =
    let describe x = to_string x
    let tag x = "int"

let result = describe 42 + ":" + tag 42
// --- Stdout:
"42:int"
```

IMPORTANT: Verify the exact output format by running the binary manually if needed. The `formatValue` function for StringValue wraps in quotes. ListValue of StringValues prints as `["1"; "2"; "3"]`. Adjust expected Stdout accordingly.

Replace `{BINARY_PATH}` with the actual path: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree`
  </action>
  <verify>
1. `../fslit/dist/FsLit tests/flt/file/typeclass/typeclass-runtime-basic.flt` passes
2. `../fslit/dist/FsLit tests/flt/file/typeclass/typeclass-runtime-higher-order.flt` passes
3. `../fslit/dist/FsLit tests/flt/file/typeclass/typeclass-runtime-multi-method.flt` passes
4. `../fslit/dist/FsLit tests/flt/file/typeclass/` -- all 12 typeclass tests pass (9 existing + 3 new)
5. `../fslit/dist/FsLit tests/flt/` -- full test suite passes
  </verify>
  <done>
3 runtime flt tests pass, covering: basic show call, higher-order List.map show, and multi-method typeclass. All 12 typeclass tests pass. Full flt suite passes.
  </done>
</task>

</tasks>

<verification>
1. `../fslit/dist/FsLit tests/flt/file/typeclass/` -- all 12 tests pass (9 check-mode + 3 runtime)
2. `../fslit/dist/FsLit tests/flt/` -- full test suite green
3. Success criteria 1: typeclass-runtime-basic.flt proves `show 42` -> `"42"`
4. Success criteria 2: typeclass-runtime-higher-order.flt proves `List.map show [1; 2; 3]` -> `["1"; "2"; "3"]`
5. Success criteria 3: verified by Plan 01 (no MethodCall AST node, Eval.fs unchanged)
</verification>

<success_criteria>
- 3 new flt runtime tests exist and pass
- Tests cover all 3 Phase 73 success criteria
- Full flt test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/73-dictionary-construction-elaboration/73-02-SUMMARY.md`
</output>
