---
phase: 71-parsing-and-ast
plan: "02"
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - tests/flt/file/typeclass/typeclass-parse-basic.flt
autonomous: true

must_haves:
  truths:
    - "typeclass declaration parses and --emit-ast outputs TypeClassDecl node"
    - "instance declaration parses and --emit-ast outputs InstanceDecl node"
    - "constrained type annotation parses and --emit-ast outputs TEConstrained"
  artifacts:
    - path: "tests/flt/file/typeclass/typeclass-parse-basic.flt"
      provides: "Integration tests validating parsing of typeclass, instance, and constrained type syntax"
  key_links:
    - from: "tests/flt/file/typeclass/typeclass-parse-basic.flt"
      to: "src/LangThree/Format.fs"
      via: "--emit-ast CLI flag renders new AST nodes"
      pattern: "TypeClassDecl|InstanceDecl"
---

<objective>
Validate that the new typeclass/instance/constraint syntax parses correctly via flt integration tests using --emit-ast.

Purpose: Proves the parser grammar from Plan 01 actually works end-to-end. Uses --emit-ast to verify AST structure without hitting the TypeCheck/Eval failwith stubs.

Output: New flt test file that passes with FsLit, confirming all three syntactic forms parse to correct AST nodes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-parsing-and-ast/71-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flt integration tests for typeclass parsing</name>
  <files>
    tests/flt/file/typeclass/typeclass-parse-basic.flt
  </files>
  <action>
Create the directory `tests/flt/file/typeclass/` and the test file `typeclass-parse-basic.flt`.

The flt test format uses `--emit-ast` to verify AST output without running the program (which would hit the failwith stubs). Study existing flt tests that use `--emit-ast` to understand the exact output format — check files like `tests/flt/file/` for examples using `--emit-ast`.

First, run the binary manually on a small test input to discover the exact --emit-ast output format for the new nodes. For example, create a temp file with:
```
typeclass Show 'a =
    show : 'a -> string
```
and run `dotnet run --project src/LangThree/LangThree.fsproj -- --emit-ast tempfile.fun` to see the actual output.

Then create the flt test with these test cases:

1. **Typeclass declaration:** `typeclass Show 'a = show : 'a -> string` — verify TypeClassDecl appears in --emit-ast output.

2. **Instance declaration:** `instance Show int = let show x = to_string x` — verify InstanceDecl appears in --emit-ast output.

3. **Multiple methods in typeclass:**
   ```
   typeclass Eq 'a =
       eq : 'a -> 'a -> bool
       neq : 'a -> 'a -> bool
   ```
   Verify both methods appear in the TypeClassDecl node.

4. **Constrained type annotation (if parseable in isolation):** Test `Show 'a => 'a -> string` as a type expression in a let declaration context, e.g., `let f : Show 'a => 'a -> string = fun x -> show x`. Since this would hit the type checker failwith, use --emit-ast only.

Use the `--emit-ast` flag for ALL tests since the type checker and evaluator will failwith on these new forms. The flt test runner checks stdout output against expected output.

Check the exact flt test syntax by reading an existing --emit-ast test file first (e.g., look for files in tests/flt/ that reference `--emit-ast` in their content).
  </action>
  <verify>
1. `../fslit/dist/FsLit tests/flt/file/typeclass/typeclass-parse-basic.flt` — test passes.
2. `../fslit/dist/FsLit tests/flt/` — all tests pass (659 existing + new test).
  </verify>
  <done>
New flt test file exists and passes, confirming typeclass declaration, instance declaration, and constrained type annotation all parse to correct AST nodes. Total flt test count increases from 659.
  </done>
</task>

</tasks>

<verification>
1. `../fslit/dist/FsLit tests/flt/file/typeclass/` — new tests pass
2. `../fslit/dist/FsLit tests/flt/` — all tests pass (existing + new)
3. `dotnet test tests/LangThree.Tests/LangThree.Tests.fsproj -c Release` — 224/224 still pass
</verification>

<success_criteria>
- At least 3 flt test cases covering typeclass decl, instance decl, and constrained type annotation
- All new tests use --emit-ast (avoiding TypeCheck/Eval failwith stubs)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/71-parsing-and-ast/71-02-SUMMARY.md`
</output>
