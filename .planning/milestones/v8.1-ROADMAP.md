# Milestone v8.1: Mutual Recursion Completion

**Status:** SHIPPED 2026-03-31
**Phases:** 65-66
**Total Plans:** 4

## Overview

v8.1 completes the mutual recursion story: first by fixing the LetRecDecl AST to preserve type annotations on first parameters (lost during v8.0 parser desugaring), then by implementing expression-level `let rec ... and ...` across all compiler layers. Two phases, building on 64 phases shipped across v1.0-v8.0.

## Phases

### Phase 65: LetRecDecl AST Refactoring
**Goal**: Module-level `let rec ... and ...` correctly preserves and verifies first parameter type annotations
**Depends on**: Nothing (first phase of v8.1)
**Plans**: 2 plans

Plans:
- [x] 65-01: AST definition change + parser + all 28 mechanical pattern-match site updates
- [x] 65-02: Type checker enforcement logic + flt tests for annotation enforcement

### Phase 66: Expression-Level Mutual Recursion
**Goal**: Users can write `let rec f x = ... and g y = ... in expr` inside any expression context with full type annotation support
**Depends on**: Phase 65
**Plans**: 2 plans

Plans:
- [x] 66-01: AST node change (LetRec bindings list) + parser grammar + mechanical pattern-match fixes
- [x] 66-02: Type checking/eval logic for multi-binding + flt tests

## Milestone Summary

**Key Decisions:**
- LetRec bindings list shape matches LetRecDecl exactly — `(string * string * TypeExpr option * Expr * Span) list`
- LetRecContinuation nonterminal reused for both module-level and expression-level let rec
- sharedEnvRef pattern for mutual closure linking (same as LetRecDecl)
- elaborateTypeExpr accessed as bare function via `open Elaborate` (not `Bidir.elaborateTypeExpr`)

**Issues Resolved:**
- v8.0 LetRecDecl first parameter type annotation silently dropped by parser
- Expression-level `let rec` limited to single binding (no `and` support)

**Issues Deferred:**
- Multi-line `and` on separate indented lines triggers IndentFilter issues (cosmetic — single-line format works)

**Technical Debt Incurred:**
- None

---
*Archived: 2026-03-31*
