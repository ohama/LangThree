---
phase: 43-edge-cases-and-error-handling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/file/mutable/mut-immutable-assign-error.flt
  - tests/flt/file/mutable/mut-type-mismatch-error.flt
  - tests/flt/file/mutable/mut-closure-read.flt
  - tests/flt/file/mutable/mut-closure-write.flt
  - tests/flt/file/mutable/mut-param-immutable-error.flt
autonomous: true

must_haves:
  truths:
    - "Reassigning immutable variable with <- produces E0320 error"
    - "Reassigning mutable variable with wrong type produces E0301 error"
    - "Closure captures and reads updated mutable variable value"
    - "Closure captures and writes to mutable variable"
    - "Function parameter assignment produces E0320 error"
  artifacts:
    - path: "tests/flt/file/mutable/mut-immutable-assign-error.flt"
      provides: "MUT-04 immutable assignment error test"
    - path: "tests/flt/file/mutable/mut-type-mismatch-error.flt"
      provides: "MUT-06 type mismatch error test"
    - path: "tests/flt/file/mutable/mut-closure-read.flt"
      provides: "MUT-07 closure read test"
    - path: "tests/flt/file/mutable/mut-closure-write.flt"
      provides: "MUT-07 closure write test"
    - path: "tests/flt/file/mutable/mut-param-immutable-error.flt"
      provides: "MUT-08 parameter immutability test"
  key_links:
    - from: "tests/flt/file/mutable/*.flt"
      to: "src/LangThree/bin/Release/net10.0/LangThree"
      via: "FsLit test runner"
      pattern: "Command:.*LangThree %input"
---

<objective>
Add flt tests verifying all Phase 43 edge case and error handling requirements.

Purpose: Phase 42 already implemented all MUT-04/06/07/08 behaviors. This plan adds the flt test suite that proves these behaviors work correctly, covering error diagnostics (E0320, E0301) and closure capture of mutable variables.

Output: 5 flt test files in tests/flt/file/mutable/ that all pass via FsLit.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-core-mutable-variables/42-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mutable directory and error diagnostic tests</name>
  <files>
    tests/flt/file/mutable/mut-immutable-assign-error.flt
    tests/flt/file/mutable/mut-type-mismatch-error.flt
    tests/flt/file/mutable/mut-param-immutable-error.flt
  </files>
  <action>
Create the `tests/flt/file/mutable/` directory.

Create 3 error-case flt tests. Important format notes:
- Command line: `// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`
- Error output goes to stderr, so use `// --- Stderr:` section with specific lines to match
- FsLit Stderr matching is contains-match: each expected line must appear exactly in actual stderr output
- Use `// --- ExitCode: 1` for error tests
- Do NOT include `// --- Output:` section for error tests (no stdout expected)

**MUT-04 test (mut-immutable-assign-error.flt):**
```
// Test: assigning to immutable variable produces E0320 error
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = let x = 5 in x <- 10
// --- ExitCode: 1
// --- Stderr:
error[E0320]: Cannot assign to immutable variable 'x'. Use 'let mut' to declare mutable variables.
```

**MUT-06 test (mut-type-mismatch-error.flt):**
```
// Test: reassigning mutable variable with different type produces E0301 error
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = let mut x = 5 in x <- "hello"
// --- ExitCode: 1
// --- Stderr:
error[E0301]: Type mismatch: expected int but got string
```

**MUT-08 test (mut-param-immutable-error.flt):**
```
// Test: function parameter is always immutable, assignment produces E0320
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let f x = x <- 10
// --- ExitCode: 1
// --- Stderr:
error[E0320]: Cannot assign to immutable variable 'x'. Use 'let mut' to declare mutable variables.
```
  </action>
  <verify>
Run each test individually:
```
../fslit/dist/FsLit tests/flt/file/mutable/mut-immutable-assign-error.flt
../fslit/dist/FsLit tests/flt/file/mutable/mut-type-mismatch-error.flt
../fslit/dist/FsLit tests/flt/file/mutable/mut-param-immutable-error.flt
```
All 3 must pass.
  </verify>
  <done>3 error-case flt tests exist and pass, covering MUT-04, MUT-06, MUT-08</done>
</task>

<task type="auto">
  <name>Task 2: Create closure capture tests</name>
  <files>
    tests/flt/file/mutable/mut-closure-read.flt
    tests/flt/file/mutable/mut-closure-write.flt
  </files>
  <action>
Create 2 closure capture flt tests for MUT-07.

Important syntax notes discovered during planning:
- Use `let tmp = ...` instead of `let _ = ...` for discarding assignment results (underscore pattern has parse issues with `<-` expressions)
- Use named parameters (`y`) not wildcard (`_`) for closure dummy params
- Use explicit `in` syntax for chaining let expressions (offside rule has issues with `<-` in implicit-in blocks)

**MUT-07 closure read test (mut-closure-read.flt):**
A closure captures a mutable variable and reads its UPDATED value after mutation.
```
// Test: closure captures mutable variable and reads updated value
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = let mut x = 10 in let f y = x in let tmp = x <- 42 in f 0
// --- Output:
42
```

**MUT-07 closure write test (mut-closure-write.flt):**
A closure captures a mutable variable and writes to it, incrementing twice.
```
// Test: closure captures mutable variable and writes to it
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = let mut x = 0 in let inc y = x <- x + 1 in let a = inc 0 in let b = inc 0 in x
// --- Output:
2
```
  </action>
  <verify>
Run each test individually:
```
../fslit/dist/FsLit tests/flt/file/mutable/mut-closure-read.flt
../fslit/dist/FsLit tests/flt/file/mutable/mut-closure-write.flt
```
Both must pass.
  </verify>
  <done>2 closure capture flt tests exist and pass, covering MUT-07 read and write scenarios</done>
</task>

</tasks>

<verification>
Run the full mutable test suite:
```
../fslit/dist/FsLit tests/flt/file/mutable/
```
Expected: 5/5 tests pass.

Also run the full flt suite to confirm no regressions:
```
../fslit/dist/FsLit tests/flt/file/
```
Expected: 526/526 pass (521 existing + 5 new).
</verification>

<success_criteria>
- 5 flt test files exist in tests/flt/file/mutable/
- All 5 tests pass individually and as a suite
- Full flt suite passes with no regressions
- MUT-04: immutable assignment -> E0320 verified
- MUT-06: type mismatch on reassignment -> E0301 verified
- MUT-07: closure read and write of mutable variable verified
- MUT-08: function parameter immutability -> E0320 verified
</success_criteria>

<output>
After completion, create `.planning/phases/43-edge-cases-and-error-handling/43-01-SUMMARY.md`
</output>
