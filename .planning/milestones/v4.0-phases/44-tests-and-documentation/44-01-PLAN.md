---
phase: 44-tests-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/file/mutable/mut-basic-declare-read.flt
  - tests/flt/file/mutable/mut-reassign-read.flt
  - tests/flt/file/mutable/mut-module-level.flt
  - tests/flt/file/mutable/mut-assign-returns-unit.flt
  - tests/flt/file/mutable/mut-string-type.flt
  - tests/flt/file/mutable/mut-bool-type.flt
  - tests/flt/file/mutable/mut-nested.flt
  - tests/flt/file/mutable/mut-offside-rule.flt
  - tests/flt/file/mutable/mut-in-match.flt
  - tests/flt/file/mutable/mut-in-if-then-else.flt
autonomous: true

must_haves:
  truths:
    - "Basic let mut declaration and read works correctly"
    - "Reassignment updates the value and subsequent reads see the new value"
    - "Module-level let mut works with subsequent module-level reads"
    - "Assignment expression returns unit"
    - "let mut works with string and bool types, not just int"
    - "Nested let mut scopes work correctly"
    - "let mut works inside offside-rule indented blocks"
    - "let mut works inside match arms and if-then-else branches"
  artifacts:
    - path: "tests/flt/file/mutable/mut-basic-declare-read.flt"
      provides: "TST-24: basic declaration test"
    - path: "tests/flt/file/mutable/mut-reassign-read.flt"
      provides: "TST-24: reassignment test"
    - path: "tests/flt/file/mutable/mut-module-level.flt"
      provides: "TST-24: module-level mutable test"
    - path: "tests/flt/file/mutable/mut-nested.flt"
      provides: "TST-26: nested let mut test"
    - path: "tests/flt/file/mutable/mut-offside-rule.flt"
      provides: "TST-26: offside rule test"
    - path: "tests/flt/file/mutable/mut-in-match.flt"
      provides: "TST-26: match arm test"
    - path: "tests/flt/file/mutable/mut-in-if-then-else.flt"
      provides: "TST-26: if-then-else test"
  key_links:
    - from: "each .flt file"
      to: "LangThree binary"
      via: "flt test runner"
      pattern: "// --- Command:.*LangThree %input"
---

<objective>
Create comprehensive flt tests for mutable variable basic operations (TST-24) and advanced scenarios (TST-26).

Purpose: Fill the test gaps for basic `let mut` operations and advanced scenarios (nested, offside, match, if-then-else) not covered by Phase 43's error and closure tests.
Output: 10 new .flt test files in tests/flt/file/mutable/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/flt/file/mutable/mut-closure-read.flt (format reference)
@tests/flt/file/mutable/mut-closure-write.flt (format reference)
@tests/flt/file/mutable/mut-immutable-assign-error.flt (format reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create basic operation flt tests (TST-24)</name>
  <files>
    tests/flt/file/mutable/mut-basic-declare-read.flt
    tests/flt/file/mutable/mut-reassign-read.flt
    tests/flt/file/mutable/mut-module-level.flt
    tests/flt/file/mutable/mut-assign-returns-unit.flt
    tests/flt/file/mutable/mut-string-type.flt
    tests/flt/file/mutable/mut-bool-type.flt
  </files>
  <action>
Create 6 flt test files for basic mutable variable operations. Each file follows the exact format of existing tests (see mut-closure-read.flt for template).

Command line for all tests:
`// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input`

**mut-basic-declare-read.flt** — Declare a mutable variable and immediately read it:
```
// Test: basic let mut declaration and read
// --- Input:
let result = let mut x = 42 in x
// --- Output:
42
```

**mut-reassign-read.flt** — Declare, reassign, then read updated value:
```
// Test: mutable variable reassignment and read
// --- Input:
let result = let mut x = 1 in let _ = x <- 99 in x
// --- Output:
99
```

**mut-module-level.flt** — Module-level mutable variable with reassignment:
```
// Test: module-level mutable variable declaration and reassignment
// --- Input:
let mut x = 0
let _ = x <- 42
let result = x
// --- Output:
42
```

**mut-assign-returns-unit.flt** — Assignment expression returns unit:
```
// Test: assignment expression returns unit
// --- Input:
let result = let mut x = 0 in x <- 10
// --- Output:
()
```

**mut-string-type.flt** — Mutable variable with string type:
```
// Test: mutable variable with string type
// --- Input:
let result = let mut s = "hello" in let _ = s <- "world" in s
// --- Output:
"world"
```

**mut-bool-type.flt** — Mutable variable with bool type:
```
// Test: mutable variable with bool type
// --- Input:
let result = let mut b = true in let _ = b <- false in b
// --- Output:
false
```
  </action>
  <verify>
Run `cd /Users/ohama/vibe-coding/LangThree && ../fslit/dist/FsLit tests/flt/file/mutable/mut-basic-declare-read.flt tests/flt/file/mutable/mut-reassign-read.flt tests/flt/file/mutable/mut-module-level.flt tests/flt/file/mutable/mut-assign-returns-unit.flt tests/flt/file/mutable/mut-string-type.flt tests/flt/file/mutable/mut-bool-type.flt` — all 6 tests pass.
  </verify>
  <done>All 6 basic operation tests pass. TST-24 fully covered.</done>
</task>

<task type="auto">
  <name>Task 2: Create advanced scenario flt tests (TST-26)</name>
  <files>
    tests/flt/file/mutable/mut-nested.flt
    tests/flt/file/mutable/mut-offside-rule.flt
    tests/flt/file/mutable/mut-in-match.flt
    tests/flt/file/mutable/mut-in-if-then-else.flt
  </files>
  <action>
Create 4 flt test files for advanced mutable variable scenarios. Same command line and format as Task 1.

**mut-nested.flt** — Nested let mut with independent scopes:
```
// Test: nested let mut variables with independent scopes
// --- Input:
let result = let mut x = 1 in let mut y = 10 in let _ = x <- 2 in let _ = y <- 20 in x + y
// --- Output:
22
```

**mut-offside-rule.flt** — let mut inside an offside-rule indented block (module-level function body):
```
// Test: let mut inside offside-rule indented block
// --- Input:
let counter n =
    let mut i = 0
    let mut sum = 0
    let _ = i <- n
    let _ = sum <- i + 10
    sum
let result = counter 5
// --- Output:
15
```

**mut-in-match.flt** — let mut used with match expression:
```
// Test: let mut used inside match expression
// --- Input:
type Option = Some int | None
let result =
    let mut x = 0
    let _ =
        match Some 42 with
        | Some n -> x <- n
        | None -> x <- -1
    x
// --- Output:
42
```

**mut-in-if-then-else.flt** — let mut used with if-then-else:
```
// Test: let mut used with if-then-else
// --- Input:
let result =
    let mut x = 0
    let _ = if true then x <- 100 else x <- 200
    x
// --- Output:
100
```

IMPORTANT: If any test fails due to syntax differences from expected, check the actual output carefully and adjust the test. The offside-rule test uses indentation-based blocks at module level. The match and if-then-else tests use `let _ =` to capture the unit result of assignment.
  </action>
  <verify>
Run `cd /Users/ohama/vibe-coding/LangThree && ../fslit/dist/FsLit tests/flt/file/mutable/mut-nested.flt tests/flt/file/mutable/mut-offside-rule.flt tests/flt/file/mutable/mut-in-match.flt tests/flt/file/mutable/mut-in-if-then-else.flt` — all 4 tests pass.
  </verify>
  <done>All 4 advanced scenario tests pass. TST-26 fully covered (combined with Phase 43 closure tests).</done>
</task>

</tasks>

<verification>
Run the full mutable test suite:
```
cd /Users/ohama/vibe-coding/LangThree && ../fslit/dist/FsLit tests/flt/file/mutable/
```
All 15 tests (5 existing from Phase 43 + 10 new) must pass.
</verification>

<success_criteria>
- 6 basic operation tests pass (TST-24 complete)
- TST-25 confirmed covered by Phase 43's 3 error tests
- 4 advanced scenario tests pass (TST-26 complete with Phase 43's 2 closure tests)
- Full mutable test directory passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/44-tests-and-documentation/44-01-SUMMARY.md`
</output>
