---
phase: 34-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/file/import/file-import-basic.flt
  - tests/flt/file/import/file-import-qualified.flt
  - tests/flt/file/fileio/fileio-write-read.flt
  - tests/flt/file/fileio/fileio-file-exists.flt
  - tests/flt/file/fileio/fileio-append.flt
  - tests/flt/file/fileio/fileio-write-read-lines.flt
  - tests/flt/file/fileio/fileio-path-combine.flt
  - tests/flt/file/fileio/fileio-dir-files.flt
  - tests/flt/file/fileio/fileio-get-cwd.flt
  - tests/flt/file/fileio/fileio-get-env-missing.flt
autonomous: true

must_haves:
  truths:
    - "flt test for open \"lib.fun\" file import (basic bindings) exists and passes"
    - "flt test for qualified access to imported module exists and passes"
    - "flt tests for write_file/read_file round-trip exist and pass"
    - "flt tests for file_exists and append_file exist and pass"
    - "flt tests for read_lines/write_lines round-trip exist and pass"
    - "flt tests for path_combine, dir_files, and get_cwd exist and pass"
    - "flt test for get_env with a missing variable (exception branch) exists and passes"
    - "dotnet test passes with total flt count exceeding the count after Plan 01"
  artifacts:
    - path: "tests/flt/file/import/"
      provides: "file import regression tests (TST-07, TST-08)"
    - path: "tests/flt/file/fileio/"
      provides: "file I/O and system builtin regression tests (TST-09 through TST-13)"
  key_links:
    - from: "tests/flt/file/import/file-import-basic.flt"
      to: "FileImportDecl in Eval.fs"
      via: "open \"lib.fun\" directive processed at runtime"
      pattern: "open.*\\.fun"
    - from: "tests/flt/file/fileio/fileio-write-read.flt"
      to: "write_file / read_file builtins in Eval.fs"
      via: "program writes then reads back from same temp path"
      pattern: "write_file|read_file"
---

<objective>
Add flt regression tests for file import (Phase 31) and file I/O + system builtins (Phase 32).

Purpose: Phase 31 and 32 features have F# unit test coverage only. flt tests provide binary-level end-to-end regression guards for the full pipeline and for runtime behaviors that only manifest when actually running the executable.
Output: 10 new .flt files covering TST-07 through TST-13. Combined with Plan 01, the total flt test count increases from 447.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@tests/flt/file/module/module-open.flt
@tests/flt/file/let/let-sequence-complex.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file import flt tests (TST-07, TST-08)</name>
  <files>
    tests/flt/file/import/file-import-basic.flt
    tests/flt/file/import/file-import-qualified.flt
  </files>
  <action>
Create the directory `tests/flt/file/import/` and write 2 .flt files.

**Constraint:** The flt framework provides only ONE temp file via `%input`. File import tests require a second file (the imported library). The solution: the test program itself writes the library file using `write_file` before the `open` directive. The interpreter processes declarations top-to-bottom, so write_file runs before open is evaluated.

Use this exact header format:
```
// Test: <description>
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
<program source>
// --- Output:
<expected output>
```

**file-import-basic.flt** — TST-07: basic open "lib.fun" import
The program writes a lib.fun file to /tmp using write_file, then opens it:
```
// Test: open "lib.fun" imports bindings from an external file
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_file "/tmp/flt-import-lib.fun" "let add x y = x + y\n"
open "/tmp/flt-import-lib.fun"
let result = add 3 4
// --- Output:
7
```

**file-import-qualified.flt** — TST-08: qualified access to imported module binding
The imported file defines a module; the main program accesses it with qualified syntax:
```
// Test: open file then use qualified module access from imported bindings
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_file "/tmp/flt-import-mod.fun" "module Math =\n    let square x = x * x\n"
open "/tmp/flt-import-mod.fun"
let result = Math.square 5
// --- Output:
25
```

Before finalizing: manually verify that `write_file` is processed as a declaration (the `let _ = write_file ...` pattern) and that `open` is resolved relative to the absolute path given. Check by running:
```
echo 'let _ = write_file "/tmp/test-lib.fun" "let x = 42\n"
open "/tmp/test-lib.fun"
let result = x' | /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /dev/stdin
```
Expected output: `42`. If this does not work (e.g., open is processed before write_file executes), an alternative is:
- Use a fixed known path that the test writes and reads synchronously
- If the language does not support this pattern, fall back to testing `open` with a pre-existing system file (not ideal)

If the write_file + open pattern does not work, document this in the SUMMARY and mark TST-07/TST-08 as "unit-tested only, no flt feasible" with a clear explanation.
  </action>
  <verify>Run each .flt input manually through the binary. Confirm `file-import-basic.flt` outputs `7` and `file-import-qualified.flt` outputs `25`.</verify>
  <done>2 .flt files exist in tests/flt/file/import/. Both execute without error and produce the correct output. If the write_file+open pattern is unsupported at the declaration level, the plan records the limitation and skips these tests with documentation in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Create file I/O and system builtin flt tests (TST-09 through TST-13)</name>
  <files>
    tests/flt/file/fileio/fileio-write-read.flt
    tests/flt/file/fileio/fileio-file-exists.flt
    tests/flt/file/fileio/fileio-append.flt
    tests/flt/file/fileio/fileio-write-read-lines.flt
    tests/flt/file/fileio/fileio-path-combine.flt
    tests/flt/file/fileio/fileio-dir-files.flt
    tests/flt/file/fileio/fileio-get-cwd.flt
    tests/flt/file/fileio/fileio-get-env-missing.flt
  </files>
  <action>
Create the directory `tests/flt/file/fileio/` and write 8 .flt files.

Key constraints:
- `get_args`: Cannot pass command-line args in flt format — SKIP, no flt test for this.
- `get_env`: Environment variables are unpredictable in test runs. Test the MISSING variable case (exception), since that is deterministic.
- `get_cwd`: Returns the actual working directory. The test can verify the cwd is a non-empty string (print its length > 0), but cannot predict the exact path. Use `string_length (get_cwd ()) > 0` and print `true`.
- `eprint`: Writes to stderr, not captured by flt output comparison. SKIP, no flt test feasible.
- All file paths for I/O tests use `/tmp/flt-fileio-<name>.txt` to avoid collisions.

Files to create:

**fileio-write-read.flt** — TST-09: write_file then read_file round-trip
```
// Test: write_file writes content and read_file reads it back
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_file "/tmp/flt-fileio-rw.txt" "hello world"
let result = read_file "/tmp/flt-fileio-rw.txt"
// --- Output:
"hello world"
```

**fileio-file-exists.flt** — TST-10: file_exists returns true after write, false for nonexistent
```
// Test: file_exists returns true for existing file and false for missing file
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_file "/tmp/flt-fileio-exists.txt" "data"
let exists = file_exists "/tmp/flt-fileio-exists.txt"
let missing = file_exists "/tmp/flt-fileio-definitely-not-here-xyzzy.txt"
let result = exists
// --- Output:
true
```

**fileio-append.flt** — TST-10: append_file appends content
```
// Test: append_file appends to existing file content
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_file "/tmp/flt-fileio-append.txt" "hello"
let _ = append_file "/tmp/flt-fileio-append.txt" " world"
let result = read_file "/tmp/flt-fileio-append.txt"
// --- Output:
"hello world"
```

**fileio-write-read-lines.flt** — TST-11: write_lines then read_lines round-trip
Writes a list of strings as lines, reads them back as a list, then accesses an element:
```
// Test: write_lines writes list as lines and read_lines reads them back
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_lines "/tmp/flt-fileio-lines.txt" ["line1"; "line2"; "line3"]
let lines = read_lines "/tmp/flt-fileio-lines.txt"
let result = List.length lines
// --- Output:
3
```

Note: `List.length` must be available. If not, use pattern matching to verify list length. Alternatively, use `List.head` to get the first element and verify it equals `"line1"`. Choose the simpler approach that is confirmed to work.

**fileio-path-combine.flt** — TST-13: path_combine joins two path segments
```
// Test: path_combine joins directory and filename
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = path_combine "/tmp" "test.txt"
// --- Output:
"/tmp/test.txt"
```

Note: On macOS, `System.IO.Path.Combine "/tmp" "test.txt"` produces `"/tmp/test.txt"`. Verify by running the binary before finalizing.

**fileio-dir-files.flt** — TST-13: dir_files returns file list for a directory
The test writes a file to a known temp dir and verifies dir_files returns a non-empty list:
```
// Test: dir_files returns list of files in a directory
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let _ = write_file "/tmp/flt-dirfiles-test.txt" "data"
let files = dir_files "/tmp"
let result = List.length files > 0
// --- Output:
true
```

Note: `/tmp` always has files on macOS, so `List.length files > 0` is always true. If `List.length` is not available, use `match files with | [] -> false | _ -> true`.

**fileio-get-cwd.flt** — TST-12: get_cwd returns a non-empty string
```
// Test: get_cwd returns the current working directory as a non-empty string
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let cwd = get_cwd ()
let result = string_length cwd > 0
// --- Output:
true
```

Note: `string_length` must be available in prelude. Verify by running the binary. If not available, use `cwd <> ""` instead.

**fileio-get-env-missing.flt** — TST-12: get_env throws exception for missing variable
```
// Test: get_env raises LangThreeException for unset environment variable
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result =
    try get_env "FLT_TEST_DEFINITELY_UNSET_XYZZY_12345"
    with e -> "caught"
// --- Output:
"caught"
```

Note: `get_env` raises `LangThreeException` when variable is not set. The try/with pattern should catch it. Verify the exception is catchable this way by running the binary.

For each file: run the input through the binary before finalizing to confirm the expected output is correct. Adjust the expected output in the .flt file if needed (never modify source .fs files).
  </action>
  <verify>Run each .flt input manually through the binary and confirm output matches the Output section. Note any tests that need adjustment and fix the .flt expected output accordingly.</verify>
  <done>8 .flt files exist in tests/flt/file/fileio/. All execute without error and each produces output matching its // --- Output: section when run through the binary.</done>
</task>

<task type="auto">
  <name>Task 3: Run dotnet test and verify final count</name>
  <files></files>
  <action>
Run `dotnet test` from the repo root and verify all tests pass with increased total count.

Command: `cd /Users/ohama/vibe-coding/LangThree && dotnet test`

Expected results:
- All existing 447+ tests still pass (no regressions from Plans 01 and 02)
- New flt tests in tests/flt/file/char/, tests/flt/file/import/, tests/flt/file/fileio/ are picked up
- Total flt test count exceeds 447 + 11 (from Plan 01) = 458 minimum

If any new test fails:
1. Read the test name and failure output carefully
2. If the expected output in the .flt file is wrong, fix the .flt file
3. Do NOT modify any .fs source files — only .flt test files

Skipped tests (no flt feasible due to technical constraints):
- `get_args`: Cannot pass CLI args in flt framework
- `eprint`: Writes to stderr, not captured by flt output comparison

These will remain unit-tested only (in IntegrationTests.fs / ModuleTests.fs). Document in SUMMARY.
  </action>
  <verify>`dotnet test` exits with code 0 showing all tests passing and total count > 458.</verify>
  <done>dotnet test passes with zero failures. Total flt test count increased from 447 by at least 16 new tests (11 from Plan 01 + 5+ from Plan 02, excluding infeasible tests). All Phase 29-32 features covered by flt tests where technically possible.</done>
</task>

</tasks>

<verification>
1. `ls tests/flt/file/import/` shows file-import-basic.flt and file-import-qualified.flt (or SUMMARY documents infeasibility)
2. `ls tests/flt/file/fileio/` shows 8 .flt files
3. `dotnet test` passes with total count > 458
4. No FS source files were modified (only .flt files created)
</verification>

<success_criteria>
- TST-07: file-import-basic.flt tests `open "lib.fun"` basic import (or documented as infeasible)
- TST-08: file-import-qualified.flt tests qualified access to imported module binding (or documented)
- TST-09: fileio-write-read.flt tests write_file/read_file round-trip
- TST-10: fileio-file-exists.flt and fileio-append.flt test file_exists and append_file
- TST-11: fileio-write-read-lines.flt tests read_lines/write_lines round-trip
- TST-12: fileio-get-cwd.flt tests get_cwd returns non-empty string; fileio-get-env-missing.flt tests get_env exception for unset var
- TST-13: fileio-path-combine.flt and fileio-dir-files.flt test path_combine and dir_files
- `dotnet test` shows all tests passing with count > 458
</success_criteria>

<output>
After completion, create `.planning/phases/34-test-coverage/34-02-SUMMARY.md`
</output>
