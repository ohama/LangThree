---
phase: 34-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/flt/file/char/char-literal.flt
  - tests/flt/file/char/char-to-int.flt
  - tests/flt/file/char/int-to-char.flt
  - tests/flt/file/char/char-compare-eq.flt
  - tests/flt/file/char/char-compare-lt.flt
  - tests/flt/file/char/char-compare-gt.flt
  - tests/flt/file/let/let-rec-multiparam.flt
  - tests/flt/file/let/let-rec-multiparam-accumulator.flt
  - tests/flt/file/let/unit-param-shorthand-module.flt
  - tests/flt/file/let/unit-param-shorthand-expr.flt
  - tests/flt/file/let/top-level-let-in.flt
autonomous: true

must_haves:
  truths:
    - "flt test for char literal ('a', 'Z', '0') exists in tests/flt/file/char/ and passes"
    - "flt test for char_to_int/int_to_char conversions exists and passes"
    - "flt test for char comparison with <, >, = operators exists and passes"
    - "flt test for multi-param let rec (e.g. let rec add a b = ...) exists and passes"
    - "flt test for unit param shorthand (let f () = body) at module level exists and passes"
    - "flt test for top-level let...in expression exists and passes"
    - "all new .flt files produce the correct output when run through the LangThree binary"
  artifacts:
    - path: "tests/flt/file/char/"
      provides: "char feature regression tests"
    - path: "tests/flt/file/let/let-rec-multiparam.flt"
      provides: "multi-param let rec test"
    - path: "tests/flt/file/let/unit-param-shorthand-module.flt"
      provides: "unit param shorthand test"
    - path: "tests/flt/file/let/top-level-let-in.flt"
      provides: "top-level let-in expression test"
  key_links:
    - from: "tests/flt/file/char/*.flt"
      to: "/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree"
      via: "// --- Command: line in each .flt file"
      pattern: "Command:.*LangThree %input"
---

<objective>
Add flt regression tests for char literals/builtins (Phase 29) and parser improvements (Phase 30).

Purpose: These features have F# unit test coverage but no flt tests. flt tests verify the full binary pipeline (lex → parse → eval → print) and serve as end-to-end regression guards.
Output: 11 new .flt files covering TST-01, TST-02, TST-03, TST-04, TST-05, TST-06. The total flt test count increases from 447.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/flt/file/tco/tco-simple-loop.flt
@tests/flt/file/let/let-sequence-complex.flt
@tests/flt/file/implicit-in/implicit-in-let-rec.flt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create char feature flt tests (TST-01, TST-02, TST-03)</name>
  <files>
    tests/flt/file/char/char-literal.flt
    tests/flt/file/char/char-to-int.flt
    tests/flt/file/char/int-to-char.flt
    tests/flt/file/char/char-compare-eq.flt
    tests/flt/file/char/char-compare-lt.flt
    tests/flt/file/char/char-compare-gt.flt
  </files>
  <action>
Create the directory `tests/flt/file/char/` and write 6 .flt files.

Use this exact header format (same as other file-mode flt tests):
```
// Test: <description>
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
<program source>
// --- Output:
<expected output>
```

Files to create:

**char-literal.flt** — TST-01: char literals print correctly
```
// Test: char literals are printed with single quotes
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let a = 'a'
let z = 'Z'
let zero = '0'
let result = a
// --- Output:
'a'
```

**char-to-int.flt** — TST-02: char_to_int conversion
```
// Test: char_to_int converts a char to its ASCII code
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = char_to_int 'A'
// --- Output:
65
```

**int-to-char.flt** — TST-02: int_to_char conversion
```
// Test: int_to_char converts an ASCII code to its char
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = int_to_char 65
// --- Output:
'A'
```

**char-compare-eq.flt** — TST-03: char equality
```
// Test: char equality comparison
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = 'a' = 'a'
// --- Output:
true
```

**char-compare-lt.flt** — TST-03: char less-than
```
// Test: char less-than comparison
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = 'a' < 'z'
// --- Output:
true
```

**char-compare-gt.flt** — TST-03: char greater-than
```
// Test: char greater-than comparison
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result = 'z' > 'a'
// --- Output:
true
```

Verify each file by running the binary manually:
```
/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree <(echo "let result = 'a'")
```
Expected output for char literal: `'a'`

Note: The interpreter prints the last bound `result` identifier. When the program sets `let result = <char>`, the output is `'a'` (with single quotes). Verify this is the actual output before finalizing the files.
  </action>
  <verify>Run each .flt file through the binary: `/Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree /tmp/test.fun` where /tmp/test.fun contains the Input section. Confirm output matches the Output section exactly.</verify>
  <done>6 .flt files exist under tests/flt/file/char/. Each runs without error and produces the expected output when executed manually.</done>
</task>

<task type="auto">
  <name>Task 2: Create parser improvement flt tests (TST-04, TST-05, TST-06)</name>
  <files>
    tests/flt/file/let/let-rec-multiparam.flt
    tests/flt/file/let/let-rec-multiparam-accumulator.flt
    tests/flt/file/let/unit-param-shorthand-module.flt
    tests/flt/file/let/unit-param-shorthand-expr.flt
    tests/flt/file/let/top-level-let-in.flt
  </files>
  <action>
Write 5 .flt files in the existing `tests/flt/file/let/` directory. Use the same header format as Task 1.

**let-rec-multiparam.flt** — TST-04: multi-param let rec at top level
```
// Test: let rec with multiple parameters at top level
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let rec add a b = if b = 0 then a else add (a + 1) (b - 1)
let result = add 3 4
// --- Output:
7
```

**let-rec-multiparam-accumulator.flt** — TST-04: multi-param let rec inside expression
```
// Test: let rec with multiple params inside expression body
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result =
    let rec helper acc k =
        if k <= 1 then acc
        else helper (acc * k) (k - 1)
    in helper 1 5
// --- Output:
120
```

**unit-param-shorthand-module.flt** — TST-05: unit param shorthand at module level
```
// Test: let f () = body shorthand at module level
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let greet () = 42
let result = greet ()
// --- Output:
42
```

**unit-param-shorthand-expr.flt** — TST-05: unit param shorthand in expression context
```
// Test: let f () = body shorthand in expression context
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result =
    let f () = 99
    in f ()
// --- Output:
99
```

**top-level-let-in.flt** — TST-06: top-level let...in expression
```
// Test: top-level let x = e1 in e2 works as module declaration
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let base = 10
let result = let x = base + 5 in x * 2
// --- Output:
30
```

After writing, verify each file by running the binary against the Input content and confirming the output matches.
  </action>
  <verify>Run each .flt input section through the binary and confirm outputs: 7, 120, 42, 99, 30 respectively. `dotnet test` run is NOT needed here — just binary verification to confirm expected values before committing files.</verify>
  <done>5 .flt files exist in tests/flt/file/let/. Each produces the correct output when manually executed through the binary.</done>
</task>

<task type="auto">
  <name>Task 3: Run dotnet test and verify count increased</name>
  <files></files>
  <action>
Run `dotnet test` from the repo root and verify:
1. All existing 447 flt tests still pass (no regressions)
2. The new flt tests are picked up and pass
3. Total flt test count is now > 447

Command: `cd /Users/ohama/vibe-coding/LangThree && dotnet test`

If any new test fails, read the failure message, identify whether the expected output is wrong (fix the .flt file) or the binary has a bug (investigate). The most likely issue is an incorrect expected output in a .flt file (e.g., char literal format mismatch). Correct the .flt file and re-run.

Do NOT modify any source .fs files in this task — only .flt files.
  </action>
  <verify>`dotnet test` output shows all tests passing with total test count > 447.</verify>
  <done>dotnet test passes with zero failures and the total count of flt tests exceeds 447.</done>
</task>

</tasks>

<verification>
1. `ls tests/flt/file/char/` shows 6 .flt files
2. `ls tests/flt/file/let/` shows new .flt files including let-rec-multiparam.flt, unit-param-shorthand-module.flt, top-level-let-in.flt
3. `dotnet test` passes with count > 447
</verification>

<success_criteria>
- TST-01: char-literal.flt tests 'a' literal output
- TST-02: char-to-int.flt and int-to-char.flt test round-trip conversion
- TST-03: char-compare-eq/lt/gt.flt test all three char comparison operators
- TST-04: let-rec-multiparam.flt and let-rec-multiparam-accumulator.flt test multi-param let rec
- TST-05: unit-param-shorthand-module.flt and unit-param-shorthand-expr.flt test unit param shorthand
- TST-06: top-level-let-in.flt tests top-level let...in expression
- `dotnet test` shows all tests passing with count > 447
</success_criteria>

<output>
After completion, create `.planning/phases/34-test-coverage/34-01-SUMMARY.md`
</output>
