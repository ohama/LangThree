---
phase: 38-array-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/LangThree/Ast.fs
  - src/LangThree/Type.fs
  - src/LangThree/Unify.fs
  - src/LangThree/Bidir.fs
  - src/LangThree/Eval.fs
autonomous: true

must_haves:
  truths:
    - "Project compiles with zero warnings after adding ArrayValue and TArray"
    - "ArrayValue carries a Value array (no outer ref) and satisfies CustomEquality obligations"
    - "TArray propagates through apply, freeVars, formatType, formatTypeNormalized, collectVars"
    - "Unify handles TArray t1, TArray t2 with one new arm"
    - "Bidir NotAFunction guard includes TArray so applying an array gives a clear error"
    - "formatValue in Eval.fs prints ArrayValue as [|e1; e2; ...|]"
  artifacts:
    - path: "src/LangThree/Ast.fs"
      provides: "ArrayValue DU case with CustomEquality/Comparison arms"
      contains: "ArrayValue of Value array"
    - path: "src/LangThree/Type.fs"
      provides: "TArray type constructor with all propagation arms"
      contains: "TArray of Type"
    - path: "src/LangThree/Unify.fs"
      provides: "TArray unification arm"
      contains: "TArray t1, TArray t2"
    - path: "src/LangThree/Bidir.fs"
      provides: "TArray in NotAFunction guard"
      contains: "TArray _"
    - path: "src/LangThree/Eval.fs"
      provides: "formatValue arm for ArrayValue"
      contains: "ArrayValue arr ->"
  key_links:
    - from: "src/LangThree/Ast.fs"
      to: "GetHashCode/valueEqual/valueCompare/valuesEqual"
      via: "CustomEquality attribute obligations"
      pattern: "ArrayValue"
    - from: "src/LangThree/Type.fs"
      to: "apply/freeVars/formatType/formatTypeNormalized/collectVars"
      via: "match arms for TArray"
      pattern: "TArray"
---

<objective>
Add the ArrayValue runtime value case and TArray type constructor as pure infrastructure — no builtins yet, just the DU cases and all propagation arms so the project compiles cleanly.

Purpose: Plan 38-02 cannot add Array builtins until ArrayValue and TArray exist and the project compiles. This plan creates that foundation.
Output: Zero-warning build with ArrayValue in Ast.fs and TArray in Type.fs plus all required propagation arms.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-array-type/38-RESEARCH.md
@src/LangThree/Ast.fs
@src/LangThree/Type.fs
@src/LangThree/Unify.fs
@src/LangThree/Bidir.fs
@src/LangThree/Eval.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ArrayValue DU case to Ast.fs with all CustomEquality obligations</name>
  <files>src/LangThree/Ast.fs</files>
  <action>
Add `| ArrayValue of Value array` to the Value DU after the `ListValue` case (line ~190).

Then add arms to all four CustomEquality obligation sites:

1. `GetHashCode` — add after `| ListValue vs -> hash vs`:
   ```fsharp
   | ArrayValue arr -> System.Runtime.CompilerServices.RuntimeHelpers.GetHashCode(arr)
   ```

2. `valueEqual` (static member) — add after `| ListValue a, ListValue b -> a = b`:
   ```fsharp
   | ArrayValue r1, ArrayValue r2 -> System.Object.ReferenceEquals(r1, r2)
   ```

3. `valueCompare` (static member) — add after `| TailCall _, _ | _, TailCall _ -> 0`:
   ```fsharp
   | ArrayValue _, _ | _, ArrayValue _ -> 0
   ```

4. In `Eval.fs` there is also a `valuesEqual` function (used in `matchPattern` for `ConstPat`). After handling `ListValue` there, add:
   ```fsharp
   | ArrayValue _, ArrayValue _ -> false
   ```
   (Two different array objects are never equal by value — consistent with reference equality.)

NOTE: Do NOT use `Value array ref` — use `Value array` directly (no outer ref cell). In-place mutation via `arr.[i] <- v` does not need an outer ref. Using an outer ref is an anti-pattern for fixed-size arrays (see RESEARCH.md).
  </action>
  <verify>dotnet build src/LangThree/LangThree.fsproj 2>&1 | grep -E "warning|error" | head -30</verify>
  <done>Zero compiler errors and zero exhaustiveness warnings related to ArrayValue in Ast.fs and Eval.fs valuesEqual.</done>
</task>

<task type="auto">
  <name>Task 2: Add TArray to Type.fs and all propagation sites (Type.fs, Unify.fs, Bidir.fs) plus formatValue in Eval.fs</name>
  <files>src/LangThree/Type.fs, src/LangThree/Unify.fs, src/LangThree/Bidir.fs, src/LangThree/Eval.fs</files>
  <action>
**Type.fs** — Add `TArray` to the Type DU after `TList`:
```fsharp
| TArray of Type   // 'a array (Phase 38)
```

Then add arms at every location that matches on `TList` — add `TArray` arm directly after each `TList` arm:

- `formatType`: `| TArray t -> sprintf "%s array" (formatType t)`
- `formatTypeNormalized` (inner `format` function): `| TArray t -> sprintf "%s array" (format t)`
- `formatTypeNormalized` (inner `collectVars` function): `| TArray t -> collectVars acc t`
- `apply`: `| TArray t -> TArray (apply s t)`
- `freeVars`: `| TList t -> freeVars t` already exists; add `| TArray t -> freeVars t` after it

**Unify.fs** — Find the `| TList t1, TList t2 ->` arm and add directly after it:
```fsharp
| TArray t1, TArray t2 ->
    unifyWithContext ctx trace span t1 t2
```

**Bidir.fs** — Find the NotAFunction guard line (around line 111):
```fsharp
| TInt | TBool | TString | TTuple _ | TList _ | TData _ ->
```
Add `| TChar | TExn` if missing, and add `TArray _` to the list:
```fsharp
| TInt | TBool | TString | TChar | TTuple _ | TList _ | TData _ | TArray _ ->
```
(Check the exact existing list first and only add `TArray _` without disrupting existing cases.)

**Eval.fs** — In `formatValue`, after the `ListValue` arm, add:
```fsharp
| ArrayValue arr ->
    let formattedElements = arr |> Array.toList |> List.map formatValue
    sprintf "[|%s|]" (String.concat "; " formattedElements)
```
  </action>
  <verify>dotnet build src/LangThree/LangThree.fsproj 2>&1 | grep -E "warning FS|error FS"</verify>
  <done>Zero compiler warnings and errors. `dotnet build` reports "Build succeeded" with 0 warnings.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```
cd /path/to/LangThree && dotnet test
```
All existing tests must still pass — this plan only adds new DU cases, it does not change any existing logic.
</verification>

<success_criteria>
- `dotnet build` reports 0 warnings, 0 errors
- `dotnet test` shows the same pass/fail count as before this plan
- `ArrayValue of Value array` exists in Ast.fs Value DU
- `TArray of Type` exists in Type.fs Type DU
- All propagation arms present: apply, freeVars, formatType, formatTypeNormalized, collectVars, Unify, Bidir NotAFunction guard, Eval formatValue, Eval valuesEqual
</success_criteria>

<output>
After completion, create `.planning/phases/38-array-type/38-01-SUMMARY.md`
</output>
