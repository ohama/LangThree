// Test: chained composition with multiple functions
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let add1 x = x + 1
let mul2 x = x * 2
let sub3 x = x - 3
let pipeline = add1 >> mul2 >> sub3
let result = pipeline 10
// --- Output:
19
