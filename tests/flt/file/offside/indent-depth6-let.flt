// Test: 6-level deep nested let bindings
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let result =
    let a =
        let b =
            let c =
                let d =
                    let e =
                        let f = 1
                        f * 2
                    e * 2
                d * 2
            c * 2
        b * 2
    a * 2
// --- Output:
64
