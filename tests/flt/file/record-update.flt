// Test: record copy-and-update syntax
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
type Point = { x: int; y: int }

let p = { x = 1; y = 2 }

let p2 = { p with x = 10 }

let result = p2.x + p2.y
// --- Output:
12
