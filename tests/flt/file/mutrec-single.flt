// Test: single let rec at module level (self-recursion without in)
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
let rec fact n = if n <= 1 then 1 else n * fact (n - 1)
let result = fact 10
// --- Output:
3628800
