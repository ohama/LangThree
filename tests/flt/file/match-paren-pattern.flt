// Test: parenthesized patterns in match
// --- Command: /Users/ohama/vibe-coding/LangThree/src/LangThree/bin/Release/net10.0/LangThree %input
// --- Input:
type Option 'a = None | Some of 'a

let x = Some 42
let result =
    match x with
    | (Some v) -> v
    | None -> 0
// --- Stdout:
42
