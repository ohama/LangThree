# LangThree Language Specification

ML 계열 함수형 프로그래밍 언어 LangThree의 형식 정의.

## 1. Lexical Specification

### 1.1 Character Classes

```
digit       = ['0'-'9']
letter      = ['a'-'z' 'A'-'Z']
whitespace  = [' ' '\t']
newline     = '\n' | '\r\n'
ident_start = letter | '_'
ident_char  = letter | digit | '_'
type_var    = '\'' letter (letter | digit | '_')*
op_char     = ['!' '$' '%' '&' '*' '+' '-' '.' '/' '<' '=' '>' '?' '@' '^' '|' '~']
```

### 1.2 Keywords (27)

| Token | Keyword | Token | Keyword |
|-------|---------|-------|---------|
| LET | `let` | IN | `in` |
| FUN | `fun` | REC | `rec` |
| IF | `if` | THEN | `then` |
| ELSE | `else` | MATCH | `match` |
| WITH | `with` | TYPE | `type` |
| OF | `of` | AND_KW | `and` |
| TRUE | `true` | FALSE | `false` |
| MODULE | `module` | NAMESPACE | `namespace` |
| OPEN | `open` | EXCEPTION | `exception` |
| RAISE | `raise` | TRY | `try` |
| WHEN | `when` | MUTABLE | `mutable` |
| CHAR | `char` | MUT | `mut` |
| WHILE | `while` | FOR | `for` |
| TO | `to` | DOWNTO | `downto` |
| DO | `do` | | |

### 1.3 Type Keywords (6)

| Token | Keyword |
|-------|---------|
| TYPE_INT | `int` |
| TYPE_BOOL | `bool` |
| TYPE_STRING | `string` |
| TYPE_LIST | `list` |
| TYPE_UNIT | `unit` |
| TYPE_CHAR | `char` |

### 1.4 Operators and Delimiters

**Single-character:**

| Token | Symbol | Token | Symbol |
|-------|--------|-------|--------|
| PLUS | `+` | MINUS | `-` |
| STAR | `*` | SLASH | `/` |
| PERCENT | `%` | EQUALS | `=` |
| LT | `<` | GT | `>` |
| PIPE | `\|` | DOT | `.` |
| COMMA | `,` | SEMICOLON | `;` |
| COLON | `:` | LPAREN | `(` |
| RPAREN | `)` | LBRACKET | `[` |
| RBRACKET | `]` | LBRACE | `{` |
| RBRACE | `}` | UNDERSCORE | `_` |

**Multi-character:**

| Token | Symbol | Token | Symbol |
|-------|--------|-------|--------|
| LE | `<=` | GE | `>=` |
| NE | `<>` | AND | `&&` |
| OR | `\|\|` | CONS | `::` |
| ARROW | `->` | LARROW | `<-` |
| DOTDOT | `..` | DOTLBRACKET | `.[` |
| PIPE_RIGHT | `\|>` | | |
| COMPOSE_RIGHT | `>>` | COMPOSE_LEFT | `<<` |

### 1.5 Literals

| Token | Pattern | Example |
|-------|---------|---------|
| NUMBER | `digit+` | `42`, `0`, `100` |
| STRING | `"..."` (escapes: `\n`, `\t`, `\\`, `\"`) | `"hello\nworld"` |
| CHAR | `'\'' char '\''` (escapes: `\n`, `\t`, `\\`, `\'`) | `'a'`, `'\n'` |
| IDENT | `ident_start ident_char*` | `x`, `myFunc`, `_temp` |
| TYPE_VAR | `'\'' letter ident_char*` | `'a`, `'key` |

### 1.6 User-Defined Operators (INFIXOP)

2文字以上の `op_char` 列は、先頭文字で優先順位が決まる:

| First Character | Token | Precedence | Associativity | Example |
|----------------|-------|------------|---------------|---------|
| `=` `<` `>` `\|` `&` `$` `!` | INFIXOP0 | comparison level | left | `<\|>` |
| `@` `^` | INFIXOP1 | concatenation | right | `^^`, `++` |
| `+` `-` | INFIXOP2 | additive | left | custom `+.` |
| `*` `/` `%` | INFIXOP3 | multiplicative | left | custom `*.` |
| `**` prefix | INFIXOP4 | exponentiation | right | `**` |

Single-character operators (`+`, `-`, `*`, `/`, `%`, `<`, `>`, `=`) are always lexed as built-in tokens, not INFIXOP.

### 1.7 Special Tokens

| Token | Description |
|-------|-------------|
| NEWLINE(col) | Line break + next line's indentation column |
| INDENT | Generated by IndentFilter (column > stack top) |
| DEDENT | Generated by IndentFilter (column < stack top) |
| EOF | End of input |

### 1.8 Comments

```
// single-line comment (to end of line)
(* block comment — nestable *)
```

### 1.9 Whitespace Rules

- Tabs are forbidden — spaces only
- Indentation is significant (F# style offside rule)
- Blank lines and comment-only lines do not generate NEWLINE tokens

## 2. Operator Precedence (lowest to highest)

| Level | Operators | Associativity |
|-------|-----------|---------------|
| 1 | `\|>` | left |
| 2 | `>>` | left |
| 3 | `<<` | right |
| 4 | `\|\|` | left |
| 5 | `&&` | left |
| 6 | `=` `<` `>` `<=` `>=` `<>` | nonassoc |
| 7 | INFIXOP0 (custom) | left |
| 8 | INFIXOP1 (custom), `::` | right |
| 9 | `+` `-`, INFIXOP2 (custom) | left |
| 10 | `*` `/` `%`, INFIXOP3 (custom) | left |
| 11 | INFIXOP4 (custom) | right |
| 12 | Function application (juxtaposition) | left |
| 13 | Unary `-`, `raise` | prefix |

## 3. Grammar

### 3.1 Program Structure

```
Program     ::= Module
Module      ::= 'module' QualifiedIdent Decls EOF
              | 'namespace' QualifiedIdent Decls EOF
              | Decls EOF

QualifiedIdent ::= IDENT ('.' IDENT)*
```

### 3.2 Declarations

```
Decls       ::= Decl*

Decl        ::= LetDecl | TypeDecl | RecordDecl | TypeAliasDecl
              | LetRecDecl | LetMutDecl | ModuleDecl | OpenDecl | ExceptionDecl
              | LetPatDecl | FileImportDecl

LetDecl     ::= 'let' IDENT '=' Expr
              | 'let' IDENT ParamList '=' Expr
              | 'let' '_' '=' Expr
              | 'let' OpName ParamList '=' Expr

LetRecDecl  ::= 'let' 'rec' IDENT ParamList '=' Expr
                ('and' IDENT ParamList '=' Expr)*

LetMutDecl  ::= 'let' ('mut' | 'mutable') IDENT '=' Expr

TypeDecl    ::= 'type' IDENT TypeParams '=' ['|'] Constructors
                ('and' IDENT TypeParams '=' ['|'] Constructors)*

RecordDecl  ::= 'type' IDENT TypeParams '=' '{' RecordFields '}'

TypeAliasDecl ::= 'type' IDENT TypeParams '=' TypeExpr

ModuleDecl  ::= 'module' IDENT '=' INDENT Decls DEDENT

OpenDecl    ::= 'open' QualifiedIdent

FileImportDecl ::= 'open' STRING                // file import (distinct from module open)

LetPatDecl  ::= 'let' '(' Pattern ',' Pattern ')' '=' Expr  // module-level tuple destructuring

ExceptionDecl ::= 'exception' IDENT ('of' TypeExpr)?

ParamList   ::= IDENT+
OpName      ::= '(' INFIXOP ')'
TypeParams  ::= TYPE_VAR*
```

### 3.3 Constructors

```
Constructors ::= Constructor ('|' Constructor)*

Constructor  ::= IDENT                              // nullary ADT
               | IDENT 'of' TypeExpr                // ADT with data
               | IDENT ':' TypeExpr '->' TypeExpr   // GADT
```

### 3.4 Expressions

```
Expr    ::= 'let' IDENT '=' Expr 'in' Expr
          | 'let' IDENT ParamList '=' Expr 'in' Expr
          | 'let' TuplePattern '=' Expr 'in' Expr
          | 'let' '_' '=' Expr 'in' Expr
          | 'let' 'rec' IDENT IDENT '=' Expr 'in' Expr
          | 'let' ('mut' | 'mutable') IDENT '=' Expr 'in' Expr
          | IDENT '<-' Expr
          | 'if' Expr 'then' Expr 'else' Expr
          | 'fun' IDENT '->' Expr
          | 'fun' '(' IDENT ':' TypeExpr ')' '->' Expr
          | 'fun' '(' ')' '->' Expr
          | 'fun' TuplePattern '->' Expr
          | 'match' Expr 'with' MatchClauses
          | 'try' Expr 'with' MatchClauses
          | 'try' Expr 'with' IDENT '->' Expr   // inline try-with without pipe (v2.2)
          | Expr '|>' Expr
          | Expr '>>' Expr
          | Expr '<<' Expr
          | Expr '||' Expr
          | Expr '&&' Expr
          | Expr CompOp Expr
          | Expr '::' Expr
          | Expr INFIXOP Expr
          | Expr '+' Term | Expr '-' Term
          | Atom '.' IDENT '<-' Expr
          | INDENT Expr DEDENT
          | Term

CompOp  ::= '=' | '<' | '>' | '<=' | '>=' | '<>'

Term    ::= Term '*' Factor | Term '/' Factor | Term '%' Factor
          | Factor

Factor  ::= '-' Factor | 'raise' Atom
          | AppExpr

AppExpr ::= AppExpr Atom | Atom

Atom    ::= '(' ')'                    // unit
          | NUMBER | IDENT | TRUE | FALSE | STRING | CHAR
          | '(' Expr ')'
          | '(' Expr ':' TypeExpr ')'  // type annotation
          | '(' Expr ',' ExprList ')'  // tuple
          | '[' ']'                     // empty list
          | '[' Expr ']'               // singleton list
          | '[' Expr ';' SemiExprList ']'  // list
          | '[' Expr '..' Expr ']'     // range
          | '[' Expr '..' Expr '..' Expr ']'  // range with step
          | '(' INFIXOP ')'           // operator as function
          | Atom '.' IDENT            // field access
          | '{' RecordExprInner '}'   // record
```

### 3.5 Patterns

```
Pattern ::= IDENT                        // variable or nullary constructor
          | '_'                           // wildcard
          | NUMBER | '-' NUMBER           // int constant (positive/negative)
          | TRUE | FALSE                  // bool constant
          | STRING                        // string constant
          | CHAR                         // char constant
          | '(' PatternList ')'          // tuple pattern
          | '[' ']'                       // empty list
          | Pattern '::' Pattern         // cons
          | IDENT Pattern                // constructor with argument
          | '{' RecordPatFields '}'      // record pattern
          | '[' Pattern (';' Pattern)* ']'  // list literal pattern (e.g. [x; y; z])

OrPattern   ::= Pattern ('|' Pattern)*
MatchClause ::= '|' OrPattern ('when' Expr)? '->' Expr
MatchClauses ::= MatchClause+

PatternList  ::= Pattern ',' Pattern (',' Pattern)*
```

### 3.6 Type Expressions

```
TypeExpr    ::= TupleType '->' TypeExpr   // function type
              | TupleType

TupleType   ::= AtomicType '*' TupleType  // tuple type
              | AtomicType

AtomicType  ::= 'int' | 'bool' | 'string' | 'unit' | 'char'
              | TYPE_VAR                   // 'a, 'b, ...
              | IDENT                      // named type
              | AtomicType 'list'          // list type
              | AtomicType 'array'         // array type
              | AtomicType IDENT           // parameterized type (e.g., int Expr)
              | '(' TypeExpr ')'
// Note: hashtable type is not yet expressible in type annotations
```

### 3.7 Records

```
RecordFields      ::= RecordField (';' RecordField)* ';'?
RecordField       ::= ['mutable'] IDENT ':' TypeExpr

RecordExprInner   ::= RecordFieldBindings
                    | Expr 'with' RecordFieldBindings
RecordFieldBindings ::= IDENT '=' Expr (';' IDENT '=' Expr)* ';'?

RecordPatFields   ::= IDENT '=' Pattern (';' IDENT '=' Pattern)* ';'?
```

### 3.8 Lists

```
ExprList     ::= Expr (',' Expr)*          // for tuples (comma-separated)
SemiExprList ::= Expr (';' Expr)*          // for lists (semicolon-separated)
```

## 4. Indentation Rules (IndentFilter)

### 4.1 INDENT / DEDENT Generation

Python 알고리즘 기반. Indent stack `[0]`에서 시작:

- `col > top` → INDENT 발생, col을 스택에 push
- `col = top` → 동일 레벨 (토큰 없음)
- `col < top` → DEDENT 발생, top을 pop (반복 가능 — 다단계 dedent)

### 4.2 Context Stack

IndentFilter는 다음 문맥을 추적:

| Context | Trigger | Effect |
|---------|---------|--------|
| InMatch(col) | `match` 토큰 + NEWLINE | `\|` 파이프를 col에 정렬 강제 |
| InTry(col) | `try` 토큰 + NEWLINE | `\|` 파이프를 col에 정렬 강제 |
| InLetDecl(blockLet, offsideCol) | `let` in expression context | blockLet=true면 offside column에서 implicit `in` 삽입 |
| InExprBlock(col) | INDENT after `=`, `->`, `in` | Expression block 진입 |
| InModule | INDENT after `module` | Module body (implicit `in` 삽입 안 함) |
| InFunctionApp(col) | Multi-line function application | 자동 INDENT/DEDENT |

### 4.3 Offside Rule (Implicit `in`)

Expression context 안의 `let`은 `InLetDecl(blockLet=true, offsideCol)` 컨텍스트를 push. 다음 토큰의 column이 offsideCol 이하이면 implicit `IN` 토큰 삽입:

```
let result =
    let x = 10       // InLetDecl(true, 4) push
    let y = 20       // col=4 <= offsideCol=4 → IN 삽입, InLetDecl pop, 새 InLetDecl push
    x + y            // col=4 <= offsideCol=4 → IN 삽입, InLetDecl pop
```

Module context의 `let`은 `blockLet=false` → implicit `in` 삽입하지 않음.

### 4.4 Pipe Alignment

`match`/`try` 뒤의 `|` 파이프는 `match`/`try` 키워드의 column에 정렬해야 함:

```
let result =
    match x with     // InMatch(4) push
    | 0 -> "zero"    // | must be at column 4
    | _ -> "other"   // | must be at column 4
```

## 5. Type System

### 5.1 Core Types

```
Type = int | bool | string | char | unit
     | 'a, 'b, ...                    // type variables
     | Type -> Type                    // function type
     | Type * Type * ...               // tuple type
     | Type list                       // list type
     | Type array                      // mutable array
     | (Type, Type) hashtable          // mutable hashtable
     | TypeName<Type, ...>             // parameterized type (ADT/GADT)
     | exn                             // exception type
```

### 5.2 Type Inference

Hindley-Milner 타입 추론 + bidirectional type checking:

- **Synthesis (synth)**: 표현식에서 타입을 추론
- **Checking (check)**: 표현식이 기대 타입과 일치하는지 검증
- **Let-polymorphism**: `let` 경계에서 타입 일반화 (generalization)

### 5.3 GADT Type Refinement

GADT 패턴 매칭에서 분기별 독립적 타입 정제:

```
type Expr 'a =
    | IntLit : int -> int Expr
    | BoolLit : bool -> bool Expr

let eval e =
    match e with
    | IntLit n -> n      // 이 분기: 'a = int, n : int
    | BoolLit b -> b     // 이 분기: 'a = bool, b : bool
```

- 주석 없음: 다형적 반환 (`eval : 'a Expr -> 'a`)
- `: int` 주석: 모든 분기가 int 반환 강제
- 불가능한 생성자 자동 제외 (completeness filtering)

### 5.4 Exhaustiveness Checking

패턴 매칭의 완전성 검사 (W0001 경고):
- 모든 ADT 생성자 커버 확인
- GADT에서는 타입 정보로 불가능한 생성자 필터링
- Or-pattern은 각 대안을 별도로 처리
- When guard가 있는 분기는 완전성에서 제외

### 5.5 Built-in Functions

**String / Output:**

| Function | Type | Description |
|----------|------|-------------|
| `string_length` | `string -> int` | 문자열 길이 |
| `string_concat` | `string -> string -> string` | 문자열 연결 |
| `string_sub` | `string -> int -> int -> string` | 부분 문자열 (start, length) |
| `string_contains` | `string -> string -> bool` | 포함 여부 |
| `to_string` | `'a -> string` | 값을 문자열로 변환 |
| `string_to_int` | `string -> int` | 문자열을 정수로 변환 |
| `print` | `string -> unit` | 출력 (줄바꿈 없음) |
| `println` | `string -> unit` | 출력 (줄바꿈) |
| `printf` | `string -> ...` | 형식 출력 (%d, %s, %b, %%) |
| `printfn` | `string -> ...` | 형식 출력 + 줄바꿈 |
| `sprintf` | `string -> ... -> string` | 형식 문자열 생성 |

**Char:**

| Function | Type | Description |
|----------|------|-------------|
| `char_to_int` | `char -> int` | ASCII code |
| `int_to_char` | `int -> char` | ASCII code → char |

**File I/O:**

| Function | Type | Description |
|----------|------|-------------|
| `read_file` | `string -> string` | Read file contents |
| `write_file` | `string -> string -> unit` | Write to file |
| `append_file` | `string -> string -> unit` | Append to file |
| `file_exists` | `string -> bool` | Check file existence |
| `read_lines` | `string -> string list` | Read lines |
| `write_lines` | `string -> string list -> unit` | Write lines |

**System:**

| Function | Type | Description |
|----------|------|-------------|
| `get_cwd` | `unit -> string` | Current directory |
| `get_env` | `string -> string` | Environment variable |
| `get_args` | `unit -> string list` | CLI arguments |
| `path_combine` | `string -> string -> string` | Join paths |
| `dir_files` | `string -> string list` | List files in directory |
| `eprint` | `string -> unit` | Stderr output |
| `stdin_read_line` | `unit -> string` | Read line from stdin |
| `failwith` | `string -> 'a` | Raise exception with message |

**Array:**

| Function | Type | Description |
|----------|------|-------------|
| `array_create` | `int -> 'a -> 'a array` | Create array |
| `array_get` | `'a array -> int -> 'a` | Get element |
| `array_set` | `'a array -> int -> 'a -> unit` | Set element |
| `array_length` | `'a array -> int` | Array length |
| `array_ofList` | `'a list -> 'a array` | List → array |
| `array_toList` | `'a array -> 'a list` | Array → list |
| `array_iter` | `('a -> unit) -> 'a array -> unit` | Iterate |
| `array_map` | `('a -> 'b) -> 'a array -> 'b array` | Map |
| `array_fold` | `('b -> 'a -> 'b) -> 'b -> 'a array -> 'b` | Fold |
| `array_init` | `int -> (int -> 'a) -> 'a array` | Initialize |

**Hashtable:**

| Function | Type | Description |
|----------|------|-------------|
| `hashtable_create` | `unit -> ('k,'v) hashtable` | Create empty hashtable |
| `hashtable_get` | `('k,'v) hashtable -> 'k -> 'v` | Get value |
| `hashtable_set` | `('k,'v) hashtable -> 'k -> 'v -> unit` | Set value |
| `hashtable_containsKey` | `('k,'v) hashtable -> 'k -> bool` | Check key |
| `hashtable_keys` | `('k,'v) hashtable -> 'k list` | Get all keys |
| `hashtable_remove` | `('k,'v) hashtable -> 'k -> unit` | Remove entry |

### 5.6 Prelude (Standard Library)

`Prelude/` 디렉토리의 `.fun` 파일이 시작 시 자동 로드:

**Types:** `Option 'a = None | Some of 'a`, `Result 'a 'b = Ok of 'a | Error of 'b`

**List functions:** `map`, `filter`, `fold`, `length`, `reverse`, `append`, `hd`, `tl`, `zip`, `take`, `drop`, `any`, `all`, `flatten`, `nth`

**Core functions:** `id`, `const`, `compose`, `flip`, `apply`, `not`, `min`, `max`, `abs`, `fst`, `snd`, `ignore`

**Operators:** `++` (list append), `<|>` (Option fallback), `^^` (string concat)

**Array functions (qualified):** `Array.create`, `Array.get`, `Array.set`, `Array.length`, `Array.ofList`, `Array.toList`, `Array.iter`, `Array.map`, `Array.fold`, `Array.init`

**Hashtable functions (qualified):** `Hashtable.create`, `Hashtable.get`, `Hashtable.set`, `Hashtable.containsKey`, `Hashtable.keys`, `Hashtable.remove`
